# 战斗系统决策记录

> 2026-08-16 起草并拍板。B1 开工前的关键决策已全部确认，本文档升级为**决策记录**。
> 关联：docs/概念提案-立项书.md（Demo 范围）、docs/04-回合流程.md、docs/05-行动系统.md、docs/06-元素与反应系统.md、docs/17-代码架构指南.md §7。
> 技术设计已落地 **docs/active/22-战斗系统技术设计.md**（2026-09-12；编号 19/21 已被 AI派蒙/商业化占用）。

---

## 一、联机权威模型（决策 3）

### 选项对比

| 维度 | A. Host 权威（结果同步） | B. 双端确定性（输入同步） |
|------|------------------------|-------------------------|
| 同步内容 | 客户端只上交**行动选择**；Host 收齐→排序→执行→广播**结果命令流** | 只同步行动选择，各端并行跑同一套逻辑 |
| 带宽 | 中（每回合一次结果流） | 极小（每回合一次选择） |
| 确定性要求 | **无**——浮点、协程动画、遍历顺序随便写 | 苛刻——C# 浮点跨平台不一致、容器遍历序、动画时序都要锁死 |
| 与现有架构契合 | ✅ 完全复用 Mirror + NetworkEventBus + OnlyHost + Immediate 模式（联机大厅已验证） | ❌ ActionQueue 协程串行 + 表现层动画耦合深，改造成本高 |
| 防作弊 | 天然免疫（客户端无逻辑权限） | 客户端可自改逻辑 |
| 单机模式 | Host=本地，零额外代码 | 同一套逻辑直接跑 |
| 断线重连 | 需补：结果流快照/追放（切片可先不做） | 天然支持（重放输入即可） |

### 推荐：A（Host 权威）

**核心理由**：
1. 已定架构是「ActionQueue 协程串行 + DamagePipeline 同步 + 战斗逻辑不走 EventBus」——协程动画天然不确定，B 方案等于推翻这个架构重写确定性逻辑层，成本不可比
2. 联机大厅层已验证的事件通道（请求类 OnlyHost + Immediate=true）可直接复用于行动选择上交
3. 个人 Demo 无电竞级公平性需求，带宽不敏感

**要点展开（拍板后进技术设计）**：
- 行动选择上交：新增 `SubmitActionRequestEvent`（OnlyHost + Immediate），携带 playerID + 行动数据
- Host 生成低级单位选择（低级单位决策阶段）→ 收齐全部玩家选择（选择阶段，含 AI 玩家操控其高级单位）→ 全部行动统一按攻速排序+延迟执行（执行阶段）。排序与延迟规则见 docs/04 §4.1（2026-09-16 修订：原五阶段中的玩家/AI 两个执行阶段合并、"AI 单位"更名"低级单位"，见决策五）
- 每个行动执行产出**结果命令**（ResultCommand：位移/伤害/死亡/特效触发/属性变化），Host 广播，客户端 BattleView 按命令流播放动画——**逻辑与表现彻底分离**，与祈愿系统 PlanShot→PlaySteps 同构
- 战斗事件不走 EventBusHub（铁律），走专用 BattleCommandChannel（NetworkEventBus 上的独立MessageType 或 Mirror SyncList/自定义序列化，技术设计时定）
- 低级单位（1-2星角色/造物）：低级单位决策阶段决策（Host 生成选择，先于玩家选择），统一执行阶段与其他单位一并按攻速结算

### 已拍板（2026-08-16）

- [x] **采用 Host 权威 + 结果命令流**。命令流复用祈愿 PlanShot→PlaySteps 模式：Host 逻辑执行产出有序步骤列表；步骤粒度=一次原子视觉事件（位移/命中/扣血/死亡/上Buff/特效）。**2026-09-12 修订**：粒度从回合级一整块降为**片级命令块流**（分步流式演算），客户端与 Host View 从"纯播放器"改为"播放器 + 执行期交互端"——详见决策四
- [x] **回合推进屏障同步**：Turn N 动画播完（或超时快进）才开 Turn N+1 选择阶段，慢机不卡死全场
- [x] **迷雾后置但协议预留可见性作用域字段**：命令流设计含 recipient 可见性过滤位（切片期恒全可见），防将来加迷雾时重设计通道
- [x] **断线重连切片不做**，仅协议占位
- [x] **先决先执行 tiebreaker 废除**（2026-09-08 回合流程重排，取代 2026-08-16 的"先决先执行 tiebreaker = Host 收到提交的顺序"）：攻速相同 = **同时开始执行**，不再按提交顺序排序；同时开始执行的多个行动，其内部结算序在技术设计时确定（表现上无先后）
- [x] **回合流程重排 + 攻速延迟执行模型**（2026-09-08 拍板，规则本体 docs/04 §4.1）：AI 行动阶段（AI 决策）→ 玩家行动阶段（玩家选择）→ 玩家执行阶段 → AI 执行阶段 → 回合结束。执行阶段按攻速从高到低排序，**每相差 10 点攻速延后 1 秒开始执行**（延迟秒数 = 攻速差 ÷ 10）；行动及其衍生事件提早完成时，后续行动跳过剩余等待立即执行（避免空等待）
- [x] **执行阶段合并 + 低级单位命名**（2026-09-16 拍板，取代上述五阶段结构、攻速延迟规则本身不变）：玩家执行阶段与 AI 执行阶段合并为**统一执行阶段**——全部单位（高级+低级）的行动统一按攻速排序，攻速时间窗跨单位归属一致生效；"AI 单位（1-2星）"概念更名为**低级单位**：无论属于真人玩家还是 AI 玩家都自主行动（如 MOBA 小兵），3~5 星为高级单位由所属玩家操控。详见决策五

---

## 二、单位视觉表现（决策 4）

### 选项对比

| 维度 | A. 3D 模型单位 | B. 2D 立牌棋子（全 2D 战场） | C. 3D 战场 + 2D 贴片单位 |
|------|---------------|------------------------------|--------------------------|
| 工作量 | **巨大**：每角色 rigged 模型+动画状态机+技能特效，6 角色≈6 套 | **小**：头像/立绘资源已在（Avatars/Cards 图集） | 中：地形 3D 化+光照，单位仍 2D |
| 与现有资源契合 | ❌ 项目 0 个角色 3D 模型管线 | ✅ SpriteAtlas 6 个、卡牌视图策略、立绘体系全复用 | 部分 |
| 战棋信息传达 | 好 | **足够**（格子+立牌+状态图标是战棋成熟范式，Into the Breach 级别验证过） | 好 |
| 相机/渲染 | 复杂（动画 LOD、遮挡） | 正交相机+SpriteRenderer，MainHall 已有同款管线 | URP 已有，需处理 billboard 朝向 |
| 战斗打击感上限 | 高 | 中（靠震屏/闪白/粒子补） | 中高 |
| 后续扩展 3D | 原生 | 需重做（但 Demo 定位下大概率不会） | 半原生 |

### 推荐：B（全 2D 立牌棋子）

**核心理由**：
1. 概念提案定位是**概念验证 Demo**——视觉管线选工作量最小、复用最大的一条
2. 现有美术资产全是 2D（头像图集、卡面、立绘、元素图标）；MainHall 的 SpriteRenderer 背景管线可直接复制到战场
3. 战棋核心体验在「同时博弈+攻速排序+势力构筑」，不在单位建模精度；打击感用震屏/受击闪白/粒子/音效补齐（ScreenEdgeGlow、CardGlow 等 shader 已有积累）
4. 单位棋子 = 头像（Avatars）+ 阵营色底座 + 稀有度边框 + 状态图标区，与卡牌 UI 语言统一，学习成本零

**要点展开（拍板后进技术设计）**：
- 相机：正交，固定俯视微俯角（类似 30-45° 视觉），支持缩放（MapZoomAndDrag 可参考）
- 战场：SpriteRenderer 平铺 Tile 网格 + 网格线/高亮（Shader 或九宫格 Sprite）
- 单位：立牌（Sprite + 底座），选中/悬停高亮复用 CardGlow 体系
- 朝向：单位立牌不做 3D 朝向，用左右翻转表达阵营朝向
- 表现层动画：位移（推力移动的匀速/击退抛物线）、受击闪白、死亡变灰（尸体）——全部 Tween，不引入动画状态机

### 待拍板

- [x] 全 2D 立牌方案确认
- [x] **棋子底图切片期用现有头像**（Avatars 图集，零成本）；Q 版立牌视觉升级后置
- [x] **斜俯视棋盘**：相机约 50-60° 俯角正交，棋盘平铺 3D 平面（SpriteRenderer 地块），立牌竖立面向相机，桌游纵深感；支持缩放；单位朝向用左右翻转表达阵营
  - ❌ **"正交"已推翻（2026-09-12 实机目检）**：用户反馈画面平面（正交投影的 2D 感）→ 改**透视 FOV45、俯角 55°**；斜俯视/立牌/其余结论不变

- [x] **立牌风格参考饥荒（2026-09-12 补充）**：2D 立牌 + 底座投影；格子表现尺寸**明显大于单位立牌**（2026-09-17 理由改写：原理由"需容纳 3 点体积的多个单位"随队形方案一并作废，保留"格>立牌"为格感边界与重叠可读性服务）
- [x] ~~**同格单位队形布局**：1 单位 = 居中；2 单位 = 并排；3 单位 = 2 前 1 后三角队形；占据数变化时实时重排~~
  - ❌ **队形已推翻（2026-09-17）**：队形偏移使特效与立牌错位（那维莱特持续水柱类技能尤甚）；判定向特效对齐会引入"队形巧躲弹道"的不可控噪声（站位自动、玩家不可控，非策略深度）→ 定案**同格单位重叠于格心 + 两态模型**：
    - **执行阶段（演出态）= 收拢重叠**：全部立牌与格心重合，特效锚格心=锚所有单位，错位在根源上消除
    - **选择阶段（决策态）= 自动散开**：进入选择阶段即全部立牌散开展示（1 居中/2 并排/3 三角的偏移布局复用为展开形态），底座正常显示；无需悬停/选中触发
    - 散开只存在于不发生判定的阶段，收拢态恰好=判定态，自洽无死角
- [x] ~~**格子判定区小于格子表现范围**：命中判定用内缩判定区（hitbox），同格全部单位共用；格子 = 最小判定范围~~
  - ❌ **已推翻（2026-09-17）**：受击范围与格脱钩——**受击体 = 立牌真实大小的圆柱**（视觉即判定：底座圆盘即受击范围的可视化）
- [x] **格 = 交互粒度，非判定粒度（2026-09-17 拍板）**：格是玩家可选择的**最小距离**——移动永远逐格走、占据/阻挡/技能范围声明按格；**判定发生在连续空间**：命中 = 投射物轨迹/特效形状 ∩ 立牌圆柱；**移动中单位处于连续插值位置、可被中途命中**（例：单位正走向邻格、箭矢射中其半路位置）= **所见即所得**；移动速度/投射物速度为**逻辑常量**挂执行阶段时间轴（表现动画只是其可视化，Host 确定性不破）
- [x] **命中分层语义（2026-09-17）**：**接触判定** = 投射物轨迹/特效形状 ∩ 圆柱（单位级，决定"谁被打到"）；**效果作用域** = 技能声明（单目标/命中格 AoE/全体，决定"打到后溅射给谁"）——两层解耦；格 AoE 技能 = 接触任一同格圆柱后按格声明生效（堆叠同心下与旧"格内全中"结果一致，向后兼容）
- [x] **投射物真实飞行（参考原神）**：箭矢按弹道飞行而非逐格瞬移；**命中 = 接触目标立牌圆柱之时**（2026-09-17 由"接触目标格判定区之时"修订）；效果作用域按技能声明（安柏战技 = 命中格 AoE）
- [x] **投放形态由技能数据驱动**：安柏战技 = 直线飞行、安柏爆发 = 沿整条直线从天斜落——技能范围声明仍按格子（Host 即时结算），仅表现形态不同
- [x] **单位指向型技能不经格子判定**：延奏/契约等精确指定单位的技能只对目标单位生效，不适用所在格判定（规则侧同步 docs/05 §5.3）

---

## 三、垂直切片范围（决策 5）

### 推荐范围

| 模块 | 切片范围 | 不进切片（后置） |
|------|----------|------------------|
| 对局人数 | **1v1**（双开控制调试模式 + LAN 联机；架构按 6 人预留 playerID） | 3-6 人、分组淘汰、Hotseat 隐私幕帘（可选后置） |
| 角色 | **6 个已有文档角色**：安柏/凯亚/丽莎/琴/芭芭拉/诺艾尔（全蒙德，正好验证延奏） | 其余国家角色 |
| AI 单位 | **2 个 1-2 星**（如丘丘人×2 变体）验证低级单位决策阶段+尸体系统 | 完整 AI 行为树 |
| 势力机制 | **蒙德延奏 only** | 璃月契约、挪德双祝福等 |
| 战场 | **20×20**，地形 3 种（平原/水/障碍）验证通行性标签 | 50×50、全 7 地形、动态战场（虚空切换）、宝箱 |
| 迷雾 | **后置**到核心循环跑通之后（visionRange 字段已在，渲染层最后补） | — |
| 命座 | 最小验证：**1 个角色 2 个命座效果**（建议安柏，文档最全） | 全势力命座 |
| 建筑 | **1 个产摩拉建筑**（验证出战建筑+摩拉产出闭环；科技树面板不做，购买入口用简化按钮） | 科技树、多建筑 |
| 物品 | 食物×2（使用）+ 武器×1（装备槽）验证 IUsable 流程 | 圣遗物等其余子类型 |
| 经济 | 局内摩拉（初始200+每回合5）+ 体力（初始60+每回合5，与摩拉统一发放时机）+ 核心掠夺手牌 | 原石结算、局外循环 |
| 胜负 | 摧毁协议核心 → 掠夺 → 结算画面 | 消散演出、观战 |
| 控制 | 硬控晕眩×1（丽莎？）验证韧性公式；**冻结反应作为第二控制验证** | 心控/压制、全部软控 |
| UI | 战斗 HUD：手牌区/体力/摩拉/回合数/行动选择面板/攻速队列显示 | 战报、回放 |

### 拍板后的落地顺序（与既有 B 批次合并）

```
B1 ActionQueue + DamagePipeline（Host 权威，见决策一）
B2 Buff 最小可用（含尸体状态、燃烧 DoT 一例）
B3 管理器职责边界（含 Framework/Battle 4 文件与 WishManager 层级归位——上一批遗留）
B4 示例技能：安柏战技/爆发 + 凯亚；元素反应做融化+冻结两例（伤害乘区 + 控制链路）
B5 战场与单位表现层（决策二）：斜俯视 Tile 网格 + 立牌 + 位移动画
B6 回合流程串联：低级单位决策阶段 → 选择阶段（含时限 docs/04 §4.2）→ 执行阶段（全部单位统一攻速排序，2026-09-16 合并拍板，docs/04 §4.1）→ 回合结束
B7 联机闭环：双开控制调试模式（单人公开操控双方，不经网络层）→ LAN（行动上交 + TurnScript 广播）
B8 切片收口：建筑/物品/命座最小集 + 迷雾 + 结算
```

### 已拍板（2026-08-16）

- [x] 1v1 + 6 角色全蒙德（安柏/凯亚/丽莎/琴/芭芭拉/诺艾尔，正好验证延奏）
- [x] 迷雾后置到 B8 尾部（TurnScript 协议预留可见性作用域字段，见决策一）
- [x] **B7 顺序改为「双开控制调试模式 → LAN」**，Hotseat（隐私幕帘 UI）降为可选后置
- [x] 建筑砍到 1 个产摩拉建筑、科技面板不做（购买入口用简化按钮）
- [x] **元素反应做融化 + 冻结两例**：融化验证伤害乘区，冻结验证控制链路（联动韧性/尸体系统）

---

## 四、执行阶段模型与片内结算（2026-09-12 拍板）

> 起因：稻妻连携"2 秒内随时点击"、璃月"谈判时停 3 秒投票"证明执行阶段是**即时演算+实时交互**的——玩家在执行阶段实时在线，旧"整回合预计算+播放"模型作废。参考王者荣耀/原神的服务器权威结构，tick 粒度换成战棋的"攻速片"。

- [x] **执行阶段 = 分步流式演算**：Host 结算一片 → 立即广播该片命令块 → 等全体播放确认或超时快进 → 结算下一片；命令流从回合级一整块改为**片级命令块流**
- [x] **Host 永不跑在客户端前面**（片级 ack 门控）：即时输入永远作用于当前演算点之后，无回滚问题
- [x] **即时行动协议**：连携等执行期交互随时上交（SubmitInstantAction），Host 在片边界 poll 输入队列、立即插入结算；蒙德切片只预留协议位不实现
- [x] **两类交互点**：软窗（连携 2 秒——演算照走不暂停，阶段切换前扫 WaitConditions 等窗结束）、硬停（契约谈判——Pause→收投票→Resume）；"提前完成不空等"的压缩仅限纯演算片间等待，**交互窗时间不可压缩**
- [x] **同攻速片内 = 快照结算（真同时）**：片内全部行动读取片前快照、独立结算、效应统一应用——同片互杀 = 同归于尽；"死亡中断"仅跨片发生（"开始执行前被击杀才落空"）；结算顺序退化为命令枚举序（unitID 升序）不影响结果——取代 2026-08-16"内部结算序待定"
- [x] **移动 = 同片内同步逐步结算**（与快照结算并存：瞬发效应读片初状态，移动动态展开）：每步只判定将要进入的下一格、当前所在格不判定（同格共存单位不阻挡彼此离开）；完全进入下一格前位置判定仍在上一步的格；对穿互换 = 互相穿过（规则本体 docs/05 §5.3）；瞬发×移动交互暂定按片初位置结算（docs/11 跟踪）→ **该悬案已销（2026-09-17）**：投射物类命中 = 接触立牌圆柱之时、读**命中时刻的连续插值位置**（含移动中途，见决策二"格=交互粒度，非判定粒度"条）；零表现时间的纯瞬发效应仍按片初快照结算（同片并发基石不变）
- [x] **逻辑层载体**：逻辑 Unit 维持现有 MonoBehaviour 组件容器（Host 场景实例化，零重构）；客户端与 Host 显示侧为同构 UnitView 表现层
- [x] **选择期信息同步**：每回合选择阶段开始 Host 广播全量 BattleSnapshot；执行期客户端状态由命令流增量维护（连携按钮等 UI 状态也走命令）
- [x] **序列化 = JSON**（JsonUtility，union 风格命令体；序列化器接口隔离，将来可换二进制）

---

## 五、回合结构与规则修订（2026-09-16 拍板）

> 起因：2026-09-16 战斗系统设计文档全量交叉分析（04/05/06/07/11/18/22 + units）发现三项跨文档矛盾与规则缺位，当日拍板修订。

- [x] **执行阶段合并**：玩家执行阶段与 AI 执行阶段合并为**统一执行阶段**——全部单位（玩家操控的高级单位 + 自主行动的低级单位）的行动统一按攻速排序。修复"攻速时间窗在玩家/AI 边界失效"（原五阶段分离下 1-2 星单位结构性永远后手，与"每 10 攻速=1 秒先手机会"核心卖点冲突）；规则本体 docs/04 §4.1
- [x] **"AI 单位"更名"低级单位"（1~2星角色与造物）**：无论属于真人玩家还是 AI 玩家都自主行动（表现类似 MOBA 里的小兵）；3~5 星为**高级单位**，由所属玩家（真人或 AI 玩家）操控。同时明确 **AI 玩家**（AI 补位玩家）操控其高级单位走玩家选择通道，与低级单位自主决策是两套逻辑
- [x] **尸体被动改为例外制**：**绝大部分被动失效，倒下仍生效的例外在技能中额外说明**（例：凯亚的寒冰之棱——持有者倒下仍然生效，docs/units/蒙德/凯亚.md；纳塔夜魂元能流逝为势力级例外，声明在 docs/07-势力机制/纳塔.md）。原 docs/05 §5.4"被动技能失效"通则与纳塔保留的矛盾就此消除
- [x] **同格争格合并候选一删除**（"枚举序前 N 个入格其余停在格前"）：直接违反 docs/active/22 §1 枚举序铁律（枚举序仅决定命令排列与写-写冲突合并依据，**不影响结算结果**）；保留候选"全部争格失败停在格前"为唯一候选，B4 移动系统落地时拍板（docs/11 移动与体积节）
- [x] **时序模型与 Buff 计时**：整个回合 = 执行阶段**即时演算** + 选择阶段**软暂停**（表现层照播、不产生任何影响战局的结算）；回合制 Buff 只在每次回合结束时计数减一（上 buff 的**当回合**结束即减），现实秒计时只走执行阶段时轴；**执行中段被控时，已上交未执行的行动作废**（与死亡中断同构：开始执行前被控才落空）。规则本体 docs/04 §4.4 + docs/05 §5.5
- [x] **行动配额显式化**：选择阶段可用的全部动作——移动/战技/爆发/延奏/契约/交互、打出手牌（部署角色/建筑、装备物品）——**均各占每回合 1 个行动**（原 05 §5.0-5.2 已隐含，本次显式化；docs/05 §5.0 + docs/07-势力机制/00-总述）
- [x] **"玩家行动阶段"更名为"选择阶段"（2026-09-17）**：阶段名只描述本阶段实际内容——玩家在此只做选择、不做执行，消除"行动阶段里行动却还没执行"的语义歧义；"行动"一词专指**动作单位**（每回合 1 个行动）。历史拍板条目中的旧阶段名按当时原名保留；代码 `BattlePhase`（Selecting/Resolving）本与新名一致，零改动。规则本体 docs/04 §4.1 命名说明
- [x] **乘区运算 = 同乘区内加法并入**：反应/技能提供的增伤、易伤数值**加法并入**所属乘区项后再统一计算，不做独立乘法（一个乘区内的叠加量）；示例：防御 100（基础易伤 0.5）+ 1 级融化（+50%）→ 易伤 = 1.0（docs/05 §5.6 乘区运算规则）
- [x] **体力系统维持现状**：初始 60 + 每回合 5、移动/战技/爆发各耗 10 的结构不变（曾指出"前 12 回合无约束力 + 残局战斗频率减半与终局节奏反向"，知情评估后维持）
- [x] **格子距离 = 切比雪夫度量**：两格距离 = max(|横坐标差|, |纵坐标差|)（对角与正交同距，八方向等价）；"半径 N" = 切比雪夫距离 ≤ N 的全部格子（**正方形区域**：半径 1 = 9 格、半径 3 = 49 格）。对比候选：曼哈顿（与十字技能方向体系同构、菱形范围）、欧氏（浮点运算 + 需格心定义）——知情选择切比雪夫；规则本体 docs/03 §3.3，docs/05 §5.3 / docs/06 §6.4 补引用。附带影响：扩散/导电/寒冰之棱等存量半径数值需按正方形区域复核（B4 技能数值阶段）；结晶"最近单位"等距平局规则待拍板（docs/11 技能与判定）

---

## 六、战斗 HUD 布局与技能使用交互（2026-09-18 拍板）

> 布局范式 = MOBA（王者荣耀）× 视觉语言 = 项目既有原神极简（米白暖金/细线元素/玻璃深底）。设计稿 [docs/designs/battle-hud-v1.html](../designs/battle-hud-v1.html)（16:9，2560×1440 基准，三视图 + 交互状态机 + 规格表）；技术基线 = docs/active/22 §13；实现归 **B6**（回合流程串联 + 正式战斗 HUD）。

- [x] **布局四点定案**：
  - **移动技能 = 屏幕左下（王者荣耀摇杆位）**，Ø220
  - **爆发技能 = 屏幕右下技能盘盘心，放大 1.2 倍**（Ø264，其他技能 Ø220；屏幕距右 300/距底 300）
  - **战技、延奏等其余技能围绕爆发**（弧位排布，与爆发圆心距 300；契约等后续技能同圈层加位）
  - **拖动/瞄准时右上角出现取消按钮**：拖动式 = 移至取消位**松手**取消；点击式 = **点击**取消按钮取消
- [x] **技能两种使用方式（等价可达，共用瞄准框架）**：
  - **拖动式（王者荣耀式）**：按住技能按钮拖出 → 棋盘高亮**可选格子**（按技能范围声明，**吸附最小单位 = 1 格**）→ 指针吸附最近可选格 → 松手：可选格内 = 释放；取消按钮上/无效格 = 取消
  - **点击式**：左键单击技能按钮 → 显示**技能详情面板**；随后三情况：① 再点技能按钮本身 = 隐藏面板、进入瞄准态（显示可选格，点格释放）② 点在按钮/面板之外 = 仅收起面板回待机 ③ 直接拖动 = 无缝切换为拖动式
  - 两方式共用：可选格高亮 + 吸附 + 右上取消；手牌（部署/装备）的目标选择**建议复用同一框架**（B6 落地时定）
- [x] **HUD 顶栏内容定案**：回合数 + 阶段徽章 + 选择倒计时；战斗时钟（独立时钟 6:00 起 +20/回合，docs/04 §4.1）；双方徽标 + 协议核心血条（胜负条件 docs/02）+ 体力/摩拉；**攻速队列条**（选择阶段显示执行预览，执行阶段样式待 B6 定）
- [x] **素材路线 = 复用项目内已提取资产**（Avatars 头像立牌 / Cards 卡面手牌 / Skills 技能图标 / Element/Stroke 元素细线环 / Faction 徽标）

### 待拍板（B6 落地前收敛）

- [x] **单位选择交互定案（2026-09-18 拍板）**：**点击棋盘立牌选中单位**（战棋传统）→ 技能盘出现并显示**该单位**的技能，同时**隐藏手牌区**（选中态与手牌态互斥）；**点击棋盘其它空白处 = 取消选中**，技能盘收起、手牌恢复显示。即：未选中任何单位时无技能按钮可见
- [x] **移动按钮位置定案（2026-09-18 目检收敛）**：圆心距底 500（屏幕高 65% 处）——两轮上移目检后拍板
- [x] **立牌美术升级路线（2026-09-18 登记）**：当前用人物头像作立牌为**临时方案**——后续需要 **12 帧动画 + 完整人物立牌**（决策二"Q 版立牌视觉升级后置"的具体化方向：完整人物 + 12 帧动画，非 Q 版头像）
- [ ] **延奏/契约按钮图标**：延奏是通用机制按钮（每角色通用），当前用蒙德风车占位；候选 = 风车 / 音符 / 连携环
- [x] **技能按钮统一定案（2026-09-18 拍板"技能按钮应当统一，包括移动（移动是特殊的技能）"；当晚追加拍板"移动也不应当直接瞄准"）**：移动按钮并入技能盘同款 **Skill.prefab 建法**（元素色底图+主动环+选中环+外挂名称，SkillIconView.InitWithData 现有链）；数据链走 **skills[Move] 条目**（UnitConfig 已配：skillID=Common_Walk+icon=walk.png）；**图标+名称随单位常态数据驱动切换**（walk/fly/amphibious → 步行/飞行/两栖，SkillName 表通用三键）；**交互与其余三键完全统一=点击式三情况**（①点开技能详情面板②再点同键进瞄准③换点切内容，docs/14 §64b），移动唯一差异=AimMode（瞄准按移动语义结算）；特殊性仅剩=专位左下+无 Move 条目单位不隐藏（移动人人可用）。原"移动按钮图标随单位切换与否"待拍板项随之落地（fly 单位=飞行图标+「飞行」名）
- [ ] **执行阶段 HUD 变化**：技能盘收起/变灰样式、攻速队列条转执行进度样式、取消按钮尺寸微调（Ø196 距顶 212 当前避开了设置钮）——B6 表现侧一并目检
- [ ] **头顶血条常驻**（当前设计为常驻绿/红条 + 单位名）与伤害数字偏移细节（B5 表现层联动）
- [x] **素材复用优先（2026-09-18 拍板）**："很多东西本项目里已经准备好了，直接复用即可"——HUD 所需图标/头像/卡面/元素环一律先扫项目 Resources，不做外部提取或 AI 生成
- [x] **技能盘按钮 fallback 语义定案（2026-09-18 拍板）**：`GetSelectedSkillData` 无对应 SkillType 时回退首个技能**是预期行为，勿当 bug 修**——3004 号角色设计即无战技、主要靠移动（战技按钮位显示其首个技能属正常表现）；延奏置灰（`Toggle.interactable`）仅对 skills 为空的角色生效
- [x] **瞄准可选格推荐分色（2026-09-23 拍板原话「可选且推荐为半透明白色，可选但不推荐为半透明红色，不可选择无任何提示」）**：进瞄准态的高亮按推荐分色。推荐口径：**直线技能（战技/爆发）=该方向能命中敌人（含尸体，Host 判定同语义）**——预判走技能实例 `WouldHitEnemyInDirection` 静态快照预判、与 Host 判定形态同源（安柏战技=投射物圆柱接触+虚空截断、箭雨=整线扫描、凯亚霜袭=DamageDistance 距离段，覆写在各技能类）；**移动=该格实际能走到**——步进预判镜像 MovementResolver 判定链（地形 IsPassable 按 ForceType：凯亚步行入水不推荐、水面之后同不推荐；体积+阻挡规则含尸体），被挡后该向余下格全不推荐（移动停在格前）；**单位指向（延奏/契约）与部署 v1 全推荐**（目标格即语义本身，无优劣数据可分）。色值收口=BattlePalette.瞄准推荐色/瞄准不推荐色；半透明落地=BattleViewFactory.CreateTransparentUnlitMaterial（URP Unlit 透明配方）；推荐是提示非校验，Host 结算仍权威。**2026-09-24 复核修订（用户反馈"白色的石砖地形上观感不适"→推荐色白改饱和暖金）；同日视觉终版定稿（迭代链：白→金→回白+每格向内黑边→**青蓝**，末拍板原话"换个颜色，但不是白色和金色，按照原神风格"）：色块=青芯+内嵌黑边 quad——底图 BattleViewFactory.AimCellTexture 运行时生成（128px 白芯+12px 黑边环，黑边 rgb=0 乘任意 tint 恒黑故两色共用一张）；推荐=原神 Hydro 系青蓝 #4CC2F1 (0.30, 0.76, 0.95, 0.8)（原神对话选项/选中态高亮同色系+尘歌壶摆设"可放=蓝框/不可放=红框"既有语义），不推荐=红 (1, 0.3, 0.25, 0.8)；两值已烘入资产文件（白/金为中间试色已废）**
- [x] **伤害数字=原神式屏幕空间渲染（2026-09-24 拍板原话「按照原神的做法」「反应名前缀为GIC特色，保留，不需要有减号。不需要同步加聚合」）**：渲染=独立 Screen Space - Overlay 画布（sortingOrder 39，HUD=40 之下；数字不参与 3D 深度测试——任何角度正面、永不遮挡），仅取受击点世界坐标投影为屏幕锚点；曲线=首帧爆大尺寸（初始停留比 2.5——同日目检后追加拍板「初值与停留尺寸太接近了，把初值调大为2.5倍」）；同日追加拍板「出现位置更分散」：出生随机偏移散布=±0.42 X/0.3~0.65 Y/±0.15 Z）→0.35s easeOut 收缩到停留尺寸，伤害→尺寸映射 f=Clamp(0.8+0.22·log10(max(1,|dmg|)), 0.8, 1.65) 初值与终值同乘（对数防大数字占屏）+段尾渐隐；文本=伤害不带负号（"40"）、治疗"+40"、反应名前缀"蒸发 40"（GIC 特色保留）、不做同类聚合；实现=BattleDamageNumbers 层组件（运行时懒建+池化，无 GraphicRaycaster——Overlay 画布加射线器会挡 HUD 按钮，勿补）
- [x] **头顶血条/元能条=原神式屏幕空间渲染（2026-09-24 拍板原话「把血条也这样改造，并把血条下面加一条元能条。风格遵循原神分隔」）**：与伤害数字同路线——独立 Screen Space - Overlay 画布（sortingOrder 38，伤害数字 39 之下、HUD 40 之下）逐帧投影 UnitView.OverheadBarAnchor（含 55° 后仰偏移）→ 不受遮挡/任何角度正面；结构自上往下=血条（敌我染色+**原神分隔线**默认 4 段）→**元能条**（新字段 Palette.元能蓝，分隔量子=10 与 B6a 获取粒度一致、段数上限 10）→附着图标随血条左缘（旧世界 SpriteRenderer 附着图标退役同移屏幕空间）；UnitView 数据化（公开 Hp/MaxHp/AllyHpBar/AttachedElement/OverheadBarAnchor getter，视觉移层），名字/Buff 徽章仍挂立牌倾斜组（「都应当斜」拍板未推翻这两条）；元能条尸体隐藏；实现=BattleOverheadBars 层组件（运行时懒建，无 GraphicRaycaster）。「能量环视觉」由本条承接（B6d 对应缺口销案）；**2026-09-25 目检追加拍板**：①条=圆角药丸形+黑边框（非矩形；同日拍板「不要程序化生成」→ 底图资产化=`Resources/UI/Battle/BarBg.png`（黑边框 5px 环+白芯乘底色）/`BarFill.png`（纯白圆角），运行时零生成只 Resources.Load，美术替换直接覆盖文件；**填充内缩条高×5/24 露底图边框——同尺寸会整盖住边框环致黑边不可见**；UGUI Filled 必须赋 sprite 才裁切——空 sprite 时 fillAmount 被忽略条恒满，陷阱=docs/14 §75）；②**血条填充=玩家所选队伍色**（与底座同色，替换首版敌我绿/红——Palette 血条我方绿/敌方红不再被头顶条消费）；③**元能条=白色**（元能蓝→元能条色改名+默认白色）

---

## 七、B6 回合批次数值与机制拍板（2026-09-22 拍板）

> 起因：B6a 元能系统开工前拍板会（docs/11 三处规则缺位：元能数值、部署落点、手牌机制）+ 开发中用户修正消耗口径。

- [x] **元能获取口径（用户原话要点）**：**移动使用即可获得 10**（被挡也算——移动行动已使用）；**战技如果能命中（比如射箭命中敌人，治疗命中友军），该战技需要有至少 1 次命中才能获得 10 元能，即便多次命中也不再获得**（实现=命中产出按行动者同片合并去重）；爆发/延奏命中不获能（按行动技能类型分档）
- [x] **元能消耗口径（用户纠偏"爆发消耗等在单位配置里全都写的有"）**：**消耗=技能条目 EnergyCost 参数**（SkillParamKey 9；安柏爆发 30、凯亚 40、芭芭拉 100、安柏延奏 20）——**门槛=技能消耗值而非上限**（延奏 20<上限 30=不必攒满）；**上限=UnitConfig baseEnergy**（缺省 100；三角色爆发消耗恰与上限相等=攒满才放、放完清零）；命座/天赋改消耗只动技能参数自动生效。**结算序（2026-09-25 拍板原话「应当先扣除，再加」）**：同段多来源元能效应**先消耗后获取**——获取先应用会被 baseEnergy 上限钳位吞掉（30+10→40 钳 30，再 −20=10 ≠ 期望 30−20+10=20；自协奏首版实证）；实现=TurnResolver.ApplyEffects 元能两段应用 + MergeEnergyEffects 命令发射同序（客户端增量与 Host 状态一致）
- [x] **部署落点规则**：**出战角色只可在协议核心半径 2 内、或其它建筑半径 1 内**；**部署建筑则分别为协议核心半径 3 内、建筑半径 2 内**（切比雪夫度量，docs/03 §3.3）；出战费=UnitConfig deployCost 按星级（1→10/2→20/3→50/4→100/5→300）；卡不消耗留手牌（docs/05 §5.1 既有规则）。**落点碰撞规则（2026-09-25 拍板原话「需要根据碰撞决定……体积作为最高级判定」）**：部署进入落点须过与移动同构的碰撞判定链（docs/05 §5.3，含尸体）——**体积绝对层为最高级判定**（格内现有体积+部署单位体积 ≤ 3，无视阻挡配置不可绕过）+ **阻挡规则层**（与格内单位互不阻挡才可部署：互不阻挡时格内有我方单位也可部署；有阻挡则该格不可部署）+ 地形层（按部署单位常态移动类型通行，步行不可入水）；实现=DeployUnitExecutor.IsDeployCellValid（Host 权威）+ BattleHud.CanDeployEnterPreview（客户端推荐分色镜像，红格仍可点击 Host 兜底）
- [x] **战斗内手牌机制**：**初始手牌=玩家当前卡组**（相当于从收藏背包带入战斗场景，淘汰 B1 固定测试牌组；2026-09-22 细化：**完整卡组投影——角色卡+物品卡都进手牌**，物品卡使用/装备链后续批次、进手牌先做展示+点击提示；**物品卡带入数量=备战数**（min(存档持有, ItemConfig maxPrepareCount 备战上限)——如背包 100 体力、备战上限 60 → 带 60 进战斗；货币类 maxPrepareCount=0 不可入组））；**开局全手牌、无抽牌**（战棋式全可见，抽牌机制留后续批次）；手牌横向展示在屏幕底部、**允许左右滑动**；**选中角色时隐藏手牌**（与技能盘互斥=docs/18 决策六既有拍板的沿用）
- [x] **切片实现范围收窄**：当前只做**安柏、凯亚、芭芭拉**三个角色的技能链（含后续 B6c/B6d 同口径）
- [x] **AI 双脑拆分默认案**（用户未逐条表态项按推荐走）：低级单位 v1 极简三档（战技射程内有敌→打最近/否则朝最近敌移动 1 步/不可动缺席）；AI 玩家 v1 优先级=战技可命中→元能够放爆发→朝最近敌全距离移动→Pass

### 落地状态

- B6a 元能系统：已落地目检通过（ff5d60d）
- B6b 四阶段（低级单位决策+AI 玩家脑）：已落地目检通过（fb80dfc）
- B6c 部署+手牌 / B6d 执行阶段 HUD 变化：待开工

## 八、时轮系统（多轨道技能时间轴，2026-09-23 拍板）

> 起因：用户要求"米哈游级技能实现 + 专业的技能制作工具（调研成熟成品）"，示例拆解=安柏战技：十字方向选择其一 / 前摇 0.3s+射箭动作 / 射出箭矢+声音 / 两发箭矢 / 火元素 40% / 后摇 0.5s+收尾动作——拼积木式多轨道。网检（webrefs/skill-timeline-system/，2026-09-23）：业界无"即用且匹配 Host 权威回合制"的成品（主流全实时 ACT/帧同步向），学习对象=[m969/EGamePlay](https://github.com/m969/EGamePlay)（2379★，Ability/Execution/Status 概念三分）+XCSkillEditor（事件队列模型）+NBC.ActionEditor（轨道 UI 参照，无 license 仅参照）。用户拍板（原话"开始。我们可以从中学习 m969/EGamePlay，研发我们一套我们自己的，取个名"）=**立项自研，系统命名「时轮」（SkillTimeline）**，技术设计=docs/active/28-时轮系统技术设计.md。

- [x] **立项 B-S 批次（时轮系统）**：技能时间轴数据=新增 SkillTimelineAsset（SO，挂 SkillConfig.SkillData.timeline，null=无时轮兜底旧行为）；单真源双消费——Host 判定事件时刻表 + 客户端表现视图（B-S3 接素材）
- [x] **多段逐发独立判定**（推翻 B4 简化①）：投射物技能每发独立结算/附着/反应（安柏战技两发各产一条 Damage 命令分时弹出）；**元能获取口径不变**（同片按行动者去重，决策七维持）；同片快照语义不变——同片齐发各段读同一片初附着，增伤反应每发各享（总伤与合并口径一致）、同元素附着幂等覆盖
- [x] **前摇=发射时刻偏移**（判定与表现同源）：Host 时间轴上投射物自 launch 起存在（发射前不参与判定）；后摇=纯表现节奏；打断语义沿用现状（跨片死亡中断天然覆盖、同片无打断、不新增规则）
- [x] **投射物规格 per-skill 化**：速度/判定直径/射程入时轮 clip（0=BattleMetrics 默认兜底）；BattleMetrics 降为默认常量收口
- [x] **数值分工铁律**（防双源）：时间与规格归时轮（发射时刻/间隔/速度/体积/射程）；数值归 SkillParamKey（每发伤害%/发数/消耗——本地化渲染同源）
- [x] **命令协议扩展**：Damage/消散 Effect 带 launchMs（发射时刻毫秒，客户端延迟起飞勿推算）；新增 SkillCast 命令（时轮演出起点，段内最前发射）
- [x] **B-S1b 追加拍板（2026-09-23 同日，用户三指令）**：①**箭矢间隔 0.15s**（战技两发 0.30/0.45、箭雨 4 段同隔）；②**移动是特殊的技能，也用时轮完成**——移动行动同产 SkillCast（移动技能条目枚举值）、移动步数上限数据驱动=移动技能 MoveDistance 参数（HUD 瞄准+Host 权威收口同源；全移动技能均配置 10 格，无参数才回落 3——凯亚 skills[0]=Common_Walk 带 10；**丘丘人实为 skills=[]（无技能条目）走 MaxMoveDistance 回落 3，与「10%×移速=3 格」净结果相同**——2026-09-25 审查回读勘误）、**移动瞄准方向=十字四向**（2026-09-23 用户纠偏修正：首版误做成米字 8 向，全员移动技能描述=「选择十字方向其一」）、移动逻辑不动（MovementResolver 既有时间轴）；③**完成安柏全部技能**（排除命座与变奏）——延奏百发百中实装（单位指向=全图我方含自身、**HUD 瞄准修正：延奏=我方格/契约=敌方格**（此前延奏误按敌方）、AttackUpBuff 叠层+时长累加经 UnitStats 修改器生效、协奏元能蒙德/自身+10 触发变奏框架位/非蒙德+20、数值单源=技能参数经 ApplyBuffEffect 参数通道）；飞行冠军沿途采集依赖资源点系统（未落地，docs/11 登记；AI 脑斜向移动/距离基准语义两项缺口同日登记 docs/11）
- [x] **移动瞄准=十字四向（2026-09-23 用户纠偏拍板原话「安柏的移动怎么会是米字方向？仔细看看技能描述」）**：全员移动技能描述=「选择十字方向其一」——HUD 瞄准米字 8 向为首版实现错误，已修正为十字四向；FlyingChampion 时轮 aimMode 同步改 CrossDirection。衍生缺口登记 docs/11（AI 脑仍八向上交/Host 方向校验/方向模式数据驱动化）
- [x] **移动距离=10%移速换算（2026-09-23 用户拍板原话「配置文件里的10，还要看基于类型。拼接后为10%移速」）**：移动技能 MoveDistance 参数按 baseType 解释——BasedOnMoveSpeed=百分比×移速（安柏 50→5 格、凯亚等 30→3 格）、Fixed=直读格数、缺参数默认 10%（B-S1b 首版误直读 10 格已修正）；落地=SkillData.ResolveMoveDistance 单出口+UnitState.moveSpeed 快照字段（HUD 与 Host 同源、移速含 Buff——减速缩短移动距离）
- [ ] 编辑器（B-S2，UI Toolkit 多轨道窗）与 SFX/箭矢素材路线（B-S3）——素材路线待拍板（docs/11 登记）

---

## 附：技术设计落地

技术设计已按 2026-08-16 与 2026-09-12 两轮拍板落地 **docs/active/22-战斗系统技术设计.md**（编号 21 已被 docs/21-商业化与发布边界 占用）。B1-B8 各批次开工前在该文档按需补充细化，不再单建；实现细节（ActionData/片级命令块/Snapshot 结构/TurnResolver 骨架/移动判定链）以 docs/active/22 为准。
