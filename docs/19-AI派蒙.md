# AI派蒙（桌宠）

> 2026-08-21 立项。设计草稿 + 分阶段实现——本文记录需求、方案与实现进度；§7 待拍板项已于 2026-08-21 回合收敛；**透明置顶窗口 spike 已于 2026-08-21 验证通过并合入主干（见 §5.8）**。操作工作流（修复→构建→验证）见 `gic-pet` skill；重定向管线操作见 `gic-paimon-retarget` skill；重定向技术细节见 `.codely-cli/HANDOFF-派蒙动画重定向.md`；踩坑汇总见 docs/14。

## 1. 定位

AI派蒙 = **常驻桌宠 + 对话式游戏助手**：

- **表现层**：派蒙 3D 模型作为桌面宠物常驻屏幕——只要游戏进程开启就存在，且**不止在游戏窗口内**：游戏失焦/最小化时仍以置顶悬浮形式出现在桌面（桌宠模式）。**桌面置顶悬浮一期直接做**（2026-08-21 拍板，不走"先仅游戏内"妥协路线）
- **生命周期（2026-08-21 二次拍板，取代早前"同生共死"）**：**拉起但不绑命**——启动游戏自动唤出派蒙（桌面已有则跳过）；游戏退出/崩溃后派蒙**独立存活**，继续留在桌面；重开游戏不重复拉起（Mutex 单例）；派蒙的关闭方式=双击她（后续可换右键菜单）。她专为本项目打造，但不必陪葬
- **交互层**：鼠标拖拽（抓斗篷拎起）、待机动作、视线跟随鼠标、屏幕边缘探头（见 §3.5）
- **功能层（预留）**：对话指令入口——"设置游戏内时间 / 打开背包 / 帮我抽卡"等自然语言操作游戏。**接 LLM 实现已拍板，一期不做**（2026-08-21）

非目标：战斗/联机中的表现策略**暂不考虑**（2026-08-21 拍板，届时再说）；**不**细致到与每个小窗口交互（只认屏幕边缘，不做 DesktopMate 式"坐在某窗口标题栏上"）；不承诺移动端桌面形态（见 §6）。

## 2. 模型来源

**2026-08-21 晚定案：模之屋 MMD 模型为唯一模型基底；原神提取路线放弃**——GI 网格反优化始终无解（FBX 只出骨骼），且其 Bip001 动作与 MMD 骨骼不通，两头的对接成本都不划算。AnimeStudio 工具链与已导出资产保留在工具目录备查（不再投入）。
**2026-08-22 更新：动画不通问题已由方案 B 重定向管线根治（§2.5）**——GI 提取的 254 个看板动作经编辑器内管线转换为 MMD 骨骼动画直接播放，GI 动作**恢复为首选动画来源**（MMD 模型零改动）。

### 2.1 模之屋模型（已入库，定案采用）

- 下载：aplaybox（链接见立项记录），包 `派蒙_by_原神_7a9eb2d0448dee7d6fac7d36542919cb.zip`，已解压至 `D:\Tool\PaimonModel\`
- 格式：**PMX 2.0**（模型名"派蒙"，10379 顶点，UTF16LE），贴图 5 张（头发/披风×2/脸 + toon/mc3/表情）
- 授权（使用规则.txt，神帝宇改模，版权 miHoYo）：**允许改造/优化骨骼刚体重制 UV；禁止二次配布、商用、18禁/极端内容**——个人 Demo 自用合规；模型文件不可再分发（游戏也不可公开分发）
- 转换：Unity 不认 PMX，需 Blender + mmd_tools（v4.5.13 已备）转 FBX

### 2.2 原神提取路线判死（2026-08-21 深夜 CLI 全面复查）

**2026-08-24 复查补充（网络调研+本地复测）**：①判死结论维持——CLI 复测 Animator 导出仍为纯骨骼 FBX（0 Deformer；Body/Face/EyeStar 仅空节点无几何）；②**现用 MMD 模型即官方发行版**：使用规则.txt 确认"模型编辑：神帝宇/版权 miHoYo"，包名"派蒙_by_原神"= aplaybox 官方账号（u/680828836）分发——社区所谓"官方模型下载"（DeviantArt/Sketchfab 整理帖）均指此源，Sketchfab 6k+ 下载的派蒙也是 PMX 转的，非游戏提取；③**顶点对比**：官方 Kanban 网格合计 8049 顶点（Body 6053/Face 681/Brow 52/EffectHair 1162/EffectMesh 51/EyeStar 50），低于 MMD 版 10379——GI 手机端优化更狠，换官方网格无精度收益（贴图本就同 UV，§5.9 已直接用官方贴图）；④工具链现状：Razmoth HoYoStudio 已被米哈游 DMCA（末版 v0.17.00），活跃后继=Escartem/AnimeStudio（本地 commit 1ccfbc1 即其最新线），其 issue 区无"GI 网格导出普遍损坏"报告——断链为 Kanban 预制体/自定义渲染类特定问题；各 fork README 均注明 GI MeshRenderer 已知问题（"Disable Renderer"选项只是绕开报错，绕开=更无网格）；⑤理论兜底路径（不投入）：改 AnimeStudio 源码 dump Mesh 的 m_BindPose/m_BoneNameHashes/blend 通道 + 骨骼名哈希配对 + Blender 重建蒙皮——实测 Raw dump 为无 type tree 打包二进制（字段名全无），需逆向 GI 网格布局，工程量大且收益为负（网格更简）。

**2026-08-24 深夜·捷径命中，纯原神资产全套到手（用户手动下载 models-resource 完整 rip）**：包在 `D:\Picture\gichess\unity\模型\PC _ Computer - Genshin Impact - Main Protagonists - Paimon\`（asset 328738）。**`Paimon.fbx`（1.93MB）= 完整绑定官方模型，Blender 实测验证**：① Armature "NPC_Kanban_Paimon" 76 骨，**顶点组 48/48 精确匹配骨骼名**（Bip001 全套 + 头部表情骨 +EyeBone/HairS/EarB + 斗篷链 AmiceB L/R A-C×3 + DMZ R 01/02 拇指链 + CloakRoot/Point）——**此前三缺（骨架/索引名/权重）一次全补齐，无需任何重建**；② 蒙皮健康：Body 8397 顶点 48 组 avg_w=1.000 max_inf=3、Cloak 316/9、EyeStar 90/2、Face 1858/1（挂头骨）；③ **Face 带 49 个语义名表情 morph**（Eye_WinkA/B、Eye_Jito/Ha/Wail/Tired、Brow_×10、Mouth_A/I/U/E/O/N+Smile01-06+Angry01-04+Neko/Doya/Pero/Delta/Line/Square——**比 MMD 版 51 morph 更官方更全，连哈希名问题都消失了**）；④ Body/Face 各带 _UV1 副本（lightmap UV）；⑤ 高 0.937m 与 dump blend 完全一致（同一模型确认）。配套：全套官方贴图（含此前缺的 Cloak_Lightmap/Body_Shadow_Ramp 等）+ DetectiveGoggles 挂件。`Paimon.dae` 58MB=morph 完整版备胎。

**2026-08-24 深夜·纯原神资产落地完成（PaimonPet 场景已切换官方模型）**：
- **资产入库**：`Assets/Art/PaimonPet/GI/`（Paimon.fbx Legacy 导入 + Textures 32 张 + Animations 199 条）。199 条=Ani_NPC_Kanban_Paimon* 全量（排除 Ani_Cs_ 过场与 Beyd_Eff 特效骨）；clip 经编辑器 `legacy=true` 批量转换（m_Legacy:1 落盘）
- **零重定向直读验证**：.anim 曲线路径 `Bip001/...` 与 FBX 骨架同源命名；Standby 664 绑定 634 直名解析 + 30 哈希路径（path_数字，AnimeStudio 对部分手指/Nub 骨的 hash 序列化）经**引擎 binding hash 正常匹配**（实测 Finger1 8.7° 驱动）；Greet 手臂 34.6°/头 35.5° 大幅驱动确认
- **场景接线**（`Tools/桌宠/GI官方模型: 材质与场景接线` PetGIModelSetupTool，幂等）：GI_Root（Prefab 实例，scale 0.272 对齐 MMD 视觉基准高 18.38、localPos.y=-12.07 脚底对齐）提到 Paimon 子物体首位（命中链路 GetComponentInChildren 抓 Body）；Body/Body_UV1→GI_身体、Face→GI_脸、Cloak→GI_披风、EyeStar→GI_特效（PaimonToon _ShadowStrength=0 无光照定案，_UV1 副本禁用防重绘）；**Animation 组件从 Paimon 根挪到 NPC_Kanban_Paimon_Model 骨节点**（Bip001 路径解析起点），199 clip 注册+Standby 默认；MMD 三件（Paimon_arm/Paimon_mesh/_DropShadow）禁用留兜底
- **运行时适配**（代码改动）：PetLookAtController/PetBlinkController 骨名与 morph 名改 Inspector 可配字段（默认 MMD 名，场景配 GI 名：Bip001 Neck/Head、+EyeBone L/R A01、Eye_WinkA_L/R）；PetBlink/PetEmotion 的 SMR 查找改"取 morph 数最多的 SMR"（GI 多 SMR 场景 morph 全在 Face，MMD 单 SMR 兼容）；情绪表 Anger→Mouth_Angry01；PetFingerPoseController 禁用（GI 动画自带手指曲线，无需程序化手指层）
- **构建验证闭环**：RESULT OK + 构建 DLL UTF8 元数据验新代码（blendShapeCount=True）+ Player.log 零异常零警告 + 199 clip 预热 13ms + 帧率 6.06ms 零掉帧。**PlayerDataCache 陈旧坑复现**（ImportAsset+Refresh+BuildImmediate 一把梭时 delayCall 推迟构建仍用旧缓存；正解=ImportAsset → start_compilation_pipeline 等编译完 → 再 BuildImmediate，size 变化+DLL 标记双验证）

**2026-08-25·星星眼+披风无星空双 bug 根治（EyeStar 隐藏/披风星点合成）**：用户目检"眼睛怎么是星星眼+斗篷纹理不对"。
- **星星眼根因 = EyeStar 金色四角星条带**：官方立绘与 Q 版头像实证**默认眼神=干净浅蓝/宝蓝渐变虹膜+黑色小瞳孔+白色圆形高光，无星形图案**（星星元素在发饰；"星星眼"是特写表情梗）——且该星形几何烘焙实测比虹膜盘大 3 倍多（Y 向 0.022 vs 0.006），金色不透明渲染=巨大金星糊满双眼。MMD 基准版（用户认可）也无此层。虹膜本身采样头发贴图右下星空圆区（UV[0.53,0.99]×[0.01,0.19]）与 MMD 完全一致=正确（官方"远看蓝瞳近看星辰"）。修复=PetGIModelSetupTool 接线后 EyeStar SetActive(false)（恢复=重新激活节点）
- **披风根因 = Lightmap 光照 UV 与 Diffuse UV 空间不重合**：官方银河披风的星点在 Cloak_Lightmap，但官方 Lightmap 走光照 UV 通道（rip 无 UV1；Body_UV1/Face_UV1 分体网格即此机制），星点在光照 UV 的位置（星簇集中在 V0.5-0.95）与 Cloak 网格 Diffuse UV 桶不重叠——直接原位叠加后渲染全落星稀疏区=完全无星（实测）。修复=工具提取 Lightmap 亮像素星点（22 颗，洪泛连通）伪随机散布到 Diffuse 蓝色内衬区（V<0.45，白区不污染）合成 `NPC_Kanban_Paimon_Tex_Cloak_Composed.png`（固定种子幂等；重生成=删文件+重跑接线菜单）。分网格隐藏对照验证：只留 Cloak 时深蓝披风星空正常渲出；脖子深蓝领是 Body 网格采纯色蓝区=官方纯色设计（非 bug）
- **方法论**：GI 贴图考古要分清 **Diffuse UV / 光照 UV（UV1）两套空间**——Lightmap/ILM 类贴图与 Diffuse 不在同一 UV 空间时不能直接按像素叠加；判定用"网格 UV 桶分布 vs 亮像素簇分布"相交测试

**2026-08-25·GI 时代收尾（随机小动作/测试面板修复）**：GI 切换后三处 MMD 残留导致"久待机无随机小动作 + 测试面板按钮全无反应"——①PetBehaviorController 序列化值（待机/打招呼/出场/退场/随机小动作列表 12 个名）仍是 `_MMD` 后缀，Animation 注册表里无此名 → Play 静默返回；②PetSceneSyncTool 还是 MMD 时代实现（ClipDir 指 Animations/MMD + FindObjectsOfType 取第一个 Animation，会命中 GI_Root 空残留组件），测试按钮全绑 `_MMD` 名；③GI_Root 上残留空 Animation 组件。修复=三值同步 GI 名 + 工具改 GI 时代（ClipDir=GI/Animations、目标=PetAnimSwapper.TargetAnimation（新增公开属性，杜绝按遍历顺序找）、排除 ~100 条 Left/RightArm/Hand/Forearm/Finger/HeadControlRotation 单帧姿势层——测试面板 102 个按钮全为完整动作）+ 删空残留组件。**教训：模型/动画体系切换时，凡"按名字引用动作"的序列化字段（行为层列表/按钮 onClick 持久参数）必须随切；动画引用审计要覆盖 Inspector 序列化值，不只看 Animation 组件注册表**。

**2026-08-25·GI 模型三大目检 bug 根治（头发贴图错乱/眼睛只有眼白/体型过大）**：
- **根因一（材质槽）**：Body 网格实为 **2 子网格/2 材质槽 = [身体 | 头发]**——头发、虹膜（291 顶点，采样头发贴图右下星空区域，与 MMD 眼睛网格同机制）、发饰全在槽 1。旧版 PetGIModelSetupTool 用 `smr.sharedMaterial = mat` 单值赋值把两槽全压成身体材质 → 头发渲染身体贴图内容（含左下角星空衬里图案 →"星空跑到头发上/斗篷缺一块"）、虹膜渲染成白布（"眼睛只有眼白"）。修复=按槽位赋值 `sharedMaterials = [GI_身体, GI_头发]`（工具已改幂等接线）。EyeStar 条带（双眼星形瞳孔几何，50v/64tri，横跨双眼采样脸贴图左下角 255,236,173 淡金纯色块全不透明）→ GI_特效贴脸贴图取色
- **根因二（体型）**：GI_Root scale 0.2717 对齐的"MMD 基准高 18.38"是幻影基准——实测本场景 MMD Standby 世界高仅 **0.6078**（近相机 Z≈0 体制），GI 模型却摆到 Z≈-19 远处放大到 18.6 高（屏幕占比 108.8% vs MMD 42.9%，2.5 倍）。修复=**完全复刻 MMD 摆位体制**：以"Standby 同源动画烘焙包围盒逐维对齐"为判据（高/底Y/中心XYZ 五项全 0 误差），GI_Root scale 0.00887、localPos (0.03,-0.14,-0.02)，模型回到 Z≈0 近相机小尺寸体制——与 MMD 版完全相同的屏幕占比 42.9%/底部 viewportY 0.315，且所有运行时控制器（命中/视线/行为层）继续工作在 MMD 时代验证过的小尺寸世界坐标系
- **方法论沉淀**：跨模型替换的"视觉对齐"必须以**同姿势烘焙包围盒实测**为准（AnimationMode.SampleAnimationClip + BakeMesh + localToWorldMatrix 变换），不能信文档里的旧基准数字；烘焙测高同时是蒙皮健康与摆位的双重验收

**2026-08-25·Show 系王冠不动根因=GI 原始 clip 哈希路径死绑定（已根治）**：用户目检"Show_3 摘王冠动作王冠纹丝不动"。根因不是模型也不是动作源——是 **AssetStudio 导出时辅助骨曲线的 path 是未解析哈希字面量 `path_<CRC32(骨完整路径)>`**：王冠几何（Body 网格 502 顶点权重 1.0）挂 `+EarB CF AF01` 骨，该骨曲线数据完整存在（Show_3 262 keys/4.35s/180° 翻转展示）但 path 写的是 `path_3120751086`，Animation 按路径逐字绑定 → 场景无此节点 → 曲线静默丢弃 → 王冠全程冻在绑定姿势。§2.2 落地记录中"30 哈希路径经引擎 binding hash 正常匹配"的结论**只对主链骨成立**，辅助骨哈希实际全是死绑定。**影响面**：199 条 clip 共 8986 行哈希 path，涉及王冠/头发 +HairS/披风链 CloakRoot+AmiceB/部分手指骨（每文件 27 骨×旋转/位置/缩放三段）——头发披风手指僵直同源。**修复**（`Tools/桌宠/GI原始动画: 哈希路径修复` PetGIAnimHashFixTool，幂等+干跑，文本级重键不动 m_ClipBindingConstant 派生表）：按场景模型层级建 CRC32 表还原真实路径，109 文件 8262 处替换（34s），Show_3 具名活绑定 360→610、王冠 180° 曲线绑定成功。**11 个未解析哈希残留**（2370 绑定）=骨不在本 FBX 骨架（推测 GI 全骨架的右侧披风链等），无目标节点不可修，保留无害。**注意**：方案B管线 v23 转出 MMD clip 时已做同样还原（同 CRC 算法）；本工具补齐 GI 原始直读路线；**GI 动画重新导出后必须重跑此菜单**。工具性能红线：199×20MB 大文件禁 text.Split（GC 风暴致首跑 330s 桥超时→无限重试僵尸→杀编辑器重启），用指针式行迭代+"path:"IndexOf 预筛+段头行放行（段头不含"path:"，漏放行 section 恒空全部漏修——首版实证）。

**蒙皮坍缩 Bug 复盘（2026-08-24 深夜，用户目检"骨架在动没有皮"）**：首版构建模型渲染为黑色骨架剪影。逐层客观排查（像素统计/烘焙网格/红壳二分）：①网格+动画+相机全正常 ②烘焙网格顶点全部坍缩在骨架根部（8 千顶点挤在 Y±3）——**蒙皮矩阵错**。根因=**ModelImporter useFileScale=True 的 fileScale=0.01（FBX 厘米标记）与 FBX 内容器节点 scale=100 冲突**：网格顶点被 ×0.01 但 bindpose 保持单位缩放、骨骼链带 ×100 → 蒙皮公式三空间量级不匹配（Blender 验证时正常因 Blender 导入烘干容器 scale）。修复=**importer `useFileScale=false` 重导入**（三空间同量级）+ 删场景旧实例重新 Instantiate（prefab 实例缓存旧骨骼位置）+ 相机数学重摆位（目标：世界高 18.38 对齐 MMD 基准、中心 X=0/Y=-2.83/Z=-24.58，GI_Root scale=0.2717）。Play 终验：画面 33% 覆盖、暖肤色 5980 像素+白色 69029（白衣白发贴图正常渲出）、Standby 烘焙高 13.09（蜷腿合理）。**教训**：①FBX 导入后必须"烘焙测高"验收蒙皮（静态实例 BakeMesh bbox 应=网格原 bounds，被压扁=导入器 scale 冲突）②cam.Render() 手动渲 RT 在 URP 下不画无 SRP tag 的 pass（PaimonToon 全透明假象），验证渲染必须 ScreenCapture（且需 end-of-frame 后调）③Play 模式里读 TextureImporter.textureType 曾误报 Sprite（meta 实为 Default=0），资产状态以 meta 文件为准。
- **待用户目检**：官方模型渲染/动作表现/眨眼/情绪表情；**遗留**：GI 阴影壳未建（影子暂禁用）、PetLookAt 眼球骨曲线存在性待目检（GI clip 有 EyeBone 曲线，叠加层可能与动画冲突）、测试面板按钮还是 MMD clip 名（需 PetSceneSyncTool 同步 GI 版）

**2026-08-24 晚·成品下载验证（GameBanana Mod 圈资源，路线重启）**：用户拍板"现动作问题多，不如纯原神资产，先下成品"。已下载 rain_9 两个派蒙 Mod 的 modder 资源包至 `D:\Tool\PaimonModel\RipTest\`（redesign blend 14.9MB + reshaped blend 7.8MB + 3DMigoto 原始 dump vb0/ib/hash.json + 贴图 PSD/DDS；作者要求 credit）。**关键收获：原始权重网格已到手**——blend 内 vanilla 部件合计 ~11750 顶点（General 2403/Skirt 4439/Hair 1130/Sleeves 1171/Hands 1180/No skirt 1583/Legs/Boots/Ears/Eyes），71 槽骨骼调色板中 48 个索引在用，权重归一化 1.000（真蒙皮，来自 3DMigoto vb0 帧导出——**"权重断链"从此有解**）；reshaped blend 另含 7147 顶点头部（比 Kanban 导出面部件 ~2000 顶点细 3.5 倍，live 伴侣派蒙比 gi70 Kanban 资产新/更细）。**仍缺三块**：①blend 无 Armature（Mod 注回游戏不需要骨架）——但 AnimeStudio FBX 导 Blender 得 103 个 EMPTY（含 53 Bip001 完整层级+位置，无 bone 数据），可脚本重建骨架；②顶点组是裸编号 0-70（Mod 圈靠手动目测映射，社区无文档），需空间配对自动化（组质心 vs 骨位置，左右对称可验证）；③dump 不含表情 morph（伴侣派蒙游戏内有表情，为独立 draw call）。**决定性终测方案**：重建骨架→配对→FBX 入 Unity→原始 .anim（Bip001 路径）直接绑定播放=零重定向。备选捷径：models-resource asset 328738（Paimon 15.55MB/45 文件完整 rip，同站 Chasca 包含 .fbx+.dae+全贴图——若其 FBX 自带蒙皮骨架则跳过全部重建；站点 Cloudflare 盾脚本下载被拦，需浏览器手动下载）。

工具链（AnimeStudio，Escartem fork）已打通解密与 AssetMap，但看板派蒙**无专属本体网格**（名字带 Kanban 的 Mesh 仅特效/预览网格，本体复用普通派蒙 NPC 模型且资产名不含 "paimon" 无法定位）；CLI GameObject/Animator 导出全部 `has no mesh, skipping`——**仅角色 SkinnedMesh 引用链断**（Unknown ClassIDType 1152437153 自定义渲染类，AssetStudio 系解析不了），GUI 手动导同一解析内核大概率同断。**结论：GI 网格提取判死，MMD 路线定案**；254 个看板动作 .anim 在 `D:\Tool\AnimeStudio\out_kanban\` 备查（Bip001 骨骼，经方案 B 重定向用）。工具链/资产位置速查见项目记忆与 HANDOFF 文档。

### 2.3 资源位置速查（AI 开发用）

| 资源 | 位置 | 说明 |
|------|------|------|
| **项目内入库** | `Assets/Art/PaimonPet/` | MMD 模型 FBX + URP 材质 ×8 + 贴图 + 动作（~112MB） |
| **模之屋原始包** | `D:\Tool\PaimonModel\` | PMX 原文件+贴图（唯一模型基底） |
| **GI 已导出动作** | `D:\Tool\AnimeStudio\out_kanban\` | 254 个 .anim + 16 张贴图（看板派蒙全量动作源） |
| **Blender 转换链** | `D:\Tool\Blender\` | blender.exe（4.2.3）+ mmd_tools + `pmx2fbx.py` |

**动作可用性（2026-08-24 全量完成）**：GI .anim（Bip001 骨骼路径）经 `Tools/桌宠/方案B: GI动画重定向到MMD骨架` 管线（§2.5）转换为 MMD 骨骼动画——**GI 看板动作为动画主来源，254 个导出中全部命名动作已转换（97 条）**。新动作流程：从 out_kanban 拷贝 .anim 进 `Animations/` → Refresh 验证导入 → 管线 clips 数组加名字 → 跑菜单（详见 gic-paimon-retarget skill；注意 m_Compressed=1 压缩标志坑）。VMD/手 K/AI 作为补充来源备选。

### 2.5 方案 B：GI 动画重定向到 MMD 骨架（2026-08-22 v8 完成 / 2026-08-24 v20 定案 / v22 切线定案）

- **产物**：管线 `Assets/_Scripts/Editor/PaimonRetargetPipeline.cs`（菜单 `Tools/桌宠/方案B: GI动画重定向到MMD骨架`，日志 `Logs/retarget_log.txt`）；转换 clip 输出 `Animations/MMD/Ani_..._{名字}_MMD.anim`（legacy）。**单场景方案（2026-08-24）**：独立测试场景 PaimonRetargetTest 已废弃删除——管线步骤 6 直接同步 PaimonPet（`PetSceneSyncTool.SyncInternal`：动画列表注册 + AnimUICanvas 测试按钮重建，onClick 持久绑定 PetAnimSwapper.Play）；测试面板挂 `PetEditorOnly` 门（编辑器 Play 可见，构建版 Awake 自禁用——桌宠进程不出现测试 UI）
- **当前动作集（2026-08-24 傍晚，命名动作全量）**：**97 条 = 全部命名看板动作**——原 25 条（Standby/Greet/Anger/Sneer01/Clap01/Nod01/ShakeHead01/Refuse01/Run/SitLoop/Sleep01/Turnback/Domagic + Shy01/Confuse01/Think01/Like01 各 AS/BS/Loop）+ 2026-08-24 新增 72 条（转换清单 `PetRetargetConfig.asset`）：Appear/Disappear/Show_1-4/Hope/Guide + Chat01/Chat02/LookAround01/Whisper01/Shrug01/Akimbo01/HoldArm01/CoverEyes01/PutHand01/StrikeChest01/Forerake01/Backrake01/PointL01/PointR01 各 AS/BS/Loop + Nod02/ShakeHead02 + FlyMove/NormalMove/Turn_90L/Turn_90R + Dive 系（Run/RunDown/RunUp/Walk 各 AS/BS/Loop + Turn90L/R）。**未转换类**（有据可查的排除）：Ani_Cs_* 编号过场（用户拍板不要）；Beyond_* 5 条零骨骼曲线（纯特效）；Beyd_Eff_* 3 条水晶球特效骨；Left/Right Arm/Hand/Forearm/Finger + HeadControlRotation 共 ~100 条 0.017s 单帧姿势层（GI 程序化方向叠加，非动作，未来做手势系统可用）
- **Nod02/ShakeHead02 平反（2026-08-24）**：旧"源曲线全哈希路径"诊断有误——真根因是这两个文件是 254 个导出里仅有的 `m_Compressed: 1`（压缩标志使 GetEditorCurve 取不出显式曲线）；改 0 重导后正常转换。二者是头链局部动画（仅 首/頭/发/眼 11 骨曲线），身体回退 Standby t0 静止——播放时"只有头在动"属源数据特性。Dive 系 15 条同理无腿曲线（腿=Standby t0）
- **架构（v20 定案）**：MMD 网格/权重/bindpose **零改动**；对齐复用 A 期手轴矩阵（scale=1.158 rotY≈150.2° 盆锚点）；旋转 = GI 世界增量共轭 × f0 姿势基准（54 代表骨，**腿部 D 链 6 骨已并入代表骨**——足D/ひざD/足首D ↔ Thigh/Calf/Foot 单链语义，腿部蒙皮全在 D 链，v13 曾回归冻结大腿段）+ **全骨跟随驱动**（159 辅助骨绝对世界追踪 qWant）；位置仅腰锚传递 bob/根运动，其余骨恒绑定（骨长恒定=蒙皮零拉伸）；躯干骨姿势参考用"指向头骨"；腰锚舞台偏移扣除（Cs 系列根骨 12m 舞台位移归位原地）
- **手指（v20 双约束重建，2026-08-24 根治全 clip"掐爪"）**：20 手指骨为代表骨，qPose = F2·F1⁻¹ 双约束——骨段方向 locDir→R⁻¹·dGi(0) + 扭角 palmLocal→R⁻¹·palmBackGi(0)（GI 末节段用 Nub 尖定向），骨段/掌心全程精确跟踪 GI（恒等式同 v6），手指审计 25/25 seg 误差 0.0°。v17 的 `LookRotation(绑定骨段, 掌背)` 整体替换世界旋转是病根——Blender 导出的 MMD 手指子骨局部偏移沿 local+Y，替换后骨段被掰 ~90° 折成掐爪（实测根节弯 76-85° vs GI 22-49°）。**R 拇指走 DMZ 链**（GI R 手无 Bip001 R Finger0/01，拇指=DMZ R 01→02→Finger0Nub，配置表精确规则，此前 R 拇指全静态）。**源数据上限**：多数 clip 缺 R环指/小指（R Finger3/31/4/41）旋转曲线（实测仅 Standby 有），管线回退 Standby f0 局部随手走——非 bug。审计工具：`Tools/桌宠/手指方向审计`（PaimonFingerAudit，每帧先回 Standby 参考姿势再采样——AnimationMode 不采样无曲线骨，残留末态会报恒定假误差）
- **曲线切线与播放流畅度（v22 定案，2026-08-24）**：输出曲线全部 key 用 **C1 Catmull-Rom 有限差分切线**（in=out=(v[n]-v[p])/(t[n]-t[p])，`SmoothKey`）——**播放帧率高于 key 密度时的正确插值**（165fps 播 60Hz 密 key，key 之间靠切线插值，C1 保证段间速度连续无脉冲）。版本结论：v21 弦切线（in=前段斜率/out=后段斜率）= 精确线性插值，是 C0 折线——高刷细分采样下每个 key 拐点暴露 60Hz 速度脉冲，弃用；v19 与 v22 数值同构（同为 FD 中心差分），v19 时期的目检卡顿实为运行时 bug（小动作连环 churn/MeshCollider 烘焙尖峰/首播冷初始化/帧节奏，2026-08-24 逐一修复）。**运行时配套：开机预热全部 clip**（`PetAnimSwapper` 启动预热=逐 clip Play+Sample+Stop 后回待机）——legacy Animation 首播冷初始化实测 150ms 掉帧串（Player.log 16 连掉帧无子系统标记、t=64 首次摆手实证），预热把成本挪到启动帧，开销几十 ms 一次性。**目检终局（2026-08-24 用户拍板）：摆手（Greet）动作仍卡顿，放弃修复**——全套措施（C1 切线/预热/帧节奏/钩子零分配/动作期烘焙暂停）落地后残余卡顿为已接受上限，勿再主动排查动画卡顿
- **配置与自检**：`PetRetargetConfig` SO（映射规则表/CLIPS/阈值数据化，加动画只改配置）；数学核心 `PetRetargetMath` + 自检菜单 `Tools/桌宠/方案B: 数学自检`（109 断言）
- **审计工具**：`Tools/桌宠/腿部方向审计`（PaimonLegAudit，标准链+D链 双测）+ `Tools/桌宠/全身部位审计`（PaimonBodyAudit，14 部位）。**采样约定：只在 GI 曲线 key 时刻取值**——GI 源 Bip001 曲线存在 q≡-q 符号跳变，AnimationMode 均匀采样跨跳变逐分量插值产出假翻转伪影（审计伪影非管线 bug，管线 v11 已符号连续化加固）。**姿态审计铁律：必须用已知映射矩阵对比，禁止从骨骼位置重建局部坐标系**（此前多轮排查被"模型自身前后左右重建"误导）
- **演化坑史与逐版本踩坑**：全实测记录在 git 历史与 docs/14，此不赘述。目检结论（2026-08-22）：Standby/Greet/Anger 腿/腰/手臂病灶全部根治，13 条 clip 审计全绿

### 2.6 表情层（2026-08-22 完成，语义级复刻）

- **数据现状（实测）**：旧批次 `Ani_NPC_Kanban_Paimon_*`（含 Standby/Greet/Anger）**不含表情曲线**；Cs 系列（`Ani_Cs_*`）带 blendShape 曲线但属性名哈希化（GI mesh 打包时 morph 名哈希化，网格不可导出 → 哈希→语义映射无本地解）
- **表情与动作是两套系统**：状态机动画零表情曲线=运行时表情由另一层驱动（程序化眨眼/表情系统，行业通行 body/face 分层做法）；重导多少遍旧批次也导不出表情——源数据本就没有
- **眨眼数据陷阱（v2 修正，用户目检纠正）**：Cs 高频短脉冲曲线（每秒 12~30 次）是**说话嘴部/眼球微颤**，不是眨眼——真人眨眼间隔 2-10s。v2 修正：每循环 1 眨（t=1.3s）
- **方案（v4 定案，行业通行三层架构）**：**表情全部运行时驱动，零烘焙进 clip**
  - **身体层** = Animation clip（方案 B 管线产物，morph 无曲线）
  - **情绪层** = `Assets/_Scripts/Pet/PetEmotionController.cs`（指令式 SetEmotion，情绪表 Inspector 中文可配；`PetAnimSwapper.Play` 按动作名映射下发，当前 Anger→怒り 100% 过渡 0.25s；无映射动作下发空名清回默认脸）
  - **微动作层** = `PetBlinkController`（随机 timer 2.5~5.5s 眨眼，时长 0.13s，Inspector 中文可调）
  - **morph 独占约定**：组件管理的 morph 不得出现在任何 clip 曲线中（Animation 只覆盖 clip 中存在的曲线）——`PaimonFacePipeline.cs` 为纯清理工具（菜单 `Tools/桌宠/表情曲线清理(迁移运行时架构)`，幂等）；**重跑方案 B 管线后无需再跑表情注入**
- **MMD morph 表（51 个，实测）**：怒り[17]/にこり[16]/はぅ[5]/びっくり[6]/じと目[7]/困る[15]/真面目[14]/上[18]/下[19]/恐ろしい子！[12]…全表见 `PaimonFacePipeline.cs` 注释与模型 Inspector
- **待办**：其余动作情绪未配映射（Greet→にこり、Shy→照れ、Confuse→困る 等，加映射表条目即可零代码）；说话嘴型待桌宠对话功能时再做

## 3. 表现与交互设计

### 3.1 常驻显示

- 生命周期：启动游戏 → 自动唤出派蒙（已存在则跳过）；游戏退出/崩溃 → **派蒙独立存活**（2026-08-21 拍板不绑命）；双击派蒙 → 播退场动画后派蒙退出（2026-08-24 起，见 §3.3 出场/退场）
- 显示层级：优先于游戏窗口与其他应用；游戏后台时仍置顶悬浮于桌面
- 位置：默认屏幕角落；拖拽松手后记忆停留位置（**已实现 2026-08-24**：写入独立配置文件 `pet.json`——同 exe 双进程不得碰主存档，见 §5.8 约束；缩放倍率同文件持久化，防抖 1s 落盘+退出兜底）

### 3.2 拖拽交互（抓斗篷 · 仓鼠式拎起）

核心手感：**鼠标抓住的是背上的斗篷，不是派蒙本体**——像抓仓鼠后颈皮一样把派蒙整个拎起：

- 按住派蒙（命中斗篷区域）→ 斗篷被"捏住"，派蒙身体悬垂
- 移动鼠标 → 抓点跟随鼠标，身体因重力自然垂坠、随移动惯性摆动（钟摆效应，拖快了会被甩起来）
- 松手 → 下落回停靠位置，轻微弹跳/晃动后恢复待机

物理要求（"真实物理效果"）：

| 状态 | 要求 |
|------|------|
| 拎起 | 身体绕抓点下垂摆动（单摆），有角速度惯性与阻尼 |
| 拖动 | 弹簧-阻尼跟随（滞后+回弹，非刚性瞬移）；头/四肢相对躯干次级滞后摆动 |
| 斗篷 | 被捏住处固定，其余部分布料式摆动 |
| 松手 | 自由落体 + 落地弹跳衰减 |

- 候选实现：Verlet 链/弹簧骨骼（把派蒙当作挂在抓点下的多节弹簧链）或 Unity SpringBone；斗篷用 Cloth 或骨骼链。**不做全刚体 Ragdoll**（易穿模难稳定，弹簧链手感更好）
- 手感参数（摆幅/阻尼/回弹强度）落地时暴露为 Inspector 中文命名字段，用户自调

### 3.3 待机动作

- 动作集（初版候选，待模型入库后确认）：悬浮呼吸/原地扑腾、左右张望、打哈欠、发呆眨眼、长时间无操作→打瞌睡
- 触发：随机轮换 + 事件触发（无操作计时、拖拽结束、指令执行）
- 依赖模型动画与 BlendShape，具体清单待定

**实现（2026-08-24 首期，`PetBehaviorController`）**：光标在派蒙旁停留（模型包围盒投影屏幕矩形扩 90px 内停留 1.2s，冷却 45s）→ 播 Greet 一次回待机；久待机（光标不在旁）→ 随机小动作轮换（Nod01/ShakeHead01/Sneer01/Clap01/**Show_1~Show_4**，间隔 25-55s，列表/参数 Inspector 中文可调）。播放统一走 PetAnimSwapper.PlayOnce（新增：wrapMode Once + 情绪/手指挂钩同 Play），行为层只管"何时播什么"；拖拽中不触发。动作列表同步：菜单 `Tools/桌宠/同步 PaimonPet 动作列表`（PetSceneSyncTool，重跑重定向管线后跑一次——管线删旧建新 clip 会换 GUID，场景引用靠它对齐）。

**出场/退场（2026-08-24 傍晚）**：出场=进程启动后首帧（PetAnimSwapper 预热完成的同帧、渲染前）播 `Appear` 一次回待机（`PetBehaviorController.出场动画名`，Inspector 可换）；退场=双击退出改为先播 `Disappear`、播完才真正 `Application.Quit()`（`请求退场(Action)` API，窗口控制器注入退出回调；无 clip/无行为层时兜底立即退出；退场中屏蔽重复双击/新拖拽/一切行为触发）。编辑器 Play 下 Quit 无效——退场动画播完自然回待机，可反复目检。**出场/退场期间视线跟随与眨眼一并静默**（`置仪式静默`：头链完全交给动画曲线——`PetLookAtController.Set视线静默` 恢复时首帧重置平滑基准防瞬移归位）；普通单次小动作只静默眨眼不静默视线（待机微动+视线叠加是常态）。

### 3.4 视线跟随（头看鼠标）

- 头部实时朝向鼠标屏幕位置，平滑阻尼（转头不瞬移）
- 角度限制：超过肩颈合理范围即停止/带动身体，避免"拧脖子"
- 模型支持的话，眼球骨骼/BlendShape 瞳孔小范围跟随

**实现（2026-08-23，`PetLookAtController`，PaimonPet 场景）**：首(分担35%)→頭→目.L/目.R 分层叠加，世界空间增量法（LateUpdate 在 clip 姿势上叠加"光标相对屏幕中心偏移×最大角"的旋转增量，动画微动完整保留）；光标走 `PetWindowController.TryGetCursorUnityScreenPos`（Win32 全局轮询，无焦点/屏外可追踪）；指数阻尼平滑（头 8 / 眼 14）。"带动身体"未做（先只钳制停止）。

**眼球修复（2026-08-23 晚，v5 定案）**：曾"只剩眼白 + 鼠标不动眼球自转"。根因有二：①clip 中目.L/目.R 本体无曲线（曲线在 目戻/_dummy/_shadow 子骨上），动画每帧不覆写眼球骨骼，v3/v4 在上帧残留旋转上继续叠加增量 → **复利式累积自转**到极限位只剩眼白；②颈分担 35% 后头又叠加满份（头世界旋转已含父骨增量）→ 总偏转 1.35× 过冲，眼球余量被迫反向补偿。修复：**每帧用启动时本地基线重建中性姿态**（父骨当前旋转 × 本地基线快照）再叠加余量，不再累积；余量基准从首帧快照改为本帧动画姿态（待机摇晃不再漏进眼球反向补偿）；头分担改 `1-颈分担`（颈+头恰 1×）；眼球最大角 8°→15°（小幅不可见）。眼球余量 = inv(头链本帧实际叠加增量) × 目标增量，头未转满时眼球快速补足、头收敛后余量归零眼球随动画回中。

### 3.5 屏幕边缘探头（扒边框）

派蒙被拖到/停靠在**屏幕边缘**时的特殊状态（2026-08-21 新增拍板）：

- **进入**：拖拽松手时停靠点距屏幕边缘小于阈值（或直接被甩出屏幕），派蒙滑到边缘外，转为"扒边"——**身体在屏幕外，只露出头和扒住屏幕边框的双手**，朝屏幕内探头张望
- **表现**：手扒边框 + 探头（可配张望/眨眼/偶尔缩回去只露半张脸等小动作变体）；探头姿态下视线跟随鼠标仍生效
- **退出**：再次抓住拖回屏幕内，恢复正常悬浮；长时间无操作可自行爬回或原地打瞌睡（变体，落地时定）
- **实现要点**：
  - 边缘检测：虚拟桌面坐标系下判断宠物锚点是否越出屏幕工作区边界（多显示器：只认主屏/宠物所在屏的边缘，跨屏拖拽待定）
  - 动画：优先用原神提取的扒边/探头类动作参数还原；无现成动作则手 K（手部定位于边框 + 身体 offset 到屏幕外）
  - 与拖拽物理衔接：松手时的惯性决定"滑出"还是"停在场内"，越界后钳制到扒边姿态吸附点
- **边界**：只认**屏幕边缘**，不做逐窗口边框交互（拍板：暂不需要细致到每个小窗口）

## 4. 对话功能（预留接口）

草稿阶段只定**指令边界与架构预留**，不实现对话 UI 与 LLM 接入。**已拍板：接 LLM（一期不做）**，框架调研见 §4.3。

### 4.1 首批指令清单

| 指令（自然语言示例） | 映射游戏功能 | 现状 |
|----------------------|--------------|------|
| "设置游戏内时间（白天/黑夜）" | 大厅昼夜切换（PositionBack {time} 背景、dayAudios/nightAudios 配乐） | 昼夜资产已有，统一"游戏内时间"入口待定 |
| "打开背包" | BackpackScreen（SceneType.Load） | 已有 |
| "帮我抽卡" | WishScreen / 祈愿流程（WishManager） | 已有 |

### 4.2 架构预留

- 分层：**对话层（LLM 意图识别/Function Calling）→ 指令层（Intent→Action 映射）→ 既有系统调用**——不新写业务，只做编排（背包/祈愿/昼夜都是现成 Service，桌宠只是新 Controller）
- LLM 直接接入已拍板；关键词/规则匹配仅留作 LLM 失败/离线时的兜底降级
- 抽卡等敏感操作必须在主进程执行并走正常校验（货币扣减），桌宠侧只做表现与传话
- 指令执行结果以派蒙气泡/动作反馈（具体 UI 待定）

### 4.3 Agent/LLM 框架调研（2026-08，一期不实现仅选型参考）

本项目需求画像：**单角色对话 + 工具调用（Intent→Action）+ Unity C# + 中文**——不是多智能体编排场景，重量级 Agent 框架（LangGraph/CrewAI/AutoGen 等，且均为 Python/TS 生态）对本需求偏重，接入它们意味着为 Unity 游戏外挂一个 Python sidecar 服务。候选分层：

| 方案 | 类型 | 优点 | 缺点 | 适用 |
|------|------|------|------|------|
| **云 LLM API + 自研薄 Intent 层**（豆包/DeepSeek/GLM，OpenAI 兼容 + Function Calling） | 云 API | 零依赖、成本可控、中文强、function calling 原生 | 需联网/密钥管理 | **推荐主选**：C# HttpClient 直连，Intent 层就是 doc 已设计的 Intent→Action |
| **LLMUnity**（github.com/undreamai/LLMUnity） | 本地推理（llama.cpp 内嵌 Unity） | 完全离线、开源、Unity 原生组件（LLM/LLMAgent），支持 RAG；官网案例里就有"Anime Desktop AI Companion"桌宠 | 无原生 function calling（需手写 JSON 输出解析）；本地模型能力弱于云端；移动端仅 1-2B 小模型 | 离线/隐私敏感时的备选；也适合先做对话体验 demo |
| **Ollama/LM Studio + HTTP** | 本地推理（本地服务） | 模型可换、支持 tool calling 的新模型（Qwen 等）、与 Unity 解耦 | 本机常驻服务进程；用户机器需自装 | 开发期本地调试方便 |
| **Agora Conversational AI Engine / Inworld / Convai** | 商业 NPC 平台 | 语音全链路（ASR→LLM→TTS）开箱即用、游戏状态注入 | 按量计费、绑定平台、中文与定制受限 | 将来做"语音对话"阶段再评估，文字指令阶段不需要 |
| **通用 Agent 框架**（LangGraph/CrewAI/Vercel AI SDK/OpenAI Agents SDK 等 2026 主流） | 编排框架 | 多智能体/持久化/MCP 生态成熟 | Python/TS 生态，需 sidecar 进程；本需求用不上多智能体 | 暂不采用；若未来派蒙要多步任务规划（"帮我清日常"）再评估 |

选型结论（草稿建议，一期落地前复核）：
- **文字指令期**：云 API function calling + 自研 Intent 层（架构即 §4.2，无需引入框架）；LLMUnity 作为离线降级备选
- **语音期（远期）**：TTS 已有项目内通道（AudioManager.PlayVoice，可 voice_clone 派蒙音色）；ASR/对话编排届时评估 Agora 类平台 vs 自建
- 关注 **MCP（Model Context Protocol）** 作为工具接入标准的演进——若 C# 生态出现成熟 MCP SDK，Intent 层工具注册可对齐该标准，避免自研格式锁死

**关于"接团结提供的 GLM"（2026-08-21 查证）**：不可行——Tuanjie AI/Codely 的 GLM-5.2 Max 是**编辑器侧**开发工具模型（公测免费额度），AI Graph 也是编辑器侧 AIGC 资产工作流，均不提供游戏运行时 API。要接 GLM，直接用**智谱 BigModel 开放平台**（bigmodel.cn，同款 GLM 模型，OpenAI 兼容 + function calling，按量计费，自备 API Key），或经阿里云百炼等聚合平台调用。

## 5. 技术要点与风险（PC 桌宠形态）

1. **透明置顶窗口**：Unity 无原生支持——需 Win32 API（无边框 + WS_EX_LAYERED + 置顶 + 色键透明），相机纯色背景——**✅ 2026-08-21 spike 验证通过，已实现，详见 §5.8**
2. **双进程架构（已实现，生命周期二次拍板为"拉起不绑命"）**：同 exe 加 `--pet-mode` 参数启动轻量宠物进程（透明置顶窗口 + 派蒙渲染 + 拖拽/探头逻辑），主游戏进程照常全屏。主进程 GameScene.Awake 拉起子进程前先 OpenMutex 探测（Local\GIC_PaimonPet_SingleInstance，已有派蒙则跳过）；**不绑 Job Object、不持句柄**——游戏退出/崩溃派蒙照常存活；派蒙进程启动时 CreateMutex 声明单例（重复实例首帧前 Quit）。将来 IPC（named pipe/socket）仍按预留设计——指令（§4）经 IPC 下发主进程执行，游戏在跑则执行、不在跑则派蒙单机卖萌
3. **输入穿透**：悬浮窗非模型区域鼠标事件穿透到下层（按像素 alpha 命中动态切换穿透，Win32 WS_EX_TRANSPARENT 动态开关）；模型区域接收点击/拖拽
4. **模型导入**：MMD/原神提取 → Unity 的材质/骨骼/物理/动作重定向重建（§2）
5. **性能**：宠物进程常驻渲染单个角色需限帧（如 30fps）、待机降频、单光源简单 Toon；不能影响主游戏帧率与功耗
6. **多显示器**：边缘检测与停靠位置记忆需明确单屏/多屏策略（§3.5）
7. **战斗/联机兼容**：暂不考虑（拍板）；将来需要时再做隐藏/缩小策略

## 6. 平台差异

| 平台 | 形态 |
|------|------|
| Windows（PC） | 完整桌宠：桌面置顶悬浮 + 拖拽 + 对话指令 |
| Android | **游戏内悬浮（已拍板 2026-08-21）**——派蒙作为常驻 UI 挂件浮在游戏画面上（可拖动，仅游戏前台时存在）；不做独立进程/系统悬浮窗（安卓无桌面概念，`--pet-mode`、Win32 窗口改造在安卓均不适用，`PetProcessLauncher.Launch` 已加 `UNITY_STANDALONE_WIN` 守卫）。模型/动画资产与 Windows 版共用（已随 APK 打包）；实现待排期 |

目标全平台互通，移动端先保证游戏内形态不干扰正常操作。

### 5.8 Spike 验证记录（2026-08-21：透明置顶窗口 ✅）

最高风险项验证可行，实现已合入：

- **运行时代码** `Assets/_Scripts/Pet/`（命名空间 GIC.Pet）：`PetMode`（静态开关，解析 `--pet-mode`，编辑器恒 false）、`PetWindowController`（Win32 窗口改造+输入）、`PetLookAtController`（视线跟随，§3.4）、`PetAnimSwapper`（动作切换）、`PetEmotionController`/`PetBlinkController`/`PetFingerPoseController`（表情层，§2.6）
- **场景** `Assets/Scenes/PaimonPet.unity`：由 PaimonPreview 派生（模型摆位/相机构图/灯光不变），相机 tag=MainCamera，PetWindow 节点挂控制器；已入 BuildSettings
- **Boot 分支**：`GameScene.Awake` PetMode 守卫→跳过 Wargame 组合根/存档/设置，直接 LoadScene(PaimonPet)；Mirror 网络组件（MyNetworkManager/MyNetworkDiscovery）Awake 同样守卫（宠物进程无上下文，曾 NRE）
- **构建/运行**（同 exe 双形态）：菜单 `Tools/桌宠/构建 Windows 桌宠测试包`（Development 构建 → `Builds/PetSpike/gic.exe`）；运行 `gic.exe --pet-mode`；编辑器联调菜单 `Tools/桌宠/Play 时自动拉起派蒙`（默认开，`PetEditorAutoLauncher`，编辑器进 Play 自动拉起上次构建的桌宠 exe，Mutex 防重；退出 Play 去留跟随设置 closePetOnExit）
- **窗口改造**（`PetWindowController`）：去 WS_CAPTION/THICKFRAME 无边框；WS_EX_LAYERED + TOOLWINDOW + TOPMOST 置顶；**DWM 逐像素 alpha 透明**（`DwmExtendFrameIntoClientArea(margins=-1)` + 相机 bg alpha=0，主流方案；色键 LWA_COLORKEY 降为 Inspector 兜底开关）；启动停靠工作区右下角；**帧节奏 = vSyncCount=1（2026-08-24 晚拍板，屏幕满刷）**——演化：旧"限帧 30"（vsync=0 + targetFrameRate=30）在 165Hz 高刷屏上 33.3ms/6.06ms=5.5 不整除，每帧显示 5/6 个刷新交替 + 无 vblank 约束的产出抖动透传 DWM 合成 → 帧率不低但动作有顿挫感（动画数据 60fps 密集 key/循环无缝、MeshCollider 重烘 0.06ms 均实测排除）；中期 vSyncCount=2（每 2 刷新一帧，165Hz→82.5fps，任意刷新率节奏均匀）；现按用户拍板改 **vSyncCount=1（每个刷新一帧=满刷，165Hz→165fps）**，Inspector `垂直同步` 可调（0=限帧30后备/1=满刷/2=半刷），仅宠物进程执行、编辑器分支跳过（防污染编辑器 QualitySettings），vSyncCount>0 时 targetFrameRate 被忽略；v22 C1 切线本就为"播放帧率>key 密度"设计，满刷下插值密度更高更平滑；**MeshCollider 动态烘焙蒙皮网格**做像素级命中（每 0.15s 重烘跟随姿势，未命中时挂 WS_EX_TRANSPARENT 穿透）；按住左键抓模型拖动窗口（`GetAsyncKeyState` 全局轮询，不依赖窗口焦点）

**关键坑（全实测，详见 docs/14）**：
- **DWM 生效前提**：必须关闭 Player Settings 的 `useFlipModelSwapchain`（flip model swapchain 不透传逐像素 alpha，开启时 DWM 窗口整体全透明连模型一起看不见）
- **DPI 缩放**：exe 清单 PerMonitorV2（客户区物理像素=渲染像素）；窗口客户区物理尺寸=逻辑尺寸(550×825@96dpi，2026-08-24 由 480×720 扩出阴影边距，见 §5.10)×GetDpiForWindow/96×当前缩放；命中/视线换算改 GetClientRect+ClientToScreen（消 GetWindowRect 隐形边框误差）
- **缩放对齐主流桌宠（2026-08-24 三期：固定窗口=闪烁根治）**：**窗口客户区尺寸恒=基准(550×825 逻辑×DPI)×有效缩放上限，运行期不随缩放变化**——平滑过渡期间逐帧 SetWindowPos 改窗口尺寸会令 swapchain/DWM 高频重建合成（派蒙肉眼高频闪烁，实测），固定窗口后缩放只改 Paimon 根 localScale；相机/窗口恒定→**脚底客户区位置天然锚定**（绕脚底原地长高，对齐 DesktopMate/VPet 脚底语义）；oversized 窗口区域全透明+命中外穿透，无视觉/交互代价；**平滑过渡**=滚轮只改目标倍率，显示倍率每帧指数趋近（`缩放平滑速度`=12，0=瞬达）；**持久化**=`{persistentDataPath}/pet.json`（缩放+窗口客户区原点物理像素；防抖 1s 落盘+OnDestroy/退出兜底；启动恢复位置优先于右下角停靠，钳制到虚拟屏幕矩形——拔显示器后拉回可找范围；编辑器恒跳过，主进程永不读写该文件）；有效缩放上限=min(缩放最大, 工作区 95%)。**大小基准变化（2026-08-24）**：旧"窗口随缩放"版派蒙屏幕尺寸∝s²（模型+窗口同倍），固定窗口版∝s（仅模型）——同 s 值新版渲染偏大，存量 pet.json 缩放偏大属预期，滚轮重调一次即可
- **构建竞态**：`Tools/桌宠/构建 Windows 桌宠测试包` 经 `delayCall` 排到时若脚本编译/导入尚未落地，`BuildPlayer` 报 `Error building Player because scripts are compiling`（`RESULT FAIL errors=0 size=0` 假失败）。修复=`PetSpikeBuildTool.BuildInternal` 起手 + `AssetDatabase.Refresh` 后各加一道 `isCompiling||isUpdating` 守卫。**建成判据**：Editor.log `[PetSpikeBuild] RESULT OK errors=0 size=778059931`，exe/dll 属 payload 时间戳不更新、别以 exe 时间为准。**更深一坑（2026-08-24 实证）**：守卫齐全+编辑器程序集已新，BuildPlayer 仍可能复用旧 PlayerDataCache 产出旧代码的"RESULT OK 假成功"——判据升级=除 RESULT OK 外验证构建 DLL 含新代码字面量（UTF16 文本搜索）；补救=对改动 .cs 显式 `ImportAsset(ForceUpdate)` 后重编译再构建（详见 gic-pet skill）
- **命中关键坑**：`BakeMesh(mesh, useScale=true)` 输出在 SMR 局部/骨骼空间（顶点模长 <1），命中节点必须与 SMR 同 transform 且不能带额外缩放；DWM 透明分支必须挂 `WS_EX_LAYERED`（曾漏挂致穿透/双击全失效）；命中坐标须按 `Screen.width/窗口物理宽` 换算 DPI
- **拖拽目前是"移动窗口"**，抓斗篷/钟摆物理（§3.2）未做——下一步核心手感

**同 exe 双进程约束（重要，新功能必读）**：
- 桌宠与主游戏共享 HKCU\Software\HGAME\gic 的 Screenmanager 注册表——桌宠以窗口模式启动会写坏"上次窗口尺寸"，主进程启动若只切 fullScreenMode 不设分辨率会**竖屏启动**（已踩，2026-08-21）。修复=SettingsApplier.ApplyDefaultFullscreen() 显式 SetResolution(原生, FullScreenWindow)；新增显示逻辑必须走显式分辨率
- 桌宠**永不读写主存档**（PlayerSaveData/存档系统）——位置记忆等桌宠自有状态将来走独立文件（如 %USERPROFILE%/AppData/LocalLow/HGAME/gic/pet.json）
- 桌宠**永不触发存档写盘**（当前 PetMode 分支已隔离 Wargame/SaveManager，维持）

### 5.9 派蒙原神风格卡渲（2026-08-22 一期完成）

- **Shader** `Assets/Shaders/PaimonToon.shader`（内置管线 CGPROGRAM 风格，URP 下无光照 pass 正常渲染，项目既有风格）：完全自包含**不读场景灯**——虚拟光方向=材质属性 `_LightDir`，无灯场景保持贴图原生亮度（用户拍板"不需要光照"）；Pass1 本体 + Pass2 视图空间法线外扩描边（Cull Front 壳，距离补偿）
- **关键原料结论（实测）**：模之屋 MMD 贴图（脸/头发/衣服）与官方 Diffuse **UV 布局完全一致**（纹理相关性 1.000，即官方贴图放大重绘版）→ 官方 ShadowRamp/Face_Lightmap 可直接用于 MMD 模型；披风2/表情是改模者自绘布局（相关性 0.1，不接入）
- **ramp 行选择有官方依据**：Body_Lightmap R 通道=材质 ID（皮肤 texel R=1.0、白布 R=0）× 官方公式 `rampV=ilm.r*0.5+0.25` → 皮肤 v=0.75（暖光紫影带）、衣服 v=0.25（薰衣草影带）；头发固定行 v=0.425
- **面部=原神 SDF 阴影**：Face_Lightmap R 通道（R=G=A 同图，已设线性导入 sRGB=off）即 SDF 面部阴影图；`_FACESHADOW` keyword 启用，光从右→镜像采样（**实测 SDF 高UV亮侧=模型左脸**，镜像条件 L.x≥0）；亮/暗色与皮肤同 ramp 行 → 面部与身体色调连续
- **材质参数**（8 个 .mat 在 `Assets/Art/PaimonPet/Materials/`，全部 Inspector 中文可调）：脸=SDF+皮肤行+描边0.7、皮肤=0.75行、衣服=0.25行、头发=Hair ramp 0.425+描边1.2、披风=0.925行双面、眼白/眼睛/表情=全亮不描边；Ramp 贴图已关 mipmap 防跨行混合
- **测试场景已并入 PaimonPet（2026-08-24 单场景方案）**：渲染与灯光彻底解耦原则不变（PaimonToon 材质不读灯）；原 PaimonRetargetTest 的无灯/绿色地面设定随场景废弃（审计工具为纯数学审计不依赖地面）
- **2026-08-22 二次拍板：桌宠完全无光照（当前生效状态）**——用户目检反馈"看起来仍有光照、被照成绿色"。像素采样实证根因：白发×ramp 中段被染灰压暗+部分 ramp 段黄绿脏色调+SDF/ramp 明暗渐变的"光照感"。**全部 8 材质阴影强度=0、脸部 SDF 关闭**——输出=纯贴图颜色（复采样头发回中性灰）。ramp/SDF/虚拟光参数全部保留在材质上，将来想开随时 Inspector 调回；重开时注意头发 ramp 行 0.425 亮端偏黄绿 (0.92,0.93,0.82) 应换行
- 踩坑：Tuanjie ShaderLab 属性解析器不支持属性值引号/中文（docs/14 §13）；shader 编译错误判定用 ShaderUtil.ShaderHasError（console 读不到）
- 遗留：官方 ILM 其余通道（AO/金属/眼影）、Genshin 边缘光（RimLight）、发影（头发在脸上的投影）未做；SDF 阈值随头骨旋转的动态跟随未做（当前按绑定姿态近似，虚拟光固定时静态阴影已够）

### 5.10 桌宠阴影（2026-08-24 二期重做：高斯投影 = 2026 主流桌宠做法）

- **需求定案**：用户要的不是脚下地面阴影，而是 **DWM 窗口阴影式屏幕投影**——派蒙轮廓投到桌面上、跟随她的姿势（像网页/Windows 窗口在屏幕上的投影）。首版"脚下椭圆 Quad"已废弃；次版"PaimonToon 加 Pass3"因 **URP 只执行第一个 Pass**（多 Pass 描边/投影 Pass 全失效）而完全不渲染，已回滚；一期定案的"双物体法线外扩硬壳"（PaimonDropShadow.shader + .mat）**只有硬边实心影、无真正柔边（法线衰减是棋盘噪点），不符合主流观感，二期废弃已删**
- **二期方案 = 剪影 → 高斯模糊 → 偏移合成**（CSS `filter: drop-shadow` / DWM 窗口阴影 / VPet DropShadowEffect 同款管线，2026 主流桌宠标准做法）：
  1. `_DropShadow` 影子壳沿用一期结构（复制本体 Paimon_mesh 的 SMR，**共享同一套 219 骨骼 + 根骨 + 网格** → 姿势逐帧同步），8 槽全换 `PaimonShadowSilhouette.mat`（`GIC/PaimonShadowSilhouette`：纯黑 alpha=1 剪影、无属性），整体进专属层 `PaimonShadow`(slot 8)；主相机 cullingMask 剔除该层（当前 -257）
  2. `PaimonDropShadowController`（GIC.Pet，挂 Paimon 根，Inspector 全中文）运行时自建全套管线：`_ShadowCam`（主相机子物体、逐帧同姿同投影、只渲 PaimonShadow 层、`enabled=false` 由 LateUpdate 手动 `Render()` 保时序）→ 剪影 RT（高 512、宽随 aspect 换算、16bit 深度）→ `Hidden/GIC/PaimonShadowBlur` 双向 17-tap 高斯 Blit ×2 轮（每轮半径=总半径×0.66，两轮方差相加 σ≈0.42×总半径）→ `_ShadowComposite` 全屏 Quad（主相机近裁面前逐帧贴合视锥，`Hidden/GIC/PaimonShadowComposite`，Queue=Background+10 最先画，本体 Opaque 后画自然盖住重叠区；UV 平移=屏幕像素偏移，界外 alpha 清零防 Clamp 寻址 streak）
  3. 零灯光、零阴影贴图、零后处理——与 §5.9"桌宠完全无光照"定案兼容；半透明像素经 DWM 逐像素 alpha 直接合成到桌面
- **参数**（控制器 Inspector 中文，逐帧应用即调即见）：阴影颜色（Alpha=浓度，默认 0.6）、阴影偏移像素（x 右正/y 上正，默认 (0,-24) 下垂=光源在上方）、模糊半径像素（默认 14，RT texel）、阴影贴图高度（默认 512）、启用开关。防线：Awake 强制影子壳进层 + 主相机剔除，场景接线漂移也能工作
- **太不明显二次修复（2026-08-24 晚，双根因）**：
  1. **合成 Pass 的 Blend 把 alpha 平方了**——`Blend SrcAlpha OneMinusSrcAlpha` 单写法对 alpha 通道同样生效，缓冲区 A_out=A_src×A_src（dst 起始为 0），浓度 0.45 实际写入 swapchain 只剩 0.2，DWM 合成后影子淡掉一半以上。修复=`Blend SrcAlpha OneMinusSrcAlpha, One Zero`（alpha 直写；黑影在 DWM 直通/预乘两种解释下视觉一致）
  2. **窗口=模型包围盒没有影子落脚空间**——偏移 (0,-16) 把影子推出窗口底边被裁，脚下阴影全无，只剩两侧模糊光晕。修复=窗口留边距：窗口逻辑 480×720→550×825（×1.1458，2:3 比例不变）+ 主相机 FOV 40→45.27（同倍率放大视野）——**派蒙屏幕像素尺寸/构图完全不变**（窗口与视野同倍率扩大），四周留出 ~55-70 逻辑像素边距供影子落脚；启动参数（PetProcessLauncher/PetEditorAutoLauncher `-screen-width/-height`）同步 550×825。副作用：停靠右下角时派蒙脚下离屏幕底边略高（≈边距×缩放）
  3. 参数加强：浓度 0.45→0.6、偏移 (0,-16)→(0,-24)、模糊 12→14（配合边距加大）
  4. **影子呈"多个格子"= 模糊欠采样平台条带（三次修复，2026-08-24）**——旧 9-tap 高斯 tap 间距=radius/4（≈3.5 texel），硬边剪影卷积出平台状阶梯：相邻 tap 之间 alpha 恒定、tap 位置瞬间跳变（最大一档 23%），轮廓呈多层格子/贝壳纹。修复=Blur shader 重写 **17-tap**（间距 radius/8，权重 exp(-k²/32) 归一化、形状与半径无关→任意半径采样密度恒定）+ 控制器 **H+V 跑两轮**（每轮半径=总半径×0.66，两轮方差相加=旧单轮 σ≈0.42×总半径——模糊量/观感不变，第二轮把第一轮残余台阶再抹平）。已排除的非根因：8-bit alpha 量化（154 级/44px 足够平滑）、RT 双线性放大、Composite 采样
- **踩坑（2026-08-24 构建实测）**：内部 shader（Blur/Composite）**不能用 `Shader.Find("Hidden/...")` 运行时装**——构建期无场景/资产引用会被当未引用裁剪致 null（Awake `ArgumentNullException: shader`，编辑器能跑但打包版没影子）。正解=序列化 `Material` 引用（`PaimonShadowBlur.mat`/`PaimonShadowComposite.mat`，构建期必收录），运行时 `new Material(模板)` 克隆后改参
- **已知取舍**：眨眼/情绪 morph 权重存在各 SMR 组件上**不共享**——影子壳不眨眼不皱眉（morph 差异仅眼部几个像素，12px 高斯下不可见）；骨骼/蒙皮共享，姿势同步不受影响
- **遗留增强**：阴影方向/浓度当前为静态参数；将来可在 PetWindowController 按停靠屏幕边缘动态调偏移（如贴右边缘时影子偏左，对齐 DWM 光源语义）；拖拽物理落地后可接"拎起时影子变淡缩小"的距离反馈

## 7. 决策记录与遗留问题

已拍板（2026-08-21）：

1. **桌面置顶悬浮一期直接做**（双进程架构，见 §5.2），不做"先仅游戏内"妥协；不做逐小窗口交互，只认屏幕边缘
2. **模型**：模之屋链接已定（§2.1，入库时人工确认细节），原神提取作为模型+动作参数的备选/补充来源
3. **对话**：接 LLM，一期不做；框架选型见 §4.3（建议云 API function calling + 自研 Intent 层）
4. **战斗/联机中的表现策略**：暂不考虑
5. **拖拽物理手感**：落地时参数用户自调（Inspector 中文字段），先按默认值实现
6. **生命周期（晚间二次拍板，取代早前"同生共死"）**：**拉起但不绑命**——游戏启动唤出派蒙（单例防重），游戏退出/崩溃派蒙独立存活，双击派蒙退出
7. **透明方案主流优先**：默认 DWM 逐像素 alpha（DwmExtendFrameIntoClientArea，无毛边，2026 年主流桌宠同款方案）；色键 LWA_COLORKEY 降为 Inspector 兜底开关
8. **模型定案（晚间拍板）**：模之屋 MMD 模型为唯一基底；**原神提取路线放弃**（GI 网格反优化无解 + Bip001 动作与 MMD 骨骼不通，对接不划算）；动画来源转 MMD 生态（VMD/手 K/AI，三选一待定）
9. **动画来源（2026-08-22）**：方案 B 重定向管线完成（§2.5）——**MMD 模型 + GI 提取动作重定向**为最终路线；"全原神路线"的动画优势已被吸收（254 个看板动作可用），MMD 生态来源（VMD/手 K/AI）降为补充。目检结论：Standby/Greet/Anger 整体正常（腿/腰/手臂三处病灶全部根治）

遗留（落地前逐项解决）：

- ~~模型入库~~（2026-08-21 完成：MMD 版模型+贴图+材质+24 动作已入 `Assets/Art/PaimonPet/`）
- ~~模型路线~~（2026-08-21 深夜二次验证后**最终定案：MMD 唯一路线**——全原神路线的"GUI 手动导网格"首任务经 CLI 全面复查判死，见 §2.2；上游不修 GI 自定义渲染类则无解）
- ~~动作与模型骨骼不通~~（2026-08-22 方案 B 重定向管线根治，见 §2.5——MMD 模型直接播 GI 动作）
- ~~表情层缺失~~（2026-08-22 语义级复刻完成，见 §2.6）
- ~~原神提取工具链~~（AnimeStudio 就绪；网格导出判死不再需要——动作重定向路线无需 GI 网格）
- ~~透明置顶窗口 spike 验证~~（2026-08-21 验证通过并实现，见 §5.8）
- 多显示器边缘检测策略（§3.5）
- IPC 协议（派蒙→主进程指令通道；游戏不在跑时的单机降级）——生命周期解耦后不再需要"死亡通知"型 IPC
- LLM 供应商/密钥管理方案（一期不做，届时定）
