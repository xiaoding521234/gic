# AI派蒙（桌宠）

> 2026-08-21 立项。**本文 = 现状定案 + 设计意图 + 决策记录**。原则：只记"现在是什么、为什么"；过程流水与已废弃路线的排查细节在 git 历史与 docs/14，不再重复。操作工作流见 `gic-pet` skill；重定向管线操作见 `gic-paimon-retarget` skill。
>
> **可信度分级约定**：标注【实证】= 有实测数据/源码核验支撑；标注【目检】= 用户确认过观感；未标注的默认为当前实现记录。历史文档中曾被推翻的结论不删——在原地加"❌已推翻"标记，防止后人（含 AI）翻 git 旧版时再信。

## 1. 定位

AI派蒙 = **常驻桌宠 + 对话式游戏助手**：

- **表现层**：派蒙 3D 模型桌面常驻置顶悬浮，游戏失焦/最小化时仍在
- **生命周期**：**拉起不绑命**——启动游戏自动唤出（Mutex 单例防重）；游戏退出/崩溃后派蒙独立存活；关闭方式=双击她
- **交互层**：拖拽（物理手感未做，现为窗口直移）、待机动作轮换、视线跟随、屏幕边缘探头（未做，§6.3）
- **功能层**：LLM 对话指令入口——**已拍板接 LLM，一期不做**（§7）

非目标：战斗/联机中表现策略不考虑；不做逐小窗口交互（只认屏幕边缘）；移动端不做独立进程（仅游戏内悬浮挂件）。

## 2. 模型与动画（现状）

### 2.1 模型【实证】

**当前生效：GI 官方模型（2026-08-24 落地）**，来自 models-resource 完整 rip（asset 328738，用户手动下载）：

- `Paimon.fbx`（1.93MB）：Armature "NPC_Kanban_Paimon" 76 骨，顶点组 48/48 匹配，蒙皮健康（Body 8397 顶点 48 组 avg_w=1.000），Face 带 49 个语义名 morph（比 MMD 版更全，无哈希名问题），高 0.937m
- 入库 `Assets/Art/PaimonPet/GI/`（FBX + Textures 32 张 + Animations 199 条 GI 原始 .anim）
- 场景接线菜单 `Tools/桌宠/GI官方模型: 材质与场景接线`（PetGIModelSetupTool，幂等）：GI_Root scale 0.00887、模型 Z≈0 近相机小尺寸体制、屏幕占比 42.9%——**以"Standby 同源动画烘焙包围盒逐维对齐 MMD"为摆位判据**【实证，AnimationMode.SampleAnimationClip + BakeMesh】
- MMD 版（模之屋 PMX）兜底留存：场景内禁用（Paimon_arm/Paimon_mesh/MMD_DropShadow），恢复=激活+Animation 挪回+引用改回

**资产位置**：

| 资源 | 位置 |
|------|------|
| GI 官方模型+贴图+动画（现行） | `Assets/Art/PaimonPet/GI/` |
| MMD 模型+97 条重定向动画（兜底） | `Assets/Art/PaimonPet/`（~112MB） |
| 模之屋原始 PMX 包 | `D:\Tool\PaimonModel\`（授权：允许改造禁商用禁再分发——个人 Demo 合规） |
| GI 动作全量导出 | `D:\Tool\AnimeStudio\out_kanban\`（254 个 .anim） |
| Blender 转换链 | `D:\Tool\Blender\`（4.2.3 + mmd_tools v4.5.13） |

### 2.2 动画（现状）

**当前生效：199 条 GI 原始 .anim 零重定向直读**（Bip001 路径与 FBX 骨架同源）。Animation 组件挂在 NPC_Kanban_Paimon_Model 骨节点。

GI 直读路线三个数据修复**必须在重新导出/新增 clip 后重跑**（详见 gic-pet skill）：
1. 菜单 `Tools/桌宠/GI原始动画: 哈希路径修复`——AssetStudio 导出时辅助骨（王冠/头发/披风链/手指）path 是哈希字面量 `path_<CRC32>`，逐字绑定必死（王冠不动/头发披风手指僵直的根因）【实证：109 文件 8262 处替换】
2. Node 批处理 `gi_tangent_batch.cjs`——AssetStudio 导出全零切线（60Hz 速度脉冲=丝滑度差主因），FD C1 切线+四元数符号连续化
3. Node 批处理 `gi_pos_center.cjs`——Bip001 编排位移均值归位（往返型峰值减半）；**ref 必须外置固定常量**（自引用基准=非幂等跑飞，曾两轮全体漂 -8cm）

**方案 B 重定向管线（MMD 路线，现为兜底）**：97 条已转换产物在 `Animations/MMD/`，管线/配置/审计工具全在（gic-paimon-retarget skill）。切回 MMD 模型时用它。

**合成动画（2026-08-27）**：`Ani_NPC_Kanban_Paimon_SitUpright.anim`（边缘坐专用直坐姿）——上身=Standby 重采样、腿/手势=SitLoop（世界取向 Δ 补偿）、统一 SitLoop 122 键时间轴（2.017s 循环无缝）；生成器 `make_sit_upright.cjs`（.codely-cli/tmp，含两大解析坑注释）。另导入 5 条过场坐姿候选 `Ani_Cs_*` C01/C02/C04/C09/C10（legacy 补丁后入库，测试面板可播）。**Ani_Cs_ 系导入陷阱：m_Legacy:0 会被同步工具静默滤掉，须先补 1**。

> 历史注记 ❌已推翻：§2.2 早期记录"30 哈希路径经引擎 binding hash 正常匹配"**只对主链骨成立**，辅助骨哈希实际全是死绑定（2026-08-25 哈希修复工具实证）。

### 2.3 表情层（运行时驱动，零烘焙）【目检】

三层架构（行业通行 body/face 分层——GI 动画本就不含表情曲线，源数据如此）：
- **身体层** = Animation clip
- **情绪层** = `PetEmotionController`：情绪=多 Morph 组合（嘴+眉+眼联动，10 情绪 25 morph）；PetAnimSwapper 按动作名映射下发（13 条：Greet→Happy、Show_→得意、Sleep/SitLoop→Sleepy 等）
- **微动作层** = `PetBlinkController`：随机眨眼 2.5~5.5s
- morph 独占约定：组件管理的 morph 不得出现在任何 clip 曲线中

## 3. 窗口与渲染（现状）

### 3.1 窗口体制【实证+目检，2026-08-26 夜三次重构终案】

**固定画布**（VPet/eSheep 源码逐行核验后重写，用户目检通过）：

- **窗口**：750×825 逻辑（宽 2026-08-26 550→750：高度被工作区 95% 钳制顶格，横向是唯一可扩轴，两侧余量各+230px）×DPI×缩放上限固定尺寸；DWM 逐像素 alpha 透明（前提关 `useFlipModelSwapchain`）
- **核心原则【源码实证】**：一切动画期间**相机/模型根/窗口完全静止**——动画只在固定画布内演，窗口位置绝无"播动画的副作用"。VPet：全部动画渲染进固定 500px 画布（PNGAnimation img.Width=500），仅走路图（GraphType.Move）按显式速度向量移窗，其余动画一律 MoveTimer.Stop()，出界只打日志"当前动画移动设计错误"；eSheep：唯一权威状态 PositionX/Y 只由动画 XML 显式编写的步进累加（待机步进=0）。主流桌宠的位置精确是结构性的——位置只经显式移动（走路/拖拽/飞行）变更
- **窗口唯一移动源**：①启动停靠/pet.json 恢复 ②拖拽物理绝对定位（骨盆客户区投影钉物理目标，docs §6.1）③松手防丢拉回+防隐形守卫（完全出虚拟屏才干预）
- **缩放**：滚轮只改模型 localScale（固定窗口防 resize 闪烁）；pet.json 独立存档（缩放+窗口原点，防抖落盘）
- **帧节奏=DWM 帧对齐（2026-08-27 v4 终案）**：分层窗口 blt present 不阻塞→vSyncCount 实际无效（canvas 矩阵 min=6.05ms 实证 ~130fps 自由跑=匀速动画全程 judder）；`DwmFlush` 每帧阻塞到下一次桌面合成=唯一有效节拍器，165Hz 实测 99% 帧严格 6.06ms/163fps。开时强制 vsync=0+不限帧。演化史：vsync0+限帧30→vsync2→vsync1→自适应分频→**DwmFlush**（前四代均建立在"vsync 有效"的错误假设上）
- 画布性能矩阵【2026-08-26 实测，--pet-canvas-px 测试钩子留库】：2.40MP avg7.53ms/CPU80% → 3.38MP 7.61/84% → 4.81MP 7.85/81% → 全屏 6.40MP 7.8/98%。稳态帧时间几乎不随画布像素涨（+2.7×仅+4%）；真实成本=CPU 单核，全屏逼近满载。当前 3.28MP 落在免费区间

> 演化史 ❌已废弃：①"相机+窗口双跟随"增量体制（2026-08-25 首版）——三处结构性 bug：相机累积位移=角度变化、多状态链累积=漂移、双环反馈=拖拽恒半速。②**根绝对补偿+窗口跟随编排位移**（2026-08-26 二版，曾标记"定案"）——归零依赖"混合回待机恰好回到锚点捕获相位"，但待机是带骨盆微动的循环相位永不对齐；拖拽打断动作时基准根=中途补偿值；落地反应混合中途重锚——每次偏差成永久残差烤进下一锚点=随机游走漂出窗（用户报"动作越多越漂，最终不可见"+待机时窗口跟微摆），同日废弃。③全屏覆盖体制——"帧率 165→75 腰斩"结论经重测**不成立**（vsync=1 时代产物，vsync=2 预算下全屏稳态 avg 仅 7.8ms）；真实废弃理由=单核 CPU 98% 逼近满载+全屏遮蔽交互。
>
> 业界机制参考【源码核验 2026-08-26】：VPet=固定 500×500 画布+走路才移窗+出界仅打日志；eSheep=窗恒等帧尺寸+窗位即宠位+离场逐帧裁窗；Desktop Mate=大余量跟随+随缩放 resize（其 resize 抖动/贴边 disjoint 病灶恰被本项目固定窗/虚拟屏钳制设计根治）。**先网检拿一手信源再设计**（源码>失效泄漏证据>GDC>社区逆向），本节即该方法论的第一次全胜实证。

### 3.2 渲染风格 v8【目检，2026-08-26 定妆】

**纯官方贴图 × 原神式基色暗化描边**：

- 光照全关：`_ShadowStrength=0`/`_RimStrength=0`/SDF 关（桌宠小窗无环境光，官方贴图自带烘焙渐变即"原神味"；ramp 调子在小窗读作脏色/发绿，是负资产——勿再主动重开）。参数全留接线，Inspector 可调回
- 描边：宽 0.2/0.18/0.15（身体/头发/披风，脸恒 0——脸轮廓由头发承担，眉毛/眼球浮层几何无法与外轮廓区分）+ 距离/缩放补偿（近大远小屏宽恒定+滚轮缩放等比）
- **描边色=基色暗化**【原神逆向实证】：`_OutlineColorMode=1` 时描边=该处主贴图像素×`_OutlineTint(0.5,0.45,0.4)` 逐通道（白衣→冷灰、肤色→暖棕、深发→深发色，随固有色变化）；mode=0 回退固定色。原神本尊即此做法（"outline is everywhere a different color depending on the base color"），非纯黑非固定色
- 未接：原神 ilm/Lightmap **A 通道逐区域描边宽度**（主轮廓粗关节细——本项目网格无 UV1 lightmap，每材质统一宽度）；ILM 其余通道/边缘光/发影
- GI 贴图考古铁律：**Diffuse UV 与光照 UV（UV1）是两套空间**，Lightmap/ILM 类贴图不能按像素直接叠加到 Diffuse（披风星点即此坑，现为合成贴图方案）

### 3.3 桌面阴影【目检】

剪影→高斯模糊（17-tap×2 轮）→偏移合成 Quad 三步管线（CSS drop-shadow / DWM 窗口阴影同款）：影子壳共享骨骼姿势逐帧同步、专属层独立相机渲 RT、合成 Pass `Blend SrcAlpha OneMinusSrcAlpha, One Zero`（alpha 直写防平方）。内部 shader 必须序列化 Material 引用（Shader.Find 构建期被裁）。参数 Inspector 中文可调。

## 4. 行为层（现状）

- **待机循环** + 事件动作（`PetBehaviorController`）：光标停留 1.2s（包围盒屏幕矩形扩 90px，冷却 45s）→ Greet；久待机随机小动作（Nod/ShakeHead/Sneer/Clap/Show_1-4，间隔 25-55s）
- **出场/退场**：进程启动播 Appear；双击=播 Disappear 后才 Quit（`请求退场(Action)` API）；仪式期间视线+眨眼静默
- **视线跟随**（`PetLookAtController`）：颈(35%)→頭→眼球分层叠加，每帧基线重建（防复利累积自转——曾"只剩眼白"根因）【实证】。进入静默（抓起/仪式）=权重混合淡出：输出 `Slerp(动画姿态, 平滑值, w)`，停写时输出恒等于动画姿态（结构性无跳变，2026-08-27 v2）
- **动作切换（2026-08-27，GoW4 惯性化）**：硬切新 clip + PetInertializer 后处理（五次多项式 C2 衰减，速度承接任意切点无缝），替代 CrossFade 线性混合。**幅度自适应**：偏移<10° 全额拉引+0.6s 慢混（防起步平台期）；≥60° 拉引×0.3+0.3s 快切（防大姿态差"悬一下"/瞬移）；诊断日志留库（打印诊断）。切换手感整体状态见 §8 遗留
- **动作切换统一入口**（`PetAnimSwapper.Play/PlayOnce`）+ 启动全 clip 预热（legacy 首播冷初始化 150ms 掉帧串→挪到启动帧）
- **命中网格=一次性补烘（2026-08-27）**：周期性重烘全删（重 cook 15-22ms 尖峰=切换停顿主凶，219 条掉帧中 214 条与烘焙同帧实证）；只在交互结束 2s 后补烘一次，连续交互顺延；cookingOptions=UseFastMidphase

## 5. 进程架构（现状）

- 同 exe 双形态：`gic.exe`（主游戏）+ `gic.exe --pet-mode`（桌宠）；`PetMode` 静态开关，编辑器恒 false
- Boot 的 GameScene.Awake 与 Mirror 网络组件 Awake 均有 PetMode 守卫
- 主进程拉起桌宠（PetProcessLauncher，`UNITY_STANDALONE_WIN` 守卫）；Mutex 单例
- **同 exe 双进程约束（新功能必读）**：①共享 Screenmanager 注册表——主进程必须显式 SetResolution（SettingsApplier.ApplyDefaultFullscreen），桌宠永不碰主存档（独立 pet.json）、永不触发存档写盘
- 构建走 `Tools/桌宠/构建 Windows 桌宠测试包`（构建判据与竞态守卫见 gic-pet skill——**只信 Editor.log RESULT 行**）
- 编辑器联调：`Tools/桌宠/Play 时自动拉起派蒙`（编辑器 Play 自动拉起上次构建产物）

## 6. 未实现设计（落地前必读）

### 6.1 拖拽与拎起（已实现，2026-08-27 更新：刚体跟随+四肢摆动+轻微挣扎+放下反应）

**现状【目检】**：拖拽=**身体刚体 1:1 直跟光标**（eSheep 直移语义：骨盆目标=抓取时骨盆位+光标位移，零摆动零滞后）+**四肢跟拍摆动**+**轻微挣扎（2026-08-27 用户要求，推翻 2026-08-26"只有四肢摆动"旧拍板）**+**松手即停**（骨盆冻结原地、四肢弹簧 ~0.5s 归零，无飞行/弹跳/落地反应）+**放下反应（2026-08-27 目检通过，commit 8e2a74e）**。窗口按"骨盆客户区投影钉物理目标位"定位（根平移不动，仅根旋转=拎起姿势基准）。

**放下反应（按时长三档，2026-08-27）**：物理收尾（松手 0.5s 归位）结束后按本次拖拽时长分档——**<3s 无反应**直接回待机（快速挪位置不打扰）；**≥3s 害羞**（Shy01AS+害羞表情）；**≥7.5s 生气**（Anger+生气表情，用户拍板"拖太久就生气"+阈值砍半）。PetDragPhysicsController 记录拖拽时长（拖拽期实时/松手冻结），PetBehaviorController 物理结束边沿分档播单次（播完自然回待机）。阈值/动作名 Inspector 中文可调（清空动作名=跳过该档）。

**四肢摆动（PetDragPhysicsController）**：4 条欠阻尼二阶角弹簧（左右臂/左右腿），目标角∝光标速度低通值（τ=50ms）——水平分量=惯性滞后（往右拖四肢向左甩）+竖直分量=外展（下拖张开/上提收拢，张开上限 10°、收拢再×0.35 防穿模）；臂腿增益/频率微差防同步机械感。应用层=PetWindowController.LateUpdate 以**世界 Z 轴**（屏幕平面法线）旋转叠加到 `Bip001 L/R UpperArm`+`L/R Thigh`（Animation 每帧重写姿势后叠加，不累积）。手感参数全在组件 Inspector 中文自调（摆动增益/外展增益/摆动频率/摆动阻尼比/外展上限）。

**挣扎（2026-08-27）**：全身轻微摆动=侧摆±2.5°（绕骨盆钟摆，头/四肢随反侧摆）+扭动±4°（绕竖直轴肩部拧，第三频率×1.37 与主次级错开）+四肢反相扑腾±6°（左右肢镜像开合，叠加进取四肢摆动输出）；两频叠加（次级占比 0.53）防机械摆锤感；包络渐入 0.4s/收尾随剩余时间线性衰减/相位全程连续（收尾中被再抓无跳变）；应用层=应用物理帧把挣扎摆/扭叠加进根旋转（绕骨盆枢轴补偿已有，骨盆恒钉物理目标位窗口稳定）。参数 Inspector 中文可调（挣扎摆幅/挣扎扭幅/挣扎频率/挣扎次级频率占比/挣扎渐入秒/四肢挣扎摆幅，单项 0=关闭）。

**标定教训【实证】**：①`Transform.rotation` 本就是**世界旋转**（localRotation 才是父系量）——直接前置乘 `AngleAxis(角, 世界轴)` 即正确；曾误当局部量做"父系共轭换算"（pw⁻¹·R·pw），反而把每肢旋转轴劫持到各自父骨局部 Z（左右镜像骨互反→"左右手反向摆"根因），已回退。②世界 Z 轴负角实测=四肢向屏幕**右**甩（与直觉相反）——方向符号按目检标定写死在物理组件（滞后=+vx），调方向只动那两处符号。③四肢摆幅的"美观度"参数（外展上限 10°）与"活感"参数（摆动增益/阻尼比）分离调。

**拎起动画 v4+v6（Drag01 专用 clip）**：`Ani_NPC_Kanban_Paimon_Drag01.anim`（GI/Animations，从 Standby 派生）：躯干 4 骨 C 型塌陷（Spine25°/Spine1 50°/Neck45°/Head55° 链式前屈=含胸低头脸朝观众）+四肢 12 骨贴身垂落共 16 条常量曲线；披风/头发/发饰/手指保留 Standby 微动。姿势基准=拎起转身角 45°（3/4 偏左）×横躺角 0（均 Inspector 可调，收尾平滑归零）。表情=Confuse 直发情绪层。生成链=PetDragPoseMeasure（Tilt 常量）→ generate_drag_clip.cjs → ImportAsset+同步工具。**Q 版骨架实证：Spine/Spine1 前倾 25/50 起步才出塌陷；Neck/Head≤55°，超过变"头转朝地面"俯卧式。**

**沿革**：v1 程序化垂落叠加层（PetDanglePoseController，弃用留库）→ v2 仓鼠式横躺 → v3 直立垂落 → v4 瘫软塌陷+松手飞行弹跳 → v5/v6 转身角演化 → **终案（2026-08-26 深夜）**：用户反馈"太像果冻"→删挣扎/飞行/落地反应；"没抓住斗篷"→抓点钉斗篷（一版）；"不要任何摆动，只摆四肢"→刚体直跟+四肢跟拍弹簧（斗篷锚点随钟摆一并移除）。业界共通式（VPet/eSheep/Shimeji 源码核验：垂挂瘫软姿势+低频次要动画+硬切过渡）中我们保留垂落 clip+微动，摆动改由四肢弹簧承担。

### 6.2 边缘坐（坐窗口顶边/底边/任务栏横框）（已实现，2026-08-27 目检通过；2026-08-28 补窗口底边）

**机制（`PetEdgeSitController`，挂 PetWindow 物体）**：拖拽松手（无放下反应档）时磁吸探测附近"横框"——**窗口顶边**（DWM 可见帧 `DWMWA_EXTENDED_FRAME_BOUNDS`，非 GetWindowRect——后者含 Win10/11 不可见阴影边 ~7px 会悬空）、**窗口底边**（2026-08-28 用户要求补：坐下沿=身体在窗前、腿垂窗下，骨盆锚 `r.Bottom`，跟随/缩放重坐按锚定边取 Top/Bottom）与**任务栏顶**（工作区底）→ 磁吸贴合播直坐姿 SitUpright。全体横框最近者胜。掉落跨越检测仍只落顶边（物理下落只能落在上方表面——从上往下跨过某窗底边前必先跨过同窗顶边）。

- **磁吸窗**：上 120px/下 60px（≈标题栏高度）/水平容差 160px（物理像素，场景序列化值可调）。采用 VPet 式磁吸而非 Shimeji 全局掉落——不推翻既有"松手即停"拍板，其余位置松手行为零变化
- **判定/贴合基准=骨盆（屁股）**（`Bip001 Pelvis` 投影，非包围盒底脚线——目检纠正：脚线判定整条腿沉入窗下）。落座后 `坐定贴正秒=1.2s` 逐帧钉骨盆（站→坐姿势过渡平滑"坐进去"）
- **窗口跟随**（eSheep FollowWindow 同款）：垂直恒贴顶边，水平按锚定比例随窗口宽度缩放；窗口最小化/关闭/隐藏/DWM cloaked → **重力掉落**（2800px/s²，途中更低窗口顶边可接住，任务栏顶兜底），落到哪坐哪
- **坐定中纯坐**（用户拍板）：打招呼/随机小动作全压制；情绪映射无匹配=默认脸（SitLoop→Sleepy 已清空——"生病坐姿"的困困脸不再随坐姿出现）
- **缩放露馅修正**（用户拍板方案）：坐姿下滚轮缩放=模型绕脚底长高而窗口不动→屁股浮离窗沿；处理=切回站立待机→缩放稳定 0.3s 后按锚点重新落座（锚定窗口消失则磁吸探测兜底）
- **窗口枚举过滤**=eSheep FallDetect 标准（可见+标题非空+非自身）+现代补充（最小化/WS_EX_TOOLWINDOW/cloaked/最小尺寸 240×160）；0.15s 重枚举节流，坐定期失效检测 0.5s 节流
- 一手源码参照：eSheep FormPet.cs（Adrianotiger/desktopPet，FallDetect/FollowWindow 语义），源码快照 .codely-cli/tmp/esheep_formpet.cs

**坐姿动画 SitUpright（合成）**：SitLoop（原版）是"生病坐姿"——骨盆前倾 ~65°+头耷拉 ~38°+手臂前抱（含胸驼背读感）。SitUpright=Standby 直立上身（抬头+中性脸+idle 微动，重采样压缩 2.68→2.02s）+SitLoop 腿（前伸下垂）与手势（上臂世界取向 Δ 补偿：父链换成直立躯干后保扶膝手势观感，补偿链=根⊗骨盆⊗脊⊗脊1⊗锁骨——漏锁骨差 7°）+骨盆高度不变（0.214）。**合成数学：换某骨父链后保该骨世界取向 = q' = newChain⁻¹ ⊗ oldChain ⊗ q，链必须逐骨完整（非交换，省略中间骨必留残差）**。

### 6.3 屏幕边缘探头（扒边框）——❌已砍（2026-08-27 用户拍板"不需要了"）

~~停靠/拖到屏幕边缘→身体出屏只露头和扒框双手→张望变体；拖回恢复。~~设计留档备查（若未来重开：只认屏幕边缘不做逐窗口）。

### 6.4 游戏画面内版派蒙（2026-08-28 落地，提交 1d54f3f，用户复测通过）

**定位**：派蒙渲染在主游戏进程内（游戏画面悬浮），替代桌面版进程——目标安卓（Win32 不适用）与想省内存/单进程的 Windows 玩家。桌面版与游戏内版二选一，设置"派蒙"栏目切换（存档字段 petForm），**热切换+启动分发均即时生效**（启动分发在 `Wargame.Start()` 之后——存档加载在 [PostConstruct]，Awake 期读到的是默认值，曾致"设了游戏内启动仍拉桌面版"）。

**渲染架构（v2 全屏画布，2026-08-28 两轮重构定案）**：
- **全屏 RT 画中画**：RT=屏幕尺寸×rt倍率（默认 1，1:1 像素），RawImage 铺满全屏；**模型根在 RT 相机视野内移动**（相机恒静置于 (0,10000,0) 附近）。与桌面版窗口体制同构对应：桌面=固定 750×825 窗+模型窗内演+窗口移动；游戏内=固定"窗"=整屏+模型移动。此前 750×825 小画布方案废弃——放大超过画布即截断（用户拍板"画布=整个游戏画面"）。
- **正交相机**【实证：用户复测"越拖到边缘角度越大"根治】：透视投影下模型偏离光轴即斜视畸变，桌面版模型恒居窗中心故无此问题；正交视线处处平行，屏内任何位置恒正对，且世界/屏幕像素比恒定（拖拽/缩放标尺不随位置漂移）。可见世界高按原透视 FOV 45.27@模型距离折算，屏幕占比与桌面版一致。
- **缩放**：基准 localScale×(825/1080) 折算（保持屏幕显示尺寸=桌面版 750×825 逻辑窗口）；上限动态钳 `Min(缩放最大, 0.95×视野高/模型高)`（对齐桌面版"工作区 95%"语义——放大到最大恰好不截屏）。
- **交互全轮询**（legacy Input，对齐桌面版 Win32 轮询语义）：命中=光标→RT 视口→射线 vs 烘焙蒙皮碰撞体；**事件挡板**=全屏透明 Image，命中派蒙时开 raycastTarget 吃掉主游戏点击、未命中穿透（桌面版 WS_EX_TRANSPARENT 穿透切换的同构）。分辨率变化自动重建 RT+骨盆归一化位重摆。

**复用边界（2026-08-28 共用化重构后）**：待机轮换/打招呼/出场/拖拽拎起（Drag01+转身 45°+骨盆钉位+四肢摆动+挣扎+慌张表情+放下分档反应）/视线/表情/眨眼/缩放/防丢拉回——行为层/视线层经 IPetHost 宿主分发接入；**拎起姿势应用层合一进共用基类 `PetHostBase`**（拎起姿势角帧=角平滑+根旋转+挣扎+绕骨盆枢轴补偿；四肢摆动应用帧；命中网格一次性补烘+烘焙完成后钩子；缩放平滑；找骨/找本体蒙皮；共享序列化字段=拖拽物理/骨名/拎起角/烘焙宽限/缩放参数——Unity 按字段名序列化，移入基类同名保留，场景/Prefab 引用实测存活）：桌面版靠移窗钉骨盆（override 收口=旋转+位置精确还原基准），游戏内版移根（基类默认收口=骨盆枢轴补偿归位保留拖拽位置），视觉等价【实证：转身 44.9°/骨盆漂移 1.9% 屏高】。PetBehaviorController/PetLookAtController 的 窗口控制器 字段类型放宽为 PetHostBase（场景引用按名保留）。边坐等 Win32 窗口交互天然没有（游戏内为屏幕底磁吸坐的简化等价）。

**2026-08-28 三修复**（用户报障）：①游戏内拖拽松手后保持被拖拽姿势——行为层 物理结束帧 原 `宿主 is PetInGameHostController` 空分支漏了回待机（Drag01 循环永不切回），删空分支合一为"未被边坐/屏幕坐接管→回当前待机动作"；②游戏内热切换退场播完后仍停留数秒（且会"站起来"）——等待条件从 behavior.enabled（永真，空等 4s 超时）改为退场完成回调标志（播完当帧销毁），行为层加 _退场完成 冻结保持末帧，宿主按 退场中 冻结拖拽/滚轮；③桌面版边坐补窗口底边（见 §6.2）。

**游戏内屏幕坐参数对齐桌面版（2026-08-28 二轮，用户目检"磁吸范围太大+坐得太低"）**：坐线=屏底上方 4.5% 归一化（≈任务栏顶高度——任务栏 48px@96dpi 随 DPI 等比≈4.4% 屏高，任意 DPI 通用；旧 0.02 目检沉进屏底）；磁吸窗=坐线上 120px/下 60px **像素语义**（与桌面版同参；旧 0.15 归一化在 4K 屏≈324px 太大）；落座后 坐定贴正秒=1.2s 逐帧钉骨盆到坐线（桌面版同款"坐进去"观感，旧版一次性摆位无贴正）；拖拽时长 0.3s 门槛删除（桌面版无此门槛，同语义）。坐定中缩放重坐（桌面版防屁股浮离窗沿机制）游戏内版暂未移植——已知差异。**陷阱（三版实测）**：Unity 视口/屏幕 y=0 是屏底（y=1 顶部）——磁吸窗判定曾误按"y=1 为底"写 `1-偏移`，窗口落到屏幕顶部=拖底不坐/拖顶瞬移坐（用户实测报障）；任何屏幕归一化坐标判定先对齐 y 轴方向。

**渲染坑（实证）**：①ScreenBlurRendererFeature 必须跳过 RT 相机（`targetTexture==null` 才跑）——否则派蒙相机输出（模型+透明黑背景）模糊后覆盖全局 `_ScreenBlurTex`，UIBlur 毛玻璃面板全黑；②任何给全局 Shader 纹理赋值的 RendererFeature 都要按相机过滤，RT 相机是隐形写者。

**持久化**：PetPrefs 统一 pet.json v2（桌面/游戏内字段共存，游戏内位置=骨盆归一化屏幕位）；形态选择/连带关闭留在主存档（设置类）。**已知边界**：游戏失焦时 Input 冻结=拖拽/视线停住（游戏内形态天然随游戏窗口，桌面版走 Win32 全局轮询无此限）。

**设置栏目**：左侧导航"派蒙"栏（第 5 栏）："派蒙形态"下拉（桌面版仅 Windows 可选/游戏画面内版，安卓恒游戏内版）+"游戏退出时关闭桌面版派蒙"（文案 2026-08-28 补"桌面版"五语言；游戏内形态天然随进程销毁）。

### 6.5 对话功能（LLM）

**现状（2026-08-29 二轮：流式修复+多供应商，构建 OK 待用户复测）**。选型结论【2026-08-28 网检拍板】=方案A 自研薄层（2026-08-29 已多供应商化）：**UnityWebRequest + DownloadHandlerScript** 直连 OpenAI 兼容端点（**2026-08-29 替换 HttpClient——Mono 的 HttpContent.ReadAsStreamAsync 沿 .NET Framework 血统整体缓冲响应（MS 文档：Task 在全部内容读完后才完成），SSE 增量无法逐块送达=用户实测"整局卡住→一次性出完整回复"；DownloadHandlerScript.ReceiveData 在主线程逐块送达，官方文档明示**）。SSE 三通道解析 reasoning_content/content/tool_calls 分片拼接；超时=手工看门狗（首字节 超时秒/流中 90s 无数据→Abort；UnityWebRequest.timeout 会腰斩长流式回复，恒 0）。拒绝 SK/MAF/MEAI/LangChain.NET——net8+/System.Text.Json 与 Unity .NET Standard 2.1+IL2CPP 冲突无先例。记忆=memory_update 工具（Mem0 式）+稳定人设前缀吃缓存，不上向量库；Spring AI（Java sidecar）已否决（架构错配）——但其"选供应商+填自己的 key"体验已用**供应商注册表**复刻（见下）。

**多供应商（2026-08-29，PetChatProviders.cs 注册表）**：DeepSeek(deepseek-v4-flash)/Kimi(kimi-k3)/智谱GLM(glm-4.7)/通义千问(qwen-plus)/OpenAI(gpt-5.4-mini)——五家全部 OpenAI Chat Completions 兼容，仅 baseUrl+模型名不同，端点与模型名 2026-08-29 官方文档网检确认。**表下标=存档值（PlayerSaveData.petChatProvider / pet.json chatProvider），只增不删不重排**；设置「派蒙→对话模型供应商」下拉（PetProvider_* 五语言键），切换供应商清空旧 key 引导重填（旧 key 对新家无效，防静默 401）；OpenAI 不发 temperature（gpt-5 系只支持默认值 1，发 0.8 直接 400）。Inspector 端点/模型**覆盖**字段留空=按供应商表（字段名 endpointOverride/modelOverride，旧 prefab 序列化的端点/模型名已随改名废弃——防它们冒充覆盖钉死 DeepSeek）。

**三组件架构（Assets/_Scripts/Pet/Chat/）**：`PetChatClient`（薄层：UnityWebRequest SSE+FC JSON，2026-08-29 由 DeepSeekClient 更名，传输层/密钥链路/供应商解析）+ `PetChatProviders`（供应商注册表）→ `PaimonChatSession`（人设 system prompt 稳定前缀+滑窗历史 历史轮数×4 条+长期记忆 JSON 持久化+工具循环≤3 层）→ `PetChatUIController`（程序化 UI：打字机+淡出+InputLocks 压锁，挂 prefab 根经 宿主接线(宿主画布) 注入 Canvas）。密钥链路（2026-08-29 重做，根治"重启后设置里有 key 对话报未设置"）：设置「派蒙→对话 API Key」输入 → PetApiKeyCrypto AES-128-CBC 加密 → 主存档 petApiKeyCipher + **PetPrefs.WriteChatCipher 磁盘读改写**（旧走 PetPrefs.Save() 有两雷：编辑器恒跳过=密文从未落盘、跨进程陈旧缓存整体覆写会抹密文）；客户端每请求 **PetPrefs.ReadChatCipher 直读磁盘**（绕进程缓存：改 key 即生效+跨进程新鲜），兜底供应商环境变量。Save()（易变状态路径）落盘前设置类字段一律取磁盘现值——防桌面进程陈旧缓存抹 key。

**布局（2026-08-29 重构：聊天 UI 全部跟随模型，输入条不再钉屏底）**：回复气泡锚派蒙头顶（头部锚点提供器，每帧跟随）；输入条挂模型脚底下方（底部锚点提供器=烘焙碰撞体包围盒底中心），脚底放不下（贴屏底/坐任务栏）时自动翻转到气泡上方——两元素都不遮模型。坐标约定：聊天 Canvas=ConstantPixelSize 屏幕像素系，锚点提供器返回画布绝对坐标，气泡/输入条均用左下锚+anchoredPosition 绝对摆位（气泡 pivot 底中心、输入条 pivot 顶中心）。两形态共用：宿主各自注入两个锚点提供器即可，组件不关心窗口/RT 相机差异。

**事件接线约定（2026-08-29 事故实证）**：客户端流式事件是 public Action 字段（正文增量/思考增量/工具调用/完成/错误）非 event——UI 层先接 正文增量/错误，会话层再 += 自己的 工具调用/完成/错误 处理器；**任何一方只 -= 摘自己持引用的处理器，禁止整体置 null**（整体置 null 会抹掉另一方的处理器=流式正文与错误全静默）。

**开发 key（2026-08-30 拍板：开发时用固定 key，导出绝不写入存档）**：`PetApiKeyCrypto.DevKey`（sk- 开头常量，**声明整体在 `#if UNITY_EDITOR` 内——任何构建产物不含此字面量**）。消费链=SaveManager.CreateNewSave（新档初始化）→ InitDefault 预填加密 key 到 petApiKeyCipher + 同步 WriteChatCipher 落 pet.json（客户端每请求直读 pet.json，只写主存档不写它=编辑器聊天仍报无 key）；构建产物两条代码路径均不存在，导出后玩家存档初始化恒为空 key（玩家自输自己的 key，商业边界不变）。编辑器与构建共享 persistentDataPath+同机设备指纹=密文互通（编辑器设的 key 构建版可用）。**注意：key 已进 git 历史——仓库公开/协作前先去供应商后台吊销换新。**

**Chat 代码命名规范（2026-08-29 用户要求统一后执行，规则本体已收拢进 docs/20 §1.1）**：类名/Unity 生命周期方法英文；[SerializeField] 字段中文；其余成员中文（❌已推翻：同日晚用户改拍板"统一用英文，除非不得不用中文"——标识符统一英文，中文例外仅 [SerializeField] Inspector 字段，以 docs/20 §1.1 为准）；例外=线上协议 DTO 的序列化成员保持英文。序列化字段改名挂 [FormerlySerializedAs]（prefab 值已实测存活+固化）。

**发送按钮（2026-08-29 用户拍板"不符合原神风格（不简约）+去掉文字只留纸飞机图案"）**：素材重生成（SeeDream ui_button+minimalist：暖白奶油圆角方底+深棕描边+深棕纸飞机平面图标，与 pet_chat_bubble_9slice 气泡同色族）+ 泛洪去白底/裁剪/256² 重采样后原地覆盖 `Assets/Art/PaimonPet/ChatUI/pet_send_button.png`（保 GUID=保 prefab 接线）；按钮 150×44→44×44 图标按钮，输入条 Field 右内缩 -170→-64；Label 子物体（TMP+TextCombiner"发送"静态标签）已删——PetChatSend 本地化键随之移除。

**二轮修正（2026-08-29 晚，用户复测六问题）**：①**物理收尾不受聊天输入门控**（帧序铁律，两形态同改）：旧版输入期整体早退=收尾轮询停摆→`dragPhysics.IsActive` 永真→行为层卡 dragPhysicsPhase（Drag01 循环+视线静默）=**永久拎起姿势+头不跟踪鼠标**（两症状同根）；现物理块恒先推进，聊天门控只拦新拖拽/单击判定。②气泡常驻：删除 停留/淡出 协程与 字段（bubbleHoldSec/fadeOutSec 已删）——气泡显示到下一条回复覆盖或显式关闭。③外点关闭：新增 `CloseChat()`（收输入条+藏气泡）+`IsPointOnBubble()`；两形态输入期点击"输入条/气泡以外"任何地方（含穿透到游戏/其它应用的点击）=关闭对话；ESC 同款。④**桌面版补齐**：三组件挂 PaimonPet.unity 的 PetWindow 物体（场景接线素材/字体/会话），PetWindowController.接聊天()（Start 末，try-catch 防泄漏）运行时建 ScreenSpaceOverlay Canvas(ConstantPixelSize)+EventSystem（场景自带的是 PetEditorOnly 构建自禁用）+锚点提供器（相机屏幕位直通，无 RT 换算）；交互：单击派蒙（无位移松手）→**过 0.4s 双击窗口才开对话**（不与双击退出互抢）+按下待定阈值（0.15s/8px，单击零姿势零物理，移植游戏内）；穿透帧扩展——输入期光标在输入条/气泡上=非穿透（uGUI 吃点击）。⑤设置派蒙栏分组：形态/连带关闭 | 供应商/key（LLM 相邻底部）。⑥本地化事故修复：v2 脚本 RemapId 静默失败（目标 id 占用）后仍按旧 targetId 写值，误覆盖 PetChatPlaceholder/NoKey/Busy/NotWired 四键为供应商名（"输入条显示输入供应商"根因）——已按 git 原值恢复，教训记 gic-localization skill。

**首批指令·时间查询与设置（2026-08-29 落地）**：`Assets/_Scripts/Pet/Chat/PetChatIntent.cs`（Intent 层：工具定义+本地执行）。**游戏内形态**：`PetInGameHostController.接聊天` 直调注册（主进程内直接执行）。**桌面形态（2026-08-30 补齐）**：`PetWindowController.接聊天` 注册同款工具，执行走 **PetIntentIpc 文件通道**（persistentDataPath 下 pet_intent_req/resp.json：桌面写请求[Guid id+Unix 秒]→主进程 PetIntentIpcHost 250ms 轮询消费→执行 PetChatIntent.Execute→回写响应→桌面侧同步等[id 匹配防陈旧响应，超时 1.5s]）——独立进程无主进程引用，文件通道是聊天指令低频场景的轻量过渡（正式 IPC 仍是 §8 遗留）；主游戏未运行=超时报"主游戏没有在运行"（游戏时间是主进程状态，语义正确）；编辑器与构建共享 persistentDataPath=开发时编辑器 Play 可直接联调桌宠 exe 指令。两工具：①`set_game_time`（op=set/reset + hour/minute）——TimeUtility 加游戏时间偏移（`Now`/`SetGameTime`/`ResetToSystemTime`；**每次游戏启动默认=系统时间**：偏移是进程内静态不持久化，重启自然重置）；变更广播 `OnGameTimeChangedEvent`（LocalEvents.cs）→ MainHallScreen 重载背景（同时段同地址自动跳过）+ PositionManager 时段变了才换曲（同时段调时间不打断当前曲）。②`get_game_time`——查询游戏内时间+时段+来源。~~③`open_screen` 界面切换~~——**2026-08-30 用户拍板移除**（"没有必要"：手动切换足够快）；曾有两版实现，教训仍有效留档：直接 `Load()` 不藏大厅按钮=按钮透出叠穿（WishScreen 主 Canvas 是 ScreenSpaceCamera，大厅 Overlay Canvas 永远渲染其上）且可交互；AI 切界面要么与手动完全同路径（ExitToSceneAsync），要么隐藏态必须与手动退场动画结束态一致。剩余指令（帮我抽卡）待做；语音期 TTS 可用项目内 AudioManager.PlayVoice（可 voice_clone）。注：团结提供的 GLM 是编辑器侧工具不提供运行时 API，运行时要接用智谱 BigModel 开放平台。

## 7. 决策记录

| 日期 | 决策 |
|------|------|
| 2026-08-21 | 桌面置顶一期直接做（不做"先仅游戏内"妥协）；拉起不绑命生命周期；DWM 逐像素 alpha 为主流方案；MMD 模型为基底；战斗/联机表现不考虑 |
| 2026-08-22 | 方案 B 重定向管线定案（动画来源=GI 提取+重定向）；表情层运行时驱动定案 |
| 2026-08-24 | GI 官方模型落地（models-resource rip），替代 MMD 为现行模型；**模之屋 MMD 降为兜底** |
| 2026-08-24 | 摆手（Greet）残余卡顿用户拍板放弃修复，勿再主动排查 |
| 2026-08-24 | 帧节奏 vSyncCount=1 满刷（0+限帧 30 在高刷屏顿挫→2→1 演化定案） |
| 2026-08-25 | 全屏覆盖体制废弃（帧率腰斩）；渲染 v7 光照全关（用户拍板"不需要阴影"） |
| 2026-08-25 | 渲染材质槽定案：Body 网格 2 槽=[身体\|头发]，必须 sharedMaterials 按槽赋值 |
| 2026-08-26 | 窗口体制定案：相机冻结+根绝对补偿+拖拽直定位（照 VPet/eSheep 源码重写，三 bug 根治）❌同日被三次重构推翻——绝对补偿版自身随机游走漂移（见 §3.1 演化史②） |
| 2026-08-26 | 描边 v8：原神式基色暗化（网检逆向实证，固定暖棕废弃）；**确立"先网检拿一手信源再设计"方法论** |
| 2026-08-26 | 拎起动画定案：网检主流共通式（垂挂瘫软+低频次要动画+松手必有下文）做 v3 直立垂落；用户给参考图后改 v4 瘫软塌陷（C 型前屈+贴身四肢），仓鼠式横躺与程序化垂落层废弃 |
| 2026-08-26 | **窗口体制 v3 固定画布终案**（目检通过）：动画期间窗口/根完全静止，窗口唯一移动源=拖拽物理；画布加宽 550→750；性能矩阵实测推翻"全屏腰斩"旧结论（vsync=1 时代产物），画布像素对稳态帧时间几乎无感、真实成本=CPU 单核 |
| 2026-08-26 | 拖拽物理钟摆+挣扎+飞行弹跳上线后经三轮用户目检全部否决（"太像果冻"/"没抓住斗篷"/"松手不需要飞出去"），同日深夜终案=**刚体 1:1 直跟+四肢跟拍弹簧+松手即停**；方向符号按目检标定（世界 Z 负角=四肢向右甩，直觉相反）；`Transform.rotation` 即世界旋转误做父系共轭是"左右手反向摆"根因（已回退） |
| 2026-08-26 | **构建异步协议落地**：PetSpikeBuildTool 新增 ScheduleBuild（毫秒级返回+排队守卫），根治 BuildImmediate 同步阻塞桥→超时→自动重试→串行叠构建（三次实证需用户手动中断）；判建成=Editor.log RESULT 基线计数法后台轮询 |
| 2026-08-27 | **帧节奏 v4=DWM帧对齐终案**：分层窗口 blt present 不阻塞、vSyncCount 实际无效（vsync 时代四代方案全建立在错误假设上）；DwmFlush 每帧钉合成网格=唯一有效节拍器，165Hz 实测 99% 帧 6.06ms/163fps。惯性化（GoW4 五次多项式）替代 CrossFade |
| 2026-08-27 | **切换停顿/瞬移三轮实证根治**（commit 8f909b3）：①停顿主凶=命中网格周期重 cook 尖峰砸切换帧（219 HITCH/214 同帧）→烘焙一次性化+快烹饪；②大姿态差（抓起/松手 86-94°）被 0.6s 慢窗拖着走=瞬移/悬滞两极→幅度自适应（拉引倍率+大偏移窗口 0.3s 快切）；③视线静默淡出改权重混合输出（结构性无跳变，根治"转身完成瞬间头角度突变"）。新增拖拽挣扎全身摆动。**用户拍板"动作期间内不顺畅遗留不修，暂时不修改了"**——勿主动重开 |
| 2026-08-27 | 诊断方法论沉淀：切换手感问题先开 PetInertializer.打印诊断 看 Player.log 捕获行（偏移/速度/衰减采样客观数据）再动手，不盲调参；PetFingerPoseController 确认为 GI 模型上的死代码（日文骨名全 null） |
| 2026-08-27 | **拖拽放下按时长分档反应**（目检通过，8e2a74e）：<3s 无反应/≥3s 害羞/≥7.5s 生气（用户拍板阈值砍半）。附带教训：改 Inspector 字段代码默认值必须同步写场景序列化值（场景值恒优先，只改代码=白改，两轮构建实证） |
| 2026-08-27 | **边缘坐定案**（目检通过）：磁吸窗（非全局掉落，保"松手即停"）+骨盆基准（非脚线）+DWM 可见帧贴边（非 GetWindowRect）+窗口失效 eSheep 式重力掉落+坐定纯坐+缩放切站立重坐。坐姿=合成 SitUpright（Standby 上身+SitLoop 腿/手势），SitLoop 原版定性"生病坐姿"弃用。期间踩坑：GI .anim 的 path 是 YAML 折叠多行标量（续行 6 空格无冒号），行级解析须折行否则块塌缩；换父链保世界取向的 Δ 补偿链必须含全部中间骨（漏锁骨差 7°） |
| 2026-08-28 | **游戏内派蒙落地**（复测通过，1d54f3f）：全屏 RT 画布（小画布截断废弃）+正交相机（根治边缘斜视——桌面版模型恒居中无此问题，游戏内版模型移动必须正交）+交互全轮询+事件挡板（桌面穿透切换同构）；拎起姿势应用层复刻（桌面版移窗钉骨盆↔游戏内版移根，视觉等价）；ScreenBlur 特性跳过 RT 相机（RT 相机输出覆盖全局纹理=毛玻璃黑屏）；Pet 诊断日志门控到桌宠进程（组件随 prefab 跑进主游戏进程后刷主游戏控制台） |
| 2026-08-29 | **聊天二轮修复+多供应商**：①流式=HttpClient→UnityWebRequest+DownloadHandlerScript（Mono 的 ReadAsStreamAsync 整体缓冲=SSE 假流式，见 docs/14 §15）；②密文持久化重做（WriteChatCipher 磁盘读改写+编辑器也生效+ReadChatCipher 直读，见 docs/14 §16）；③多供应商注册表（Spring AI 式选供应商+填自己的 key，五家 OpenAI 兼容，表下标=存档值只增不删）；④发送按钮改纸飞机图标（44×44，去"发送"文字） |
| 2026-08-28 | **共用化重构+三修复**（用户需求"能共用的则共用"）：抽 `PetHostBase` 共用基类（拎起姿势应用/四肢摆动/命中烘焙/缩放平滑/找骨/共享序列化字段——字段按名迁移实测引用存活）；行为层/视线层宿主字段放宽 PetHostBase。修复：①游戏内松手保持被拖拽姿势（物理结束帧空分支漏回待机）②游戏内退场播完仍停留数秒（enabled 永真空等超时→退场完成回调当帧销毁+行为层冻结末帧+宿主冻结交互）③桌面版边坐补窗口底边（身体在窗前腿垂窗下，最近横框者胜；掉落仍只落顶边） |

## 8. 遗留与待办

- ~~边缘探头（原 §6.3）~~——2026-08-27 砍掉（用户拍板不需要）
- ~~游戏画面内版（§6.4）~~——2026-08-28 落地复测通过（详见 §6.4）
- 游戏内派蒙后续微调入口：rt倍率（画质）/最大屏高占比/拎起角/挡板 sortingOrder 全在 PaimonInGameRoot.prefab 的 PetInGameHostController Inspector
- 多显示器跨屏策略（边缘坐已支持多屏坐标，但跨屏拖拽体验未专门验证）
- 边缘坐磁吸手感（120/60/160px）与掉落重力（2800px/s²）为当前默认基线，观感微调走 PetEdgeSitController Inspector（场景序列化值）
- 拖拽四肢摆动/挣扎手感参数为当前默认基线，后续观感微调走 Inspector
- **动作期间内（in-action）不顺畅——用户拍板暂停（2026-08-27）**：切换层（停顿/瞬移/头跳变）已根治提交，但动作播放期间内仍有不顺畅感未修；重开时先开 PetInertializer.打印诊断 看捕获行 + PetFrameStats gcΔ/烘焙Δ/眨眼Δ 标记
- 测试面板按钮已同步 GI clip 名（103 个，含"拎起"）；PetLookAt 眼球骨与 GI 曲线叠加表现待复检
- ❌已推翻：动画卡顿封案（2026-08-24 拍板放弃）——2026-08-27 实证当年主凶之一就是烘焙尖峰，已根治（见 §7）；"放弃修复"结论不再适用
- ~~IPC（派蒙→主进程指令通道）~~——2026-08-30 聊天指令场景已用 PetIntentIpc 文件通道落地（§6.5 首批指令节）；更重的实时双向 IPC（如状态同步）仍无需求，按需再议
