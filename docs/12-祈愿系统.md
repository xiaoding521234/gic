# 祈愿系统

## 1. 概述

祈愿是玩家获取角色卡和物品卡的主要途径。采用**反应速度游戏**机制——卡道上的卡片随机滚动，玩家通过射击命中卡片来决定获得什么，而非预抽结果。玩家反应越快、越精准，越有可能命中高价值卡片。

## 2. 卡池配置

### 2.1 卡池结构

每个卡池由 `WishPoolConfig`（ScriptableObject）定义，包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| poolName | string | 卡池名称（如"风神卡池"） |
| units | List\<UnitName\> | 卡池包含的角色列表 |
| items | List\<ItemName\> | 卡池包含的物品列表 |
| star5Rate | float | 五星出卡概率（默认2%） |
| star4Rate | float | 四星出卡概率（默认8%） |
| star3Rate | float | 三星出卡概率（默认15%） |
| star2Rate | float | 二星出卡概率（默认30%） |
| star1Rate | float | 一星出卡概率（默认45%） |
| unitRate | float | 角色/物品比例（默认50%，即各一半） |

### 2.2 风神卡池（已实现）

- 配置文件：`Assets/Resources/Configs/WishPool_Anemo.asset`
- 角色（7）：温迪、琴、安柏、凯亚、芭芭拉、迪奥娜、班尼特
- 物品（5）：苹果、小麦、蒲公英酒、体力、摩拉
- 概率：2%/8%/15%/30%/45%（5★→1★），角色物品各50%

### 2.3 规划卡池

共7个卡池，按势力分类：

| 卡池 | 势力 | 状态 |
|------|------|------|
| 风神卡池 | 蒙德 | ✅ 已实现 |
| 岩神卡池 | 璃月 | 待实现 |
| 雷神卡池 | 稻妻 | 待实现 |
| 草神卡池 | 须弥 | 待实现 |
| 水神卡池 | 枫丹 | 待实现 |
| 火神卡池 | 纳塔 | 待实现 |
| 月神卡池 | 挪德卡莱 | 待实现 |

## 3. 货币与消耗

- **祈愿货币**：原石（ItemName.Primogem）
- **单次消耗**：160 原石
- **祈愿模式**：1次（消耗160）/ 10次（消耗1600）
- 初始原石：16000（新建存档）

## 4. 获取规则

- **角色卡**：获得 1 张
- **物品卡**：获得 1 份（数量 = `ItemData.countPerServing`）

| 物品 | 每份数量 |
|------|---------|
| 摩拉 | 5 |
| 苹果 | 1 |
| 小麦 | 1 |
| 蒲公英酒 | 1 |
| 体力 | 1 |
| 原石 | 5 |

## 5. 祈愿流程

### 5.1 整体流程

```
点击祈愿按钮 → 消耗原石 → 黑色背景遮屏 → 卡道开始滚动
→ 1.5秒倒计时（玩家射击或超时自动射击）→ 命中卡牌 + 特效
→ 0.6秒冷却 → 重复直到次数用完
→ 卡道隐藏 → 结果卡飞到屏幕中间横向展示 → 点击关闭
```

### 5.2 卡道机制

- 卡道位于屏幕中间竖向条带，卡片从底部移动至顶部
- 卡片**永远滚动**，射击和冷却期间不暂停
- 卡道上的卡片**随机生成**（`DrawRandomTrackCard`），与实际结果无关
- 每张卡按卡池概率随机选择星级和角色/物品
- 卡片紧密排列，无间隙（`cardSpawnInterval = 0.09s`，`cardMoveDuration = 0.75s`）
- 初始填满卡道（老虎机效果，一开始就有卡片在各位置移动）

### 5.3 射击机制

- 玩家点击鼠标左键射击（或1.5秒倒计时到期自动射击）
- 射击后发射"命运之线"：白色细线从屏幕右侧到左侧，0.2秒完成
- 命中卡道上**最接近屏幕中心的卡**
- **射中什么卡就获得什么卡**——读取该卡的 `Card.saveCardData` 作为实际结果
- 射击冷却：0.6秒（冷却期间卡道继续滚动，倒计时不计时）

### 5.4 倒计时

- 每次射击有1.5秒时间限制
- 屏幕底部居中显示横条倒计时（类似血条）
- 倒计时从两侧向中心缩短（`localScale.x` 缩放）
- 超时自动射击

## 6. 特效

### 6.1 光带特效

- 射中卡片后在**卡片上**播放 `CardLightBandEffect`（`Card.PlayLightBand()`）
- 默认金色光带从左下到右上扫过卡面
- 持续0.3秒

### 6.2 光柱特效

- 射中卡片后在**卡道中心**播放 `LightPillarEffect`
- 颜色 = `StarColor.GetStarColor(starLevel)`（1★绿/2★蓝/3★紫/4★橙/5★红）
- 光柱渲染在卡道**后面**（`SetSiblingIndex` 在 cardTrack 前），不遮挡卡片
- 包含：核心光束、多层光晕、地面闪光、双向飞溅粒子
- 粒子参数：数量30、速度1200、扩散1.5、时长0.8s
- 粒子持续飞行不停止，越接近终点越淡出
- 光柱上升0.5秒后调用 `Stop()` 淡出（0.5秒），总生命周期约1.1秒

## 7. 结果展示

### 7.1 射击后

1. 被射中的卡从卡道移除，`SetParent(resultContainer, true)` 保持世界位置
2. 在屏幕中心停留0.3秒（让玩家看清抽到了什么）
3. 飞到屏幕左侧竖向排列（从下到上）

### 7.2 全部抽完后

1. 卡道和倒计时隐藏
2. 左侧竖列的结果卡飞到屏幕中间，横向排列展示（`FlyToPosition`，缓出曲线）
3. 玩家点击任意位置后关闭祈愿，回到卡池界面

## 8. UI 结构

### 8.1 场景层级

所有祈愿UI在 `WishScreen` 场景的 `Canvas/WishDrawRoot` 下静态预设（非代码动态创建）：

```
Canvas (WishDrawController)
├── BackPanel
├── ColumbinaPanel / VentiPanel / ... (角色面板)
├── Ambience
├── Panel (TopPanel / LeftPanel / BottomPanel / RightPanel)
└── WishDrawRoot (初始隐藏)
    ├── Background (全屏黑色不透明)
    ├── CardTrack (竖向条带，无背景Image)
    │   ├── SpawnPoint (底部锚点)
    │   ├── EndPoint (顶部锚点)
    │   └── FateLineContainer (铺满卡道)
    ├── CountdownBar (底部居中横条)
    │   └── CountdownFill (居中缩放)
    ├── ResultContainer (屏幕左侧竖向条带)
    └── FinalDisplay (全屏 + CanvasGroup)
```

### 8.2 字段引用

`WishDrawController` 所有UI引用为 `[SerializeField]`，在场景中赋值：

| 字段 | 说明 |
|------|------|
| cardPrefab | Card.prefab（复用背包卡牌预制体） |
| drawRoot | WishDrawRoot（整体显隐） |
| cardTrack | 卡道容器 |
| cardSpawnPoint / cardEndPoint | 出卡点/终点 |
| fateLineContainer | 命运之线容器 |
| countdownBar / countdownBarFill | 倒计时条 |
| resultContainer | 结果卡容器（左侧） |
| finalDisplayContainer / finalDisplayCanvasGroup | 最终展示（全屏） |

### 8.3 参数

| 参数 | 值 | 说明 |
|------|:--:|------|
| cardMoveDuration | 0.75s | 卡片从底到顶的移动时间 |
| cardSpawnInterval | 0.09s | 卡片生成间隔 |
| cardWidth | 100 | 卡片宽度 |
| cardHeight | 150 | 卡片高度 |
| fateLineSpeed | 8000 | 命运之线速度（实际用固定0.2s） |
| clickTimeLimit | 1.5s | 每次射击时间限制 |
| shotCooldown | 0.6s | 射击冷却 |
| resultCardSize | 80 | 结果卡尺寸 |
| finalCardSize | 160 | 最终展示卡尺寸 |

> **注意**：`[SerializeField]` 的值在场景中序列化后，代码修改默认值不会生效。改参数时需同时更新场景中的序列化值。

## 9. 代码架构

### 9.1 核心类

| 类 | 文件 | 职责 |
|----|------|------|
| WishPoolConfig | Data/Wish/WishPoolConfig.cs | 卡池配置（ScriptableObject） |
| WishManager | UI/Screen/Wish/WishManager.cs | 货币消耗、写入存档、配置访问 |
| WishDrawController | UI/Screen/Wish/WishDrawController.cs | 全流程UI动画控制 |
| WishScreenDraw | UI/Screen/Wish/WishScreenDraw.cs | WishScreen 的祈愿入口（partial class） |
| WishTrackCard | (WishDrawController.cs 内) | 卡道上移动的单张卡牌 |
| LightPillarEffect | Tool/Component/LightPillarEffect.cs | 光柱爆发特效 |
| CardLightBandEffect | Battle/Card/CardLightBandEffect.cs | 卡片光带特效 |

### 9.2 关键设计决策

1. **反应速度游戏**：不预抽结果，玩家射中什么卡就获得什么卡。卡道卡随机生成，实际结果由 `Card.saveCardData` 读取并写入存档。

2. **卡道永远滚动**：射击和冷却期间卡道不暂停，保持视觉连贯性和紧张感。

3. **同一卡片飞行**：被射中的卡直接从卡道 `SetParent(resultContainer, true)` 转移到结果容器，保持世界位置不突变，先停留0.3s再飞到左侧。

4. **协程隔离**：`LightPillarEffect` 用 `_activeCoroutines` 列表管理自己的协程，`Stop()` 只停自己的协程不影响外部 `GlowBurstCoroutine`。粒子协程不追踪，由 `ClearSpawned` 销毁。

5. **Card.skipFadeIn**：卡道卡设置 `skipFadeIn = true` 跳过 `PlayFadeIn` 协程，避免对象池复用时 `OnEnable` 触发淡入导致半透明。

6. **性能优化**：
   - `LightPillarEffect` 的纹理和 Sprite 为 `static` 懒加载，所有实例共享，避免每次射击重建像素数据。
   - `FateLine` 的 Shader 缓存为 `static` 字段，避免每次射击 `Shader.Find`。
   - `StartWish` 时预构建星级→候选列表缓存（`Dictionary<int, List<UnitName/ItemName>>`），卡道出卡直接查表，不逐卡遍历全表。
   - 存盘改为抽卡流程结束后统一调用一次 `SaveGame()`，而非每张卡都存盘。
