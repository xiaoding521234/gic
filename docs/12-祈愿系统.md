# 祈愿系统

## 1. 概述

祈愿是玩家获取角色卡和物品卡的主要途径。采用**反应速度游戏**机制——卡道上的卡片随机滚动，玩家通过射击命中卡片来决定获得什么，而非预抽结果。玩家反应越快、越精准，越有可能命中高价值卡片。

## 2. 卡池配置

### 2.1 卡池结构

每个卡池由 `WishPoolConfig`（ScriptableObject）定义，包含：

| 字段 | 类型 | 说明 |
|------|------|------|
| poolName | string | 卡池名称（如"风神卡池"） |
| units | List\<UnitName\> | 卡池包含的角色列表 |
| items | List\<ItemName\> | 卡池包含的物品列表 |
| star5Weight | float | 五星出卡权重（默认1） |
| star4Weight | float | 四星出卡权重（默认4） |
| star3Weight | float | 三星出卡权重（默认10） |
| star2Weight | float | 二星出卡权重（默认30） |
| star1Weight | float | 一星出卡权重（默认55） |
| unitWeight | float | 角色权重（默认50） |
| itemWeight | float | 物品权重（默认50） |

### 2.2 风神卡池（已实现）

- 配置文件：`Assets/Resources/Configs/WishPool_Anemo.asset`
- 角色（7）：温迪、琴、安柏、凯亚、芭芭拉、迪奥娜、班尼特
- 物品（5）：苹果、小麦、蒲公英酒、体力、摩拉
- 权重：1/4/10/30/55（5★→1★），实际概率 = 权重 / 总权重（当前总和100，即1%/4%/10%/30%/55%）；角色物品权重各50（即各50%）

### 2.3 规划卡池

共7个卡池，按势力分类：

| 卡池 | 势力 | 状态 |
|------|------|------|
| 风神卡池 | 蒙德 | ✅ 已实现 |
| 岩神卡池 | 璃月 | 待实现 |
| 雷神卡池 | 稻妻 | 待实现 |
| 草神卡池 | 须弥 | 待实现 |
| 水神卡池 | 枫丹 | 待实现 |
| 火神卡池 | 纳塔 | 待实现 |
| 月神卡池 | 挪德卡莱 | 待实现 |

## 3. 货币与消耗

- **祈愿货币**：原石（ItemName.Primogem）
- **单次消耗**：160 原石
- **祈愿模式**：1次（消耗160）/ 10次（消耗1600）
- 初始原石：16000（新建存档）

## 4. 获取规则

- **角色卡**：获得 1 张
- **物品卡**：获得 1 份（数量 = `ItemData.countPerServing`）

### 4.1 重复角色卡→星辉转换

抽中**已经拥有**的角色卡时，不增加该卡数量，而是转换为**星辉**（ItemName.Starglitter）：

| 星级 | 星辉数量 |
|------|---------|
| 5★ | 50 |
| 4★ | 25 |
| 3★ | 15 |
| 2★ | 8 |
| 1★ | 3 |

- 重复角色卡**不增加**背包中该角色的数量
- 星辉数量累加到存档中（初始为0）
- 转换逻辑在 `WishManager.AddCardToInventory` 中实现，通过 `GetStarglitterByStar(int starLevel)` 查表

### 4.2 星辉雨动画

抽中重复角色时触发星辉雨动画：

- 从屏幕顶部随机 X 位置落下星辉图标（`masterless_starglitter.png`）
- 分批错落下落（每批 2~5 颗，批次间隔 0.15~0.35s），避免全部同时落下
- 每颗星辉：随机大小 40~60px、随机起始旋转、匀速下落、旋转下落
- 数量与真实获得星辉一致（每颗代表 1 个星辉）
- 下落持续 0.6~1.0s，落到屏幕底部以下才销毁（不淡出）
- 动画容器：`WishDrawRoot/StarglitterRainContainer`（全屏铺满，渲染层级在 Background 之上、CardTrack 之下）
- 性能优化：对象池 + 单协程批量管理（`List<StarglitterDropData>` struct 列表，无每帧 GC）

### 4.3 相遇之线

玩家每累计获取 **20 个星辉**，下一次射击从"命运之线"升级为"**相遇之线**"（金色线）。

**溢出处理**：星辉不消耗，基于累计总量计算。如一次获得 50 星辉 → 2 次相遇之线 + 余 10 进度。

**相遇之线效果**：

1. 射击线变为金色（`encounterLineColor`）
2. 射中卡后，先正常写入存档 + 重复判定 + 星辉雨（如有重复）
3. 随机星级提升次数：用卡池权重 RollStarLevel 映射

| RollStarLevel 结果 | 提升级数 |
|------|---------|
| 5★ | 4 级 |
| 4★ | 3 级 |
| 3★ | 2 级 |
| 2★ | 1 级 |
| 1★ | 0 级（不提升） |

4. 逐级提升：每升 1 级 → 从该星级候选列表随机选卡 → 更新卡片显示（`Card.Init`）
   - 角色：检查重复 → 发放星辉 → 播放星辉雨 + 光带 + 光柱
   - 物品：直接堆叠，不判重复
5. 提升过程持续播放**抖动+越来越亮**动画（保留惊喜感，实际结果已算好）
6. 提升结束后停止抖动，卡片飞到左侧

**代码位置**：`WishDrawController.EncounterRevealCoroutine` + `CardShakeGlowCoroutine`

### 4.4 星辉进度条

屏幕顶部居中显示星辉进度条（`WishDrawRoot/StarglitterProgressBar`）：

- 进度 = `(星辉总数 % 20) / 20`
- 满时（有可用相遇之线）触发**抖动 + 流彩特效**（Fill 旋转 + 位置抖动）
- 进度条不消耗星辉，仅显示累积进度

| 物品 | 每份数量 |
|------|---------|
| 摩拉 | 5 |
| 苹果 | 1 |
| 小麦 | 1 |
| 蒲公英酒 | 1 |
| 体力 | 1 |
| 原石 | 5 |

## 5. 祈愿流程

### 5.1 整体流程

```
点击祈愿按钮 → 消耗原石 → 黑色背景遮屏 → 卡道开始滚动
→ 1.5秒倒计时（玩家射击或超时自动射击）→ 命中卡牌 + 特效
→ 0.6秒冷却 → 重复直到次数用完
→ 卡道隐藏 → 结果卡飞到屏幕中间横向展示 → 点击关闭
```

### 5.2 卡道机制

- 卡道位于屏幕中间竖向条带，卡片从底部移动至顶部
- 卡片**永远滚动**，射击和冷却期间不暂停
- 卡道上的卡片**随机生成**（`DrawRandomTrackCard`），与实际结果无关
- 每张卡按卡池概率随机选择星级和角色/物品
- 卡片紧密排列，无间隙（`cardSpawnInterval = 0.09s`，`cardMoveDuration = 0.75s`）
- 初始填满卡道（老虎机效果，一开始就有卡片在各位置移动）

### 5.3 射击机制

- 玩家点击鼠标左键射击（或1.5秒倒计时到期自动射击）
- 射击后发射"命运之线"：白色细线从屏幕右侧到左侧，0.2秒完成
- 命中卡道上**最接近屏幕中心的卡**
- **射中什么卡就获得什么卡**——读取该卡的 `Card.saveCardData` 作为实际结果
- 射击冷却：0.6秒（冷却期间卡道继续滚动，倒计时不计时）

### 5.4 倒计时

- 每次射击有1.5秒时间限制
- 屏幕底部居中显示横条倒计时（类似血条）
- 倒计时从两侧向中心缩短（`localScale.x` 缩放）
- 超时自动射击

## 6. 特效

### 6.1 光带特效

- 射中卡片后在**卡片上**播放 `CardLightBandEffect`（`Card.PlayLightBand()`）
- 默认金色光带从左下到右上扫过卡面
- 持续0.3秒

### 6.2 光柱特效

- 射中卡片后在**卡道中心**播放 `LightPillarEffect`
- 颜色 = `StarColor.GetStarColor(starLevel)`（1★绿/2★蓝/3★紫/4★橙/5★红）
- 光柱渲染在卡道**后面**（`SetSiblingIndex` 在 cardTrack 前），不遮挡卡片
- 包含：核心光束、多层光晕、地面闪光、双向飞溅粒子
- 粒子参数：数量30、速度1200、扩散1.5、时长0.8s
- 粒子持续飞行不停止，越接近终点越淡出
- 光柱上升0.5秒后调用 `Stop()` 淡出（0.5秒），总生命周期约1.1秒

## 7. 结果展示

### 7.1 射击后

1. 被射中的卡从卡道移除，`SetParent(resultContainer, true)` 保持世界位置
2. 在屏幕中心停留0.3秒（让玩家看清抽到了什么）
3. 飞到屏幕左侧竖向排列（从下到上）

### 7.2 全部抽完后

1. 卡道和倒计时隐藏
2. 左侧竖列的结果卡飞到屏幕中间，横向排列展示（`FlyToPosition`，缓出曲线）
3. 玩家点击任意位置后关闭祈愿，回到卡池界面

## 8. 氛围特效

### 8.1 概述

祈愿界面背景层包含两层 ParticleSystem 驱动的氛围特效，由 `WishAmbienceController` 统一控制。

Canvas 采用 **Screen Space Camera** 模式（非 Overlay），配 UICamera（Orthographic）使 ParticleSystem 可在 UI 层渲染。

排序层级：Background 子 Canvas (sortingOrder=50) < SmokeParticles (74) < ElementParticles (75) < 主 UI (100)。

### 8.2 元素粒子 (ElementParticles)

- **材质**：`Wish/ParticleAdditive` 自定义 shader（硬编码 `Blend SrcAlpha One`，绕过 URP shader GUI 重置问题）
- **纹理**：`SoftCircle.png`（64×64 径向渐变，Uncompressed RGBA32）
- **混合模式**：Additive（发光效果）
- **运动**：从右往左飘动，Velocity X=[-80,-40]，Y=[-15,15]
- **颜色**：跟随卡池角色元素颜色，切换卡池时通过 `GetParticles`/`SetParticles` 修改已存活粒子的颜色
- **加速效果**：切换卡池瞬间触发 `OnPoolSwitching()`，粒子速度 ×8 持续 0.6 秒，然后 0.5 秒 lerp 平滑减速回原速。发射率同步缩放维持密度恒定
- **预填充**：`StartWithPrewarm()` 用 `Simulate(duration)` 手动预热，粒子初始分布在屏幕各处
- 参数：rate=2/s, lifetime=25-35s, maxParticles=200, size=15-30

### 8.3 雾气粒子 (SmokeParticles)

- **材质**：`Wish/ParticleAlpha` 自定义 shader（Alpha Blend）
- **纹理**：`SoftCloud.png`（256×256，Perlin 噪声调制的云雾形，Uncompressed RGBA32）
- **运动**：从右往左缓慢漂移，Velocity X=[-60,-30]，Y=[-2,2]
- **范围**：屏幕底部 30% 区域（Shape position Y=-504, scale Y=432）
- **噪声**：Noise 模块（strength=15, frequency=0.1）使漂移有机化
- **淡入淡出**：Color over Lifetime gradient 中段 alpha=1.0，透明度由 startColor（alpha=0.02）单独控制
- **尺寸呼吸**：Size over Lifetime 曲线 0.2→1→1→0.5，缓入缓出
- 参数：rate=4/s, lifetime=15-25s, maxParticles=100, size=800-1600

### 8.4 WishAmbienceController API

| 方法 | 说明 |
|------|------|
| `StartAmbience()` | OnEnable 时调用，Simulate 预填充 + Play |
| `StopAmbience()` | OnDisable 时调用，StopEmitting（非 StopEmittingAndClear） |
| `OnPoolSwitching()` | 切换卡池瞬间调用，触发粒子加速 |
| `SetElementColor(Color)` | 设置元素粒子颜色，同时更新已存活粒子 |

### 8.5 注意事项

- `prewarm=true` 只对 `playOnAwake` 生效，手动 Play 不触发。需用 `Simulate(duration, true, true)` 手动预热
- `main.startColor` 只影响新粒子，已存活粒子需 `GetParticles`/`SetParticles` 修改
- Velocity X/Y/Z 三轴 curve mode 必须一致（同为 TwoConstants 或 Constant）
- 最终 alpha = `startColor.a × colorOverLifetime.gradient.a`，透明度只在一处控制
- Tuanjie 引擎 API 差异：`ParticleSystemGradientMode.Color`（非 `ParticleSystemCurveMode.Color`），`NoiseModule` 无 `dampen` 属性

## 9. UI 结构

### 9.1 场景层级

所有祈愿UI在 `WishScreen` 场景的 `Canvas/WishDrawRoot` 下静态预设（非代码动态创建）：

```
Canvas (WishDrawController)
├── BackPanel
├── ColumbinaPanel / VentiPanel / ... (角色面板)
├── Ambience
├── Panel (TopPanel / LeftPanel / BottomPanel / RightPanel)
└── WishDrawRoot (初始隐藏)
    ├── Background (全屏黑色不透明)
    ├── StarglitterRainContainer (全屏，星辉雨动画)
    ├── StarglitterProgressBar (顶部居中横条)
    │   └── Fill (localScale.x 缩放)
    ├── CardTrack (竖向条带，无背景Image)
    │   ├── SpawnPoint (底部锚点)
    │   ├── EndPoint (顶部锚点)
    │   └── FateLineContainer (铺满卡道)
    ├── CountdownBar (底部居中横条)
    │   └── CountdownFill (居中缩放)
    ├── ResultContainer (屏幕左侧竖向条带)
    └── FinalDisplay (全屏 + CanvasGroup)
```

### 9.2 字段引用

`WishDrawController` 所有UI引用为 `[SerializeField]`，在场景中赋值：

| 字段 | 说明 |
|------|------|
| cardPrefab | Card.prefab（复用背包卡牌预制体） |
| drawRoot | WishDrawRoot（整体显隐） |
| cardTrack | 卡道容器 |
| cardSpawnPoint / cardEndPoint | 出卡点/终点 |
| fateLineContainer | 命运之线容器 |
| countdownBar / countdownBarFill | 倒计时条 |
| resultContainer | 结果卡容器（左侧） |
| finalDisplayContainer / finalDisplayCanvasGroup | 最终展示（全屏） |
| starglitterRainContainer | 星辉雨动画容器 |
| starglitterSprite | 星辉图标精灵 |
| starglitterProgressBar / starglitterProgressFill | 星辉进度条 |
| encounterLineColor | 相遇之线颜色（金色） |

### 9.3 参数

| 参数 | 值 | 说明 |
|------|:--:|------|
| cardMoveDuration | 0.75s | 卡片从底到顶的移动时间 |
| cardSpawnInterval | 0.09s | 卡片生成间隔 |
| cardWidth | 100 | 卡片宽度 |
| cardHeight | 150 | 卡片高度 |
| fateLineSpeed | 8000 | 命运之线速度（实际用固定0.2s） |
| clickTimeLimit | 1.5s | 每次射击时间限制 |
| shotCooldown | 0.6s | 射击冷却 |
| resultCardSize | 80 | 结果卡尺寸 |
| finalCardSize | 160 | 最终展示卡尺寸 |

> **注意**：`[SerializeField]` 的值在场景中序列化后，代码修改默认值不会生效。改参数时需同时更新场景中的序列化值。

## 10. 代码架构

### 10.1 核心类

| 类 | 文件 | 职责 |
|----|------|------|
| WishPoolConfig | Data/Wish/WishPoolConfig.cs | 卡池配置（ScriptableObject） |
| WishManager | UI/Screen/Wish/WishManager.cs | 货币消耗、写入存档、配置访问 |
| WishDrawController | UI/Screen/Wish/WishDrawController.cs | 全流程UI动画控制 |
| WishScreenDraw | UI/Screen/Wish/WishScreenDraw.cs | WishScreen 的祈愿入口（partial class） |
| WishTrackCard | (WishDrawController.cs 内) | 卡道上移动的单张卡牌 |
| LightPillarEffect | Tool/Component/LightPillarEffect.cs | 光柱爆发特效 |
| CardLightBandEffect | Battle/Card/CardLightBandEffect.cs | 卡片光带特效 |

### 10.2 关键设计决策

1. **反应速度游戏**：不预抽结果，玩家射中什么卡就获得什么卡。卡道卡随机生成，实际结果由 `Card.saveCardData` 读取并写入存档。

2. **卡道永远滚动**：射击和冷却期间卡道不暂停，保持视觉连贯性和紧张感。

3. **同一卡片飞行**：被射中的卡直接从卡道 `SetParent(resultContainer, true)` 转移到结果容器，保持世界位置不突变，先停留0.3s再飞到左侧。

4. **协程隔离**：`LightPillarEffect` 用 `_activeCoroutines` 列表管理自己的协程，`Stop()` 只停自己的协程不影响外部 `GlowBurstCoroutine`。粒子协程不追踪，由 `ClearSpawned` 销毁。

5. **Card.skipFadeIn**：卡道卡设置 `skipFadeIn = true` 跳过 `PlayFadeIn` 协程，避免对象池复用时 `OnEnable` 触发淡入导致半透明。

6. **性能优化**：
   - `LightPillarEffect` 的纹理和 Sprite 为 `static` 懒加载，所有实例共享，避免每次射击重建像素数据。
   - `FateLine` 的 Shader 缓存为 `static` 字段，避免每次射击 `Shader.Find`。
   - `StartWish` 时预构建星级→候选列表缓存（`Dictionary<int, List<UnitName/ItemName>>`），卡道出卡直接查表，不逐卡遍历全表。
   - 存盘改为抽卡流程结束后统一调用一次 `SaveGame()`，而非每张卡都存盘。
