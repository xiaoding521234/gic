# 战斗系统技术设计

> 2026-09-12 按 docs/18 两轮拍板（2026-08-16 + 2026-09-12）起草。B1-B8 的技术实施蓝图：**B1 按本文直接开工**，B2-B8 开工前按需细化对应章节。
> 规则本体：docs/04-回合流程、docs/05-行动系统（体积/阻挡/逐步判定）、docs/06-元素与反应、docs/07-势力机制；决策与选项对比：docs/18（决策四=执行阶段模型）；本文只管"怎么实现"。
> 现有代码骨架（Unit 组件容器/UnitStats/BaseSkill+SkillFactory/SkillContext）全部复用，见 docs/17 §7。

## 1. 三层时间模型

| 时间层 | 判定粒度 | 语义 | 读取状态 |
|---|---|---|---|
| 片间（攻速差） | 攻速片 | 硬先后：片 N 全部结算并应用后，片 N+1 才结算 | 当前状态（前序片已应用） |
| 片内（同攻速） | 瞬发效应 | **真同时（快照结算）**：读取片前快照、独立结算、效应统一应用 | 片前快照 |
| 块内（行动→衍生链） | 行动块 | 因果序：延奏→变奏等衍生效果在行动块内部串行展开 | 块内继承 |

- 攻速片：**攻速值完全相同**的单位才同片；片开始时刻 =（全场最高攻速 − 自身攻速）÷ 10 秒（连续换算，非 10 点分档）
- 死亡中断仅跨片发生（"开始执行前被击杀才落空"）；同片互杀 = 同归于尽（快照合并 HP）
- 枚举序（unitID 升序）仅决定命令在流中的排列与写-写冲突合并依据，**不影响结算结果**

## 2. 执行阶段：分步流式演算

结构对标王者荣耀/原神的服务器权威（客户端实时表现+输入随时上交），tick 粒度换成攻速片。

**片循环（Host 侧）**：
1. 收集本片全部行动 → 快照片前状态 → 各行动独立结算（只读快照）→ 效应统一应用 → 产出片命令块
2. 立即推送本片命令块 → 等全体客户端 ack 或超时快进 → 结算下一片
3. **片边界 poll 即时行动队列**（连携/契约类），有则插入结算（产出独立插入块）
4. 末片完成 → 回合结束效果（按注册序）→ 阶段切换前扫 WaitConditions（如连携 2 秒窗未走完 → 等待）→ 广播回合结束 → 下一选择阶段

**门控与约束**：
- **Host 永不跑在客户端前面**（片级 ack）：即时输入永远作用于"当前演算点之后"，无回滚
- 两类交互点：软窗（演算照走，阶段切换前等待）、硬停（Pause→收输入→Resume）；**交互窗时间不可被"提前完成"压缩**
- "提前完成不空等" = 播放器侧压缩片间等待，仅对纯演算片生效

**片内快照结算**：
```
effects = []
foreach action in slice.actions（枚举序）:
    effects += action.Resolve(stateBeforeSlice)   // 只读片前快照
state = Apply(state, effects)                     // 效应统一应用
segment = Serialize(effects)                      // 产出片命令块
```
写-写冲突（伤害/治疗合并 HP 天然成立；同格超体积、双元素同附等）走确定性合并规则（§7.4 / docs/11）。

**瞬发×移动并存**：快照结算管瞬发效应（伤害/命中按片初位置），移动在同片内按 §7 动态逐步展开；瞬发命中判定暂定按片初位置（docs/11 跟踪确认）。

## 3. 数据模型

### 3.1 ActionData（选择阶段上交）
- `ActionType { Move, Skill, Burst, DeployUnit, DeployBuilding, UpgradeConstellation, BuyCard, UseItem, EquipItem, FactionSkill, OpenChest, Pass }`
- 字段：playerID / unitId / ActionType / SkillContext 型参数（目标格/方向/目标单位）/ ForceData（移动）/ cardId 等，按类型部分有效
- B1 先实现 Move / Skill / Pass，其余枚举占位、B6 补全

### 3.2 BattleCommand（单命令，union 风格）
- `BattleCommandType { Move, Damage, Death, Heal, ApplyBuff, RemoveBuff, ElementAttach, Reaction, StatChange, Summon, Effect, UI, Pause, Resume, ... }`
- 字段：type / actorUnitId / 片号 / 片内序 / **visibilityScope（预留，切片恒全可见）** / 平铺 payload（目标格/方向/数值/位移路径…）
- 命令粒度 = 一次原子视觉事件（沿用 docs/18 决策一）

### 3.3 Segment（片命令块）
- `{ turnNumber, sliceIndex, insertedInstantAction 标记位, List<BattleCommand> }`
- 回合输出 = Segment 流（逐片推送；**不再有回合级整块 TurnScript**）

### 3.4 BattleSnapshot（选择阶段头全量）
- `{ turnNumber, List<UnitState>{ unitId/位置/HP/攻速/元素附着/Buff 列表/尸体态/阵营/体积 }, 双方资源（摩拉/体力/手牌/牌库） }`
- 地形全量仅对局开始发一次（迷雾后置；地形变化 B8 走增量命令）

### 3.5 序列化
- **JSON**（JsonUtility；平铺字段适配其序列化能力，不用 Dictionary——列表+枚举索引代替）
- `IBattleSerializer` 接口隔离，将来可换二进制且不改调用方
- 调试友好优先：片级消息肉眼可读

## 4. 逻辑层载体与双端形状

- **逻辑 Unit = 现有 MonoBehaviour 组件容器**（Unit + 8 组件 + Skills/Buffs），仅 Host 场景实例化（可隐藏层），零重构复用 UnitStats/UnitElement/UnitMoveable/UnitStatus
- **UnitView = 双端同构表现层**（客户端与 Host 显示侧同一套代码）：Snapshot 建场、Segment 驱动动画，不持逻辑状态
- 本地/单机（B1-B6）：Host 与 Client 同进程，走 IBattleTransport-Local 直通；B7 LAN：同一接口换 Mirror 实现，上层零改动
- 铁律：**战斗逻辑不走 EventBusHub**；战斗内消息只走 IBattleTransport 专用通道

## 5. 回合流程骨架（B1 核心）

新增类（GIC.Battle 命名空间，Battle/ 下按 Unit/Skill/Tile/Buff 同级新增 Flow/ 目录）：

| 类 | 职责 |
|---|---|
| TurnFlowController | 回合状态机：最小版 = 选择阶段（收齐行动）→ 执行阶段 → 回合结束循环；四阶段完整版（低级单位决策→玩家选择→执行→回合结束，docs/04 §4.1）B6 落地 |
| TurnResolver | 执行阶段片循环：攻速分桶→片内快照结算→应用→产出 Segment→ack 等待→即时行动 poll |
| ActionExecutor 族 | MoveExecutor / SkillExecutor / PassExecutor（B1）；Deploy/Use/… B6 补全 |
| BattleSimState | Host 侧战场状态访问门面（快照生成/效应应用/unitId 注册） |

- B1 的 AI 玩家决策 = 固定脚本占位（如恒定攻击最近敌人），B6 换启发式；低级单位（1~2星）自主决策逻辑 B6 落地
- B1 的技能 = DebugAttackSkill（硬编码固定伤害），用于跑通 DamagePipeline；B4 换正式 BaseSkill 子类 + SkillConfig.asset

## 6. DamagePipeline

核心公式（docs/05 §5.6）：
```
最终伤害 = (攻击力 × 攻击百分比 + 额外伤害) × (增伤 − 免伤) × 易伤
易伤 = 防御 ≥ 0 ? 100/(100+防御) : 1 − 防御/100
```
Phase hooks（B1 预留点位，B2-B4 接线）：
1. PreDamage：命中判定 / 元素反应预判（B4）
2. 乘区修改：Buff/技能/激化"+10"类插入
3. PostDamage：命中触发（获元能/吸血）+ 死亡判定

死亡判定：HP≤0 → 尸体态（UnitStatus 切换），碰撞/体积/属性/势力保留（docs/05 §5.4）。

## 7. 移动系统

### 7.1 判定链（逐格步进，每步依序）
1. **体积绝对层**：格内现有体积 + 自身体积 ≤ 3，否则停在格前——**无视阻挡能力也不可绕过**
2. **阻挡规则层**：UnitMoveable 三配置（blockAllies/blockEnemies/blockedByEnemies）——无视阻挡单位跳过本层
3. **地形层**：TileType 标签（AllowWalk/AllowFly/WaterTerrain）按 ForceType 判定

### 7.2 逐步判定原则
- 只判定**将要进入的下一个格**；移动者**当前所在格不参与判定**（同格共存单位不阻挡彼此离开/途经）
- 完全进入下一格前，位置判定仍在上一步的格

### 7.3 同片同步逐步结算
同片全部移动行动按"轮"推进（每轮所有未完成移动者各走一步）：
- 每轮先做各移动者的"下一格判定"（读本轮开始时的位置判定），通过者同轮进入
- 对方已离开的格不再阻挡 → **相向对穿 = 互相穿过**、中途同格瞬时共存不判定
- 跨片：前序片位置更新后才结算后续片（硬先后）
- 被挡 = 停在格前，该移动结束（剩余步数作废）。表现层（2026-09-21）：被挡也产出 Move 命令（metadata=MoveBlocked 标记 + direction=被挡方向；全挡 path=[原格]、部分挡 path=已走段），客户端播"撞墙弹回"——向被挡方向探出约半格再弹回原位，逻辑位置不变

### 7.4 写-写冲突合并（B4 拍板，B1 留接口）
- 同轮多单位移入同格超体积：候选 = 枚举序前 N 入格 / 全部争格失败（docs/11）
- 双元素同时附着同一目标 / 双推力同时施加：确定性合并规则待定（docs/11）
- 同片多伤害/治疗：数值合并，天然成立

### 7.5 位移类型
ForceType { Walk, Fly, Amphibious, Knockback, Pull, Jump, Teleport }：Pull 无视阻挡规则但受体积封顶；建筑免疫强制位移。

## 8. 消息协议（IBattleTransport）

| 消息 | 方向 | 说明 |
|---|---|---|
| BattleStartMessage | Host→Client | 对局开始：地形全量 + 初始 Snapshot |
| SnapshotMessage | Host→Client | 每选择阶段头：全量状态 |
| SubmitActionRequest | Client→Host | 行动选择上交（B7 LAN 时挂 NetworkEventBus OnlyHost+Immediate） |
| SegmentMessage | Host→Client | 片命令块推流 |
| SegmentAck | Client→Host | 片播放完成确认（超时快进） |
| SubmitInstantAction | Client→Host | 即时行动（连携/投票），片边界 poll——B1 占位 |
| PauseMessage / ResumeMessage | Host↔Client | 硬停交互点（契约谈判）——B1 占位 |
| TurnEndMessage | Host→Client | 回合结束 + WaitConditions 结果 |

B1-B6 本地模式 = Local 实现（同进程直调）；B7 增加 Mirror 实现。

## 9. B1 范围与验收

交付物：
1. 数据类全套（ActionData/BattleCommand/Segment/BattleSnapshot）+ JSON 序列化 + IBattleTransport-Local
2. TurnResolver 片循环骨架（分桶 / 快照结算 / ack 等待 / 即时行动 poll 占位）+ TurnFlowController 最小回合循环
3. MoveExecutor（体积→阻挡→地形判定链 + 同片逐步结算）+ SkillExecutor（DebugAttackSkill 走 DamagePipeline）+ PassExecutor
4. DamagePipeline 核心公式 + hooks 点位
5. BattlePlayer 最小播放器（消费 Segment：位移 / 扣血数字 / 死亡灰显；UnitView 同格队形布局 = 1 居中/2 并排/3 三角——队形为 B1 期实现，2026-09-17 决策二已推翻、B5 改两态模型）
6. 灰盒战场场景：色块 Tile 20×20 + 头像立牌 UnitView + MainHall 调试入口按钮 + 固定测试牌组

验收：
- [ ] 两个测试单位：上交→分桶→结算→Segment 推流→播放 全链路
- [ ] 同攻速互杀 = 同归于尽；跨片击杀 = 后续行动落空
- [ ] 逐步移动判定：三单位封死满体积格（无视阻挡也过不去）；两同攻速单位同时移入同格共存；对穿互换互相穿过
- [ ] 片边界插入调试即时行动正确生效（验证协议预留）
- [ ] 本地全链路零 EventBusHub 依赖

## 10. B2-B8 演进挂钩

| 批次 | 接入点 |
|---|---|
| ~~B2 Buff~~ | **已落地（2026-09-18）**：BaseBuff 实体化（回合制计时=回合结束减一含施加当回合，§4.4；同类合并=时长累加+级别取大）+ BattleSimState 全局注册表（回合结束效果按注册序）+ BurnBuff（每回合末 10 火伤、3 回合×级别，docs/06）+ ApplyBuff/RemoveBuff 命令（Segment 新 turnEnd 段承载回合结束效果，短节拍不高亮执行者）+ BattlePlayer 接线 + UnitView 头顶 Buff 徽章行（快照权威+命令增量）；调试技能命中附带 1 级燃烧验证链路（B4 退役）。激化对 DoT 的 +10 加成、草元素延长 → B4 乘区/反应批次 |
| B3 管理器 | TurnFlowController/BattleManager 职责边界归位（含 WishManager 层级遗留） |
| ~~B4 技能~~ | **首批已落地（2026-09-18 晚）**：SkillExecutor 正式技能链（skillIndex=UnitConfig.skills 同源索引；SkillFactory 未注册→UnimplementedSkill 占位保索引对齐、不可施放）+ 首批三技能（安柏战技一箭双丘丘=直线投射物首敌格截停 AoE 80% 火伤 delivery=1、安柏爆发箭雨=整线 AoE 160% 火伤、凯亚战技霜袭=前方 2 格 AoE 100% 冰伤）+ 元素反应二例（融化=易伤 +50% 并入伤害乘区、冻结=FreezeBuff 2 回合硬控 UnitStatus.Frozen 重复取更长）+ 附着系统（AttachElementEffect 覆盖=消耗被反应附着）+ Damage 命令 delivery 元数据+发射格 + BattlePlayer 箭矢飞行（光条 B5 换素材）+ 冻结视觉（立牌/底座冰色）+ HUD 战技/爆发=十字方向瞄准（点方向格归一提交，UIText 12014）+ DebugAttackSkill 退役。**B4 简化记录**：①多段伤害合并单次结算（一次命中只触发一次反应）②投射物命中按片初快照格位置（Host 即定路径；移动单位连续插值命中=B5 表现批次）③反应级别恒 1（多层消耗强化→层数批次）④元能消耗检查待 B6 ⑤天降视觉/箭矢素材 B5 ⑥激化对 DoT +10 加成、草延长→激化/燃烧批次。**B4 剩余**：拖动式瞄准、燃烧正式来源（切片无草角色）。写-写合并=MovementResolver 已按"争格失败停格前"实现（docs/11 候选即现状，B4 视为已定案） |
| B5 表现 | 斜俯视棋盘 / Tween / 震屏替换灰盒；UnitView 池化 + 两态模型（收拢重叠格心/选择阶段散开）/ 圆柱受击体连续判定 / 投射物飞行（§11 修订版、决策二 docs/18 2026-09-17）。**连续判定核心已落地（2026-09-19）**：ProjectileResolver 时间轴分段求交 + 命中点/消散点千分定点下发 + BattleMetrics 常量收口 + 底座圆盘化（视觉即判定）+ 播放侧同 t=0 起跑。**B5 剩余**：箭矢正式素材、箭雨天降视觉、震屏/受击闪白正式化、UnitView 池化、立牌美术升级（12 帧动画+完整人物立牌）、圆柱直径目检定 |
| B6 回合 | 四阶段全流程（低级单位决策→玩家选择→统一执行阶段，docs/04 §4.1）+ 选择时限（docs/04 §4.2）+ AI 玩家启发式与低级单位自主决策 + 牌组/部署行动类型补全 |
| B7 联机 | IBattleTransport-Mirror；双开控制调试模式 → LAN |
| B8 收口 | 建筑/物品/命座最小集 + 迷雾（visibilityScope 启用）+ 结算 |

## 11. 表现层：两态立牌与连续判定（2026-09-17 修订，B5 基线）

决策本体见 docs/18 决策二（2026-09-17 修订条目）；表现层时序与逻辑层解耦不变（逻辑即时结算，BattlePlayer 只管"何时播"）。~~2026-09-12 版"立牌队形 + 格子判定区"基线随队形方案一并作废。~~

- **立牌风格参考饥荒**：2D 立牌 + 底座投影；格子表现尺寸明显大于单位立牌（格感边界与重叠可读性服务）
- **同格单位重叠格心 + 两态模型（取代队形）**：
  - **执行阶段（演出态）= 收拢重叠**：全部立牌与格心重合；特效锚 `CellToWorld` 格心 = 锚所有单位，特效与立牌错位在根源上消除
  - **选择阶段（决策态）= 自动散开**：进入选择阶段即全部立牌散开展示（1 居中/2 并排/3 三角偏移布局复用为展开形态），底座正常显示；无需悬停/选中触发
  - 实现要点：`RefreshAllFormations` 改为阶段切换驱动（选择阶段开始→散开、执行阶段开始→收拢），不再每片重排；`GetFormationOffset` 从常驻队形改为展开布局
- **受击体 = 立牌圆柱（取代格子判定区）**：圆柱以底座圆盘为可视化（直径 0.42 起，容错 B5 目检定）；受击范围与格脱钩，视觉即判定
- **格 = 交互粒度，非判定粒度**：移动/占据/阻挡/技能范围声明按格；命中 = 投射物轨迹/特效形状 ∩ 圆柱；**移动中单位处于连续插值位置、可被中途命中**；移动速度/投射物速度为**逻辑常量**挂执行阶段时间轴（表现动画只是其可视化，Host 确定性不破）
- **命中分层**：接触判定（单位级，决定谁被打到）与效果作用域（技能声明：单目标/命中格 AoE/全体）解耦；格 AoE = 接触任一同格圆柱后按格声明生效（堆叠同心下与旧"格内全中"结果一致，向后兼容）
- **投射物真实飞行**：按弹道飞行（非逐格瞬移），命中 = **接触目标立牌圆柱之时**；效果作用域按技能声明（安柏战技 = 命中格 AoE）
- **弹射物路径与截停**：直线飞行型覆盖由 Host 逻辑即定（发射格 → 接触首个敌方圆柱截停，或 24 格上限消散——docs/05 §5.3）；命中时刻由 Host 判定后随 Segment 命令下发（发射格 + 命中点）。**连续坐标载体已定稿（2026-09-19 B5 落地）：千分定点字段 hitX/hitY（连续格心坐标×1000，格 c 的心=c+0.5，与 BattleBoard.CellToWorld 同系数）**——Damage 命令带命中点、无接触时发 Effect 命令（EffectKindProjectileVanish）带消散点，客户端零推算直读
- **B5 连续判定实现要点（2026-09-19 落地）**：①速度常量收口 BattleMetrics（投射物 8 格/秒、移动 0.18s/格、圆柱直径 0.42——判定与底座圆盘同源，视觉即判定）；②投射物技能产出 ProjectileEffect（只声明发射），ProjectileResolver 在同片移动展开后按时间轴分段求交（相对位移线性→二次方程小根=最早进入时刻），命中时刻平局按 unitId 升序（枚举序铁律）；③只有位置读命中时刻，HP/附着/Buff 等状态仍读片前快照（同片并发基石不变）；④格 AoE=命中时刻全体敌方连续位置四舍五入格==命中者所在格（接触判定与效果作用域解耦；堆叠同心下与旧"格内全中"结果一致，向后兼容）；⑤与 B4 格级近似的**行为差异**：同格堆叠敌方（贴脸）t=0 即接触命中（旧实现从相邻格起扫描、不打同格）；虚空格截断弹道（中心进入虚空格即消散）；⑥播放侧投射物/移动同 t=0 起跑（不吃命令 stagger），命中点/消散点定点驱动；箭矢正式素材/天降视觉/震屏闪白 → B5 表现美化后续波次
- **投放形态由技能数据驱动**：直线飞行（安柏战技）/ 整线天降斜落（安柏爆发）/ 单位指向无投射（延奏、契约——直接对目标单位生效，不经格子判定）；Segment 的 Damage 命令带 delivery 元数据或 BattlePlayer 按 skillId 查表（B4 定）

## 12. 预留位

- **迷雾**：visibilityScope 字段已在命令/快照结构中，B8 启用
- **断线重连**：协议占位（Snapshot 全量机制天然支持：重连发快照+跳过演算）
- **双开控制调试模式**：本地模式天然支持——两个玩家的输入 UI 共用一进程（B7 细化切换面板）

## 13. 战斗 HUD 设计基线（2026-09-18，B6）

决策本体见 docs/18 决策六；高保真设计稿 = [docs/designs/battle-hud-v1.html](../../designs/battle-hud-v1.html)（三视图 + 交互状态机 + 规格表，素材引用项目内真实资产）；实现归 B6（回合流程串联 + 正式战斗 HUD）。

### 布局规格（2560×1440 基准 · 比例锚点方案）

| 区域 | 锚点 | 规格 | 备注 |
|---|---|---|---|
| 移动技能 | 左下（摇杆位） | Ø220 · 距左 64 / 距底 190 | 圆心距底 300 与爆发盘心同高（左右对称，2026-09-18 用户目检上移）· walk 图标占位 · 体力徽章 10 · 常驻 |
| 爆发技能 | 右下盘心 | **Ø264（=220×1.2）** · 距右 300 / 距底 300 | 技能盘视觉锚点 · 金色高亮环 |
| 战技 | 围绕爆发 · 左弧位 | Ø220 · 与爆发圆心距 300 | 元素色环 · 体力徽章 10 |
| 延奏 | 围绕爆发 · 上弧位 | Ø220 · 与爆发圆心距 300 | 图标待定（风车占位）· 契约同圈层加位 |
| 取消按钮 | 右上 | Ø196 · 距右 56 / 距顶 212 | 仅拖动/瞄准态出现 · 红描边呼吸动效 |
| 设置/退出 | 右上角 | 56×56 · 距右 26 / 距顶 26 | 走 BattleExitConfirmDialog 确认 |
| 顶部回合中枢 | 顶部中央 | 胶囊（回合数+阶段徽章+倒计时）+ 战斗时钟 + 攻速队列条 | 时钟 6:00 起 +20/回合 |
| 双方信息 | 顶部左右 | 徽标 Ø86 + 协议核心血条 380×16 + 体力/摩拉 chip | 蓝红镜像；核心 HP=胜负条件 |
| 手牌区 | 底部中央 | 卡 158×212 横排 gap18 + 提示条在上 | 部署/装备/食物 · 体力消耗角标 |
| 可选格高亮 | 棋盘（世界层） | 格心径向金渐变 · 吸附框金描边 4px | 吸附最小单位 = 1 格（决策六） |

### 技能使用交互状态机（两方式等价，共用瞄准框架）

- **拖动式**：按住技能按钮拖出 → 棋盘高亮可选格（技能范围声明）→ 指针吸附最近可选格（吸附框+范围预览）→ 右上取消按钮浮现 → 松手：可选格 = 释放 / 取消按钮上 = 取消 / 无效格 = 取消
- **点击式**：单击按钮 → 技能详情面板浮出（名称/元素/伤害/范围/消耗/描述）→ 三情况分流：①再点按钮 = 收面板+进瞄准态（点格释放，取消按钮同现）②点外部 = 仅收面板 ③直接拖动 = 切换拖动式
- 移动技能同框架：拖出/点击 → 高亮可达格（移动力内）→ 吸附目标格
- 手牌部署/装备的目标选择建议复用同一框架（B6 定）

### 待 B6 收敛项（与 docs/18 决策六"待拍板"同步）

- ~~**单位选择交互**~~ → **已定案（2026-09-18）**：点击棋盘立牌选中单位 → 技能盘出现显示该单位技能 + 手牌隐藏（选中态/手牌态互斥）；点空白处取消选中（技能盘收起、手牌恢复）。**未选中单位时无技能按钮可见**
- ~~移动按钮位置~~ → **已定案**：圆心距底 500（屏幕高 65%），两轮目检收敛
- **立牌美术升级路线（2026-09-18 登记）**：头像立牌为临时方案，后续升级 = **12 帧动画 + 完整人物立牌**（B5 表现层资产方向，与 §11 立牌规格衔接）
- 延奏/契约图标仍待拍板（当前按角色 UnitData.skills 的 SkillData.icon 数据驱动）；~~移动图标随常态切换~~ → **已实现（2026-09-18，待目检）**：按 UnitData.normalMoveType 数据驱动切换现成图标 walk/fly/amphibious
- ~~执行阶段 HUD 变化~~ → **首版已实现（2026-09-18）**：技能盘收起（DeselectUnit）+ 攻速队列条高亮当前攻速片行动者（其余降透明）；"变灰/进度条化"样式待目检再调
- ~~头顶血条常驻样式与伤害数字偏移~~ → **已实现（2026-09-18）**：UnitView 头顶常驻血条（我方绿/敌方红，TeamType A/B——B7 联机按 viewer 归属重定）+ 世界空间 TMP 单位名（TextCombiner 本地化）；伤害数字随机偏移 ±0.18 + 起手弹跳放大
- **素材复用优先**：HUD 图标/头像/卡面/元素环一律先扫项目 Resources（Avatars/Cards/Skills/Element/Faction），不做外部提取或 AI 生成（用户 2026-09-18 拍板）
- 实现位置：正式 HUD 为 BattleScreen 场景 Canvas 子树（**灰盒 BattleDebugPanel 已于 2026-09-18 用户拍板撤除**——正式 HUD 顶栏/队列条落地后无并存必要；其"播放速率/即时行动插入"调试能力暂失入口），面板制纪律走 docs/23（池化/IClosable 不适用于战斗内 HUD 常驻件，仅弹窗类如详情面板按 Screen/Popup 分流——B6 落地时定层级）；**输入 = 手势层接入**（docs/24 §7.11：Battle 面实现 IGestureSurface 挂 TapRecognizer/拖拽识别器，点击选立牌/瞄准走统一手势仲裁，B6 落地时接线）

### 2026-09-18 落地增量（HUD 完善 + B5 表现层，全部复用现有体系）

- **选择时限（docs/04 §4.2）**：TurnFlowController 按公式计时（第 1 回合 25s / 2~6 回合 16s / 第 7+ 回合 −0.5s 每回合、下限 8s），**超时未交玩家自动上交 Pass**（超时行为 docs 未定义、按自然默认实现——**待用户拍板确认**）；附带修复 OnSubmitAction 对 Pass 豁免单位校验（此前 null unitId 的 Pass 会被"未知单位"忽略，AI 无可用单位时的 Pass 同受益）
- **HUD 顶栏补全（定案布局 vs 首版差距）**：双方信息块（势力徽标=ElementFactionConfig.GetFactionIcon ← **BattleMapData.faction 新协议字段**（B7 客户端无 mapConfig 资产仍可得）+ 队营色 accent + 体力/摩拉 chip=现成 Items/stamina、mora 图标；**协议核心血条留 B8**——核心未 Unit 化，不画假数据）；设置按钮图标修正（mondstadt_frame 占位 → Buttons/settings_button）；攻速队列条（UnitConfig.avatar 头像+攻速数字+队营色框，选择阶段=执行预览/执行阶段高亮当前片）
- **HUD 文案全 TextCombiner 本地化**：UIText 新增 12000 战斗段 13 键（12001~12013，五语言+CSV 同步）；阶段名/提示条/手牌/队列 label/回合 label 走新键，取消=复用 Cancel（9018）、移动/战技/爆发/延奏=复用 SkillType 表（Move=1/Normal=2/Burst=3/Enso=6）；动态数字=SetSingleEntry 静态条目（先例 BackpackScreen.Deck）
- **B5 两态模型落地（§11 实现要点）**：BattlePlayer.BindFlow 订阅阶段事件——Selecting→散开（GetFormationOffset 复用为展开布局）/Resolving→收拢重叠格心；RefreshAllFormations(spread) 化；片播放结尾按当前态重排；OnSegmentPlaying(sliceAttackSpeed) 事件
- **音效缺口登记**：项目无 UI 点击/确认类 SFX 资产（PlaySFX 仅抽卡/加载页两处用；Audios/ 无对应素材）——战斗 SFX 是否生成/提取**待拍板**（受"素材复用优先"拍板约束）
