# 16 - 代码优化计划

> 2026-08-14 制定。目标：**不改游戏行为**的前提下系统性清理技术债，让后续开发（尤其战斗系统）不用反复返工。
> 执行方式：分批次，**每次对话只做一个批次**；每批次以「编译零错误 + 用户自测通过 + git commit」收尾，再进入下一批。

## 原则

1. **不改行为**：纯结构优化。不新增功能、不改数值、不改 UI 表现（明示的 bug 修复除外）。
2. **每批独立验收**：`start_compilation_pipeline` 后 console 零错误零警告（新增），用户手动测试涉及的界面流程。
3. **不使用 asmdef**（2026-08-01 决定）：保持单一程序集 + 命名空间隔离。
4. **战斗核心不在本计划**：ActionQueue+DamagePipeline、Buff 体系、技能结算属新功能开发，落地时另行设计；本计划只做不依赖战斗核心的部分。
5. **遵循既有模式**：DI 用 [Component]/[Autowired]、UI 本地化用 TextCombiner、客户端→Host 请求事件 Immediate=true 等（见项目记忆与 skills）。

## 现状盘点（2026-08-14，共 ~204 文件 / ~31,400 行）

| 模块 | 文件 | 行数 | 主要问题 |
|---|---|---|---|
| UI | 55 | 9,980 | SettingsScreen 692 行 god class；CoopScreen 疑似无 OnDestroy；CharacterPanelController 3 处 TextCombiner 违规；货币查询逻辑在 Wish/Backpack/MainHall 三处重复 |
| Framework | 46 | 7,980 | `Wargame.Instance.Xxx` 遗留访问 80 处/30 文件；PlayerManager god class（11 个内嵌 Handler）；GameScene 731 行双加载路径并存；EventBusHub/NetworkEventBus 回指 Wargame.Instance 成环 |
| Data | 46 | 4,590 | 基本健康（CardConfigResolver/Adapter 职责单一）；PlayerSaveData.ownedCards 手工重建缓存易漏 |
| Editor | 13 | 4,105 | Unit/Item/Position 三个 ConfigEditor 重复同一套「ReorderableList+右键复制粘贴+独立窗口」模式 |
| Battle | 33 | 2,692 | 组件骨架合格但无核心执行（属战斗落地范围）；Unitfactory.cs 文件名与类名 UnitFactory 大小写不一致；两个 CardViewStrategy 重复 |
| Tool | 11 | 2,099 | LightPillarEffect 536 行偏大（四套特效挤一类） |

横切问题：
- **Debug.Log 172 处**，Release 仅靠 GameScene 全局 `filterLogType` 抑制——字符串插值在 IL2CPP 下仍会执行，存在真实性能损耗。
- **三种单例取法并存**：`Singleton<Wargame>`（唯一使用者）、`Wargame.Instance`（80 处）、`GameScene.Instance` / `EventBusHub.Instance`（MonoBehaviour 静态）。
- **`IS_DELETION_TEST_MODE = true`**（SaveManager）——删档开关默认开启，上线忘改即清空玩家存档，高危。
- 亮点（无需动）：DI 容器、Wish 三层分离（FlowController/Manager/表现层）、PlayerManager 的 Cleanup 退订范例、SkillFactory 反射注册。

## 批次计划

顺序 = 依赖顺序（基础层 → Framework 大文件 → UI → Editor/Tool）。每批预估 1 次对话。

### P1 快速止血〔小〕✅ 2026-08-14 完成（编译零错误，待用户自测）
彼此独立的小修与高危项：
1. ✅ SaveManager：删档测试模式改为「仅编辑器 + Tools/存档/删档测试模式 菜单显式开启（EditorPrefs）」，打包恒关（新增 SaveDeletionTestModeMenu.cs）
2. ✅ CoopScreen：**核验后确认原本就有完整 OnDestroy**（UnregisterClosable+PopAll+三组退订+CancelInvoke），探索报告误报，无需修改
3. ✅ CharacterPanelController：3 处直接赋值改 `EnsureTextCombiner(...).SetSingleEntry(...)`（GetEntry 系列本就返回 TextEntry，直接对接）
4. ✅ 清理残留：SkillIconView 两处调试日志删除 + toggle 退订补齐；PopupManager 空 Awake 删除；顺带修复 OnToastComplete `index==-1` 越界隐患（早退守卫）
5. ✅ `Unitfactory.cs` → `UnitFactory.cs`（git mv 含 .meta 保 GUID，静态类无场景引用，且当前零调用为战斗预留）+ 加载/创建双层空判
6. ✅ PlayerSaveData：新增 `AddOwnedUnit/AddOwnedItem` 统一入口（自动失效缓存）；修复真 bug——WishManager.AddStarglitter 首次新建星辉卡漏调 RebuildOwnedCards 导致 ownedCards 缓存过期

验收：编译零错误；自测联机房间进出、祈愿角色面板、背包增删卡后存档正确。

### P2 日志系统统一〔中〕✅ 2026-08-14 完成（编译零错误，待用户自测）
1. ✅ 新增 `Framework/GICLog.cs` 静态门面（旧版已被删，本次重写）：
   - `Info` 用 `[Conditional("UNITY_EDITOR")]` + `[Conditional("GIC_LOG")]` **编译期删除**（含调用点字符串插值，打包零 GC）
   - `Warn/Error/Exception` 恒输出；真机 verbose 排查时在 Player Settings 加 `GIC_LOG` 宏（opt-in，忘加只少日志无副作用）
   - 实现踩坑记录：`using System.Diagnostics` 与 `UnityEngine.Debug` 产生 CS0104 二义性 → 用完全限定 `[System.Diagnostics.Conditional]`（这可能正是旧版的 bug 类型）
2. ✅ 批量替换运行时目录（Framework/UI/Battle/Tool/Data）222 处调用、42 文件，脚本自动补 using（2 个文件）、保留 BOM
3. ✅ GameScene 的 filterLogType hack 收编为 `GICLog.ConfigureForRelease()`（GameScene 调用处保留 #if !UNITY_EDITOR 分支）：本 Mirror 版本无 LogFactory 分级 API，Release 仍需全局过滤压制其内部 Info 刷屏；GIC_LOG 宏定义时自动跳过压制
4. Editor 目录日志不动（P11 处理）

验收：编译零错误；Editor 日志行为不变；下次出包验证 logcat 只剩 Warning+。

### P3 依赖访问统一——去 Wargame.Instance 化〔大，拆两批〕
目标：80 处/30 文件的 `Wargame.Instance.XxxManager` 全部改为注入；EventBusHub/GameScene 静态单例同步收敛。
- **P3a Framework+Data+Battle+Tool 域** ✅ 2026-08-14 完成（编译零错误 + 启动冒烟通过，待用户自测）：manager 级直达清零，剩余 10 处均为容器级引导（`Context.Inject(this)` 惯例 + GameScene 组合根 Init/Start/Update/读存档设置）。具体：
  - InputLocks 静态门面改**后端注册制**（InputManager [PostConstruct] 注册，新增 HasLock 门面）；GameScene 场景切换守卫改走门面
  - CardConfigResolver 增加**静态 Instance**（镜像 StarVisualConfig.Initialize 先例，ConfigManager.Init 注册）——SaveCardData.Config 与两个卡牌视图策略改用
  - CardDeck/Card/CardManager：构造注入 SaveManager；Card（MonoBehaviour，运行时实例化晚于容器就绪）Awake 注入
  - AudioManager/EventBusHub/NetworkEventBus：**懒注入模式**（`[Autowired]` 字段 + 首次使用时 `Context?.Inject(this)`，解决与 GameScene Awake 顺序无保证的时序问题）；EventBus 回指环已解除
  - MyNetworkManager/MyNetworkDiscovery：补 [Autowired] SaveManager（Awake 已有 Inject）
  - ConfigManager：删除 6 个无调用者的 Service Locator getter（死代码）
- **P3b UI 域** ✅ 2026-08-14 完成（编译零错误 + 启动冒烟通过，待用户自测）：48 处全清。Screen 类加 `[Autowired]` 字段（Awake/Start 注入）；弹窗 prefab（PopupDialog/InputPopupDialog）Awake 注入 + Show 时幂等补注入；KeyBindingSettingItem 懒获取注入；DetailPanel×2 改 `CardConfigResolver.Instance`；WishScreenDraw partial 加共享注入字段；MainHallScreen/SplashScreen/CharacterPanelController 注入 AssetCache/UnitConfig/PositionManager 等 bean
- ✅ **Wargame.cs 10 个兼容属性已删除**（编译逼出 2 个漏网点：CharacterPanelController 变量名残留、GameScene 读档直达）并修复；GameScene 作为组合根保留 Init/Start/Update 引导 + `Context` 属性 + 自身 [Autowired] 注入
- `Singleton<T>` 基类暂保留（Wargame 仍继承，删除属可选清理，收益低）

验收：编译零错误；全流程冒烟（启动→主厅→联机→祈愿→背包→设置）。

### P4 EventBus 生命周期加固〔中〕✅ 2026-08-14 完成（编译零错误 + 启动冒烟通过，待用户自测）
盘点结论：现有订阅点全部配对退订（PlayerManager 11+11、Backpack 3+3、MainHall 3+3、DeckChoose/ItemCategory 1+1、测试 1+1），真实风险是「未来新增靠自觉 + 样板冗余」。
1. ✅ LocalEventBus 新增 **owner 登记制**：`Subscribe(handler, owner)` 重载在 HandlerInfo 记录 owner；`UnsubscribeAllForOwner(owner)` 一行退订该 owner 全部订阅（同步清理空类型列表 + 队列中已无处理者的事件标记完成防 Tick 空转）；返回移除数量便于排查漏传 owner
2. ✅ EventBusHub 透传 `Subscribe(handler, owner)` / `UnsubscribeOwner(owner)`
3. ✅ 迁移全部 6 个订阅方（PlayerManager.Cleanup 11 行→1 行、BackpackScreen/MainHallScreen/DeckChooseView/ItemCategoryView/EventBusFrameRateTest 同理）
4. 原 `Unsubscribe<T>(handler)` 精确退订 API 保留（owner 为 null 的订阅仍走它）
5. 「废弃区」核实为事件取消功能（CancelEvents/ClearAllWaitingEvents，EventBusHub 公共 API 一部分），非死代码，保留

### P5 GameScene 收敛〔中〕✅ 2026-08-15 完成（编译零错误 + 启动冒烟通过，待用户自测）
通读后修正认知：「重复样板」其实已共享（SetActiveSceneAfterLoad 两处调用）；真正的问题是**死路径**与**转发职责**。
1. ✅ 删除死代码（-161 行）：`LoadNewRootScene(+Coroutine)`（被 PreloadScene+Activate 路径取代）、`UnloadSceneAsync(+WithEventsCoroutine)`+`SceneType.Unload()`（零调用者，事件零订阅者）、`DelayedCleanup`、`cleanupDelay` 字段、`currentLoadCoroutine` 死字段、公共查询 API 4 个（IsSceneLoaded/TryPeekPreviousScene/GetHistoryCount/SetCurrentScene/PopHistory，留 HasPreviousScene/ClearHistory——GoBack 流程用）
2. ✅ `OnSceneWillUnloadEvent` 事件类删除（发送者与订阅者均无）
3. ✅ `SceneType.GetAllScenes()` 删除（零调用且漏了 Backpack/Settings 两场景，本身有 bug）
4. ✅ 新增 `Framework/SettingsApplier.cs`：语言/帧率/分辨率应用从 GameScene.InitSaveSettings 剥离（45 行→1 行调用）；GameScene 保留组合根职责（Wargame Init/Start/Update 引导，P3a 决策）
5. ✅ 弹窗/轻提示收敛：PopupManager 增加 `Instance` 静态入口（Awake 注册，AudioManager 同款模式），7 个 UI 文件 10 处调用改直连；GameScene 删除全部转发方法
6. GameScene 737→579 行；存活的场景路径：LoadSceneWithConfig（Single 场景）、PreloadScene+ActivatePreloadedScene（Additive 预加载，MainHall 用）、GoBack（历史栈返回）

验收：编译零错误；自测场景切换/返回/预加载路径与现在一致（场景闪烁不回退）。

### P6 PlayerManager 拆分〔中-大〕✅ 2026-08-15 完成（编译零错误 + 启动冒烟通过，待用户自测）
最终采用「数据层 / 流程层」单向依赖（RoomManager → PlayerManager），而非对等拆分：
1. ✅ 新建 `Framework/Network/RoomManager.cs`（[Component]，构造注入 SaveManager+PlayerManager）：连接/断开流程（HandleServerConnect/Disconnect）、11 个网络 Handler、颜色分配（GetNextAvailableColor 私有化）、`OnKickedFromRoom` 事件、Cleanup（owner 退订）。事件归属：名册数据事件（OnPlayerCountChanged/OnPlayerInfoUpdated）留在 PlayerManager 随数据变更触发；被踢事件归 RoomManager（随 Handler）——无需中继
2. ✅ PlayerManager 瘦身为纯数据层（633→约 300 行）：名册 `_allPlayers` + SelfPlayerID + Register/Unregister/Update + Set 系列 + 全部查询；不再依赖 SaveManager/Mirror 事件；删除 `IsOwnUnit/CanControlUnit`（零调用者，战斗系统落地时在 Battle 层重建，UnitIdentity 已有 IsEnemy/IsAlly）、死代码 `HandleClientDisconnect`（零调用者）；不再实现 IWargameManager（无生命周期逻辑），移出 managers 列表
3. ✅ 调用点同步：MyNetworkManager 注入双 Manager（连接流程→RoomManager，SelfPlayerID/ClearPlayers→PlayerManager）；CoopScreen 数据事件订 PlayerManager、被踢订 RoomManager；CoopNetworkController 删除存而不用的 PlayerManager 死字段；Wargame 注册顺序 PlayerManager→RoomManager
4. ✅ **顺手修复重连失联 bug**：原 `MyNetworkManager.OnClientDisconnect` 调用 `PlayerManager.Cleanup()`（UnsubscribeOwner 一行退订全部 11 个 Handler），但订阅只在 `[PostConstruct]` 执行一次 → 同一次 Play 内离开房间后再加入/再建房，网络 Handler 全部失联、名册不再同步。修复=断开时只 ClearPlayers 不退订（Handler 常驻订阅，CanHandle 已有 NetworkServer.active / Source==Network 守卫，断线时天然空转）；RoomManager.Cleanup 保留为应用关闭钩子（当前无调用者）

验收：编译零错误；联机全流程（建房/加入/准备/踢人/改名/换队换色 + **同会话内退出后重进**）自测通过。

### P7 Screen 基类与 UI 公共服务〔中〕✅ 2026-08-15 完成（编译零错误 + 启动冒烟通过，待用户自测）
1. ✅ 新建 `UI/Screen/ScreenBase.cs`（abstract，实现 IClosable）：
   - `protected virtual void Awake()` 统一注入（Boot 链路容器就绪，Awake 注入安全；子类重写须 base.Awake()）
   - `RegisterClosableSelf()`（基类私有 [Autowired] InputManager，子类不再自带字段）
   - 音乐幂等管理 `PushMusicVolumeSafe/PushMusicStateSafe/PopMusicSafe`（标志位防重复 pop）
   - `CloseScreen(Func<IEnumerator>)` 标准关闭模板：防重入 + Closing 锁 + 音乐恢复 + 退场协程 + GoBack 统一收尾
   - `OnDestroy` 四件套兜底：UnregisterClosable + PopAll + **UnsubscribeOwner**（EventBus 一行退订）+ 音乐 pop
2. ✅ 6 个弹层 Screen 迁移继承（Map/Backpack×5 partial/Settings/Splash/Wish×2 partial/Coop），各关闭协程尾部 Pop+GoBack 剥离进模板；MainHall 根场景不继承（非弹层不可关闭，保持独立 OnDestroy）
3. ✅ **顺手修两个真 bug**：①地图锚点传送路径（MapAnchor→CloseWithFade）原本缺防重入守卫、缺 Closing 锁、**缺音乐 pop（传送回大厅后 BGM 音量永久压低）**——统一走 CloseScreen 模板后修复；②各 Screen 被强卸载（非 Close 路径销毁）时音乐泄漏——基类 OnDestroy 幂等 pop 兜底
4. ✅ 货币/物品计数统一到 PlayerSaveData（沿 P1 AddOwnedItem 模式，数据层查询而非 [Component] 服务——重复实际只有 Wish 两处，建服务属过度设计）：新增 `GetItemCount(ItemName)` / `AddItemCount(item, amount)`（不存在则新建）/ `TryConsumeItem(item, count)`；WishManager.ConsumePrimogem/GetPrimogemCount/AddStarglitter 与 WishScreenDraw.UpdateFateCount 四处遍历循环全部改走统一入口
5. ✅ 场景跳转收敛项核实为**无需改动**：P5 后 `SceneType.Load()` 是唯一入口（内部走 GameScene.LoadSceneWithConfig），全仓 SceneManager 调用只在 GameScene 内部
6. ✅ gic-new-screen skill 骨架代码回写为 ScreenBase 继承版

验收：编译零错误；各 Screen 进出/ESC 关闭/返回行为一致；**重点自测：大地图锚点传送后 BGM 音量恢复正常**、祈愿原石/星辉计数正确、背包进出音乐正常。

### P8 SettingsScreen 拆分〔中〕✅ 2026-08-15 完成（编译零错误 + 启动冒烟通过，待用户自测）
692 行单文件拆为 7 个 partial（沿 BackpackScreen 模式，纯机械搬运不改逻辑）：
1. ✅ 拆分结果：主文件 147 行（全部 Inspector 字段 + Start + InitLocalizedTexts + Close）/ Animation 150（入场退场动画 + 动画缓存字段）/ Navigation 93（面板切换 + 选中高亮 + InitSelectPosition + 颜色常量）/ Display 129（语言/分辨率/帧率）/ Audio 39（音量滑条）/ Control 26（按键绑定）/ Account 58（账户 + 输入弹窗）
2. ✅ 顺手清理两处冗余：Start 中重复的 RegisterClosableSelf() 调用（RegisterClosable 本身幂等，删第二次调用零行为差）；PlayEnterAnimationCoroutine 结尾一段错位缩进还原（原文件缩进误导但语法正确，纯格式）
3. ✅ 指令弹窗 TODO（原 L567）随 Account partial 迁移，保留不实现
4. ✅ Unity 序列化安全：类名/字段名不变，场景引用按字段名匹配不受影响（冒烟验证）

验收：编译零错误；设置五个页签切换/修改语言/分辨率/帧率/音量/改键/改名自测通过。

### P9 CoopScreen 拆分〔中〕✅ 2026-08-15 完成（编译零错误 + 启动冒烟通过，待用户自测）
563 行拆为 4 个 partial（沿 Backpack/Settings 模式，纯机械搬运不改逻辑）：
1. ✅ 拆分结果：主文件 181 行（Inspector 字段 + Awake/Start/Update/OnDestroy + 事件绑定 + EnsureTC）/ RoomFlow 141（RoomState 状态机 + RefreshUI + 建房重试 WaitForHostStart + 离房/被踢/网络回调 + ReturnToDiscovery）/ PlayerList 66（OnPlayerCountChanged/OnPlayerInfoUpdated + RefreshPlayerList + UpdateRoomPanel）/ Discovery 129（房间发现 + 服务器列表按钮 + 空提示 + 地图下拉）
2. ✅ 职责对齐评估结论：CoopNetworkController 已承载网络发现/连接原语（P6 清过死字段），RoomState 状态机与 UI 刷新强耦合（SetRoomState→RefreshUI→PlayerList/Discovery 全链路）不下沉，保持 Screen 内 partial 分层；联机流程"下沉"诉求已由 P6 RoomManager（Framework 层）解决，本批只做 UI 内职责分文件
3. ✅ WaitForHostStart 的 Invoke 重试（nameof 跟方法迁移）、ReturnToDiscovery 的 wasAlreadyDisconnected 防重入、OnStartClick 全员就绪空校验（TODO 注释标明待战斗场景）均原样保留
4. ✅ Unity 序列化安全：类名/字段名不变，场景引用不受影响

验收：编译零错误；联机全流程自测（建房/加入/离房/被踢/返回/发现刷新/地图下拉）。

### P10 Skill/Card 详情视图解耦〔中-大〕✅ 2026-08-15 完成（编译零错误 + 启动冒烟通过，待用户自测）
1. ✅ **去克隆链路**：SkillDetailView 删除运行时 `Instantiate(cardDetailViewTemplate)` + 手工重接线 + `ClearCardDetailClone` 全套；`cardDetailViewTemplate` 字段改为 `cardDetailView`（场景 CardModeContainer 下静态预设的嵌套实例，默认不激活）。ShowCardMode 简化为 `cardDetailView.Init(saveData)`（Init 幂等重置：重建标签芯片/技能图标/子面板切换）。场景编辑：深拷贝主 CardDetailPanel 实例生成嵌套面板（含场景级子物体 UnitDetailPanel/ItemDetailPanel，内部引用自动重映射）并接线，同时清掉 PrefabInstance 上残留的旧字段覆盖
2. ✅ 「链接跳转/IsPointerOverLink/UI/Rect 判定抽公共」核实后**仅 SkillDetailView 一处使用**（P3-P9 演进中重复已自然消失），无需抽公共；顺手把 `IsPointerOverSourceIcon` 并入 `IsPointerOverUI`（逻辑完全一致）
3. ✅ UnitCardViewStrategy/ItemCardViewStrategy 抽公共基类 `CardViewStrategyBase`：InitCommon（Init 尾部 3 行）、GetTotalSkins（完全一致）、ApplySkinTo（写 skin+取图）收敛进基类，子类只留差异分支
4. ✅ SkillIconView toggle.onValueChanged 退订核实为 **P1 已修**（OnDestroy RemoveListener 存在），无需动
5. 过程陷阱：prefab 源 InstantiatePrefab 的实例**不含场景级子物体**（CardDetailPanel 的 UnitDetailPanel 是场景 override），跨场景预设须 `Object.Instantiate(场景实例)` 深拷贝；诊断脚本 LoadAssetAtPath 整 prefab + 递归遍历层级曾把编辑器主线程卡死（330s 超时），重启丢弃未保存 additive 场景即恢复

验收：编译零错误；背包卡牌详情/技能详情/技能描述内链接跳转（Unit:→卡片模式 / Concept:→规则模式）自测通过。

### P11 Editor 工具 UI Toolkit 重写〔中-大，2026-08-15 用户决策由「抽 IMGUI 基类」改为 UITK 重写〕✅ 2026-08-15 完成（编译零错误，待用户自测）
1. ✅ 新增 `Editor/ConfigEditorUITK.cs` UITK 公共层：
   - `ConfigEditorUITK.CreateList`：ListView 经 bindingPath + 根 Bind 绑定 SerializedProperty 数组，底部 +/- 增删与拖拽排序由绑定系统写回（可 Undo）；行摘要 + 行内「编辑」按钮（OnEdit 为空时隐藏）+ 行右键菜单（编辑/复制元素/删除元素/Copy Element/Paste Element，EditorJsonUtility；旧版对类元素恒为空操作的标量 Copy/Paste 菜单项不再保留）
   - `ConfigInspectorBase`：CreateInspectorGUI（列表 + 可选 BuildTools 工具区）
   - `ElementDataEditorWindowBase`：Item/Position 共用编辑窗口（全字段滚动）。**保存语义变化**：PropertyField 绑定后修改实时写入 SerializedObject（Ctrl+Z 可撤销），「保存到磁盘」按钮负责落盘（SetDirty+SaveAssets）；关闭时「未保存更改」弹窗取消
   - GetWindow 复用已打开窗口时需 `rootVisualElement.Clear() + BuildUI()` 手动重建（CreateGUI 只在首次构建 visual tree 时触发一次）
2. ✅ 三个编辑器重写：UnitConfigEditor（列表 + 语音/图片批量填充逻辑原样搬运）；UnitDataEditorWindow 手写分栏（左=单位属性排除 skills，右=技能 ListView + 选中技能 PropertyField 编辑区；customParams 走默认数组绘制——旧 customParamsListWithMenu 从未被渲染，属死 UI 不重建）；ItemConfigEditor + ItemDataEditorWindow；PositionConfigEditor + PositionDataEditorWindow；ElementDrawer 双模式（IMGUI OnGUI 保留 + UITK CreatePropertyGUI）
3. ✅ Editor 目录日志全量走 GICLog（8 文件脚本替换，BOM/CRLF 保留，QuickAPKBuilder 补 using GIC.Framework）
4. ✅ 删除 `Editor/Tool/SerializedPropertyContextMenu.cs`（旧 IMGUI 公共层，重写后全仓零引用）
5. Tuanjie UITK 差异记录：EditorWindow.CreateGUI 为按名调用的魔法方法（非 virtual，不可 override）；PropertyDrawer.CreatePropertyGUI 签名为单参数（无 GUIContent）；UIElements.Cursor 无 MouseCursor 构造函数（分栏拖拽的光标样式省略，不影响功能）；IStyle 无 borderRadius 简写（用四角属性，公共层封装 SetBorderRadius）
6. ✅ 验证通过后美化（用户授权不必对齐旧 IMGUI 样式）：行改为 [徽标]名称★星级 结构（徽标按文本哈希稳定取色 6 色盘，星级金色）；列表交替行背景；分区标题条（蓝色竖条+粗体）；窗口标题行带金星（Unit/Item）；底部统一页脚工具条（灰色提示+主色"保存到磁盘"按钮，替代 HelpBox）；"清空所有位置的音乐"红色危险按钮；Unit 窗口分栏条圆角+悬停/拖拽高亮。Inspector 契约改为 GetElementName/GetElementBadge/GetElementStars。中文字重经验：编辑器中文走回退字体无真 Bold，合成粗体导致"笔画疏的字显瘦"，全部改字号+亮度+竖条表达层级（Editor 目录 FontStyle.Bold 清零）
7. ✅ 其余 4 个 IMGUI 工具窗口统一 UITK 化（用户要求"全部统一"）：TMPFontReplacer（字段加 [SerializeField] 重编译保留选择）、ElementFactionIconAutoLoader、ItemConfigImageTool（顺手修隐性 bug：旧 TextField 传常量致路径编辑永不生效，改序列化字段；操作后统计/列表刷新）、LocalizationCsvTool（表列表条目数原每帧查询 21 张表，改为打开/导入后/手动"刷新"时重建；新增 CreatePrimaryButton 主操作按钮）。业务逻辑全部原样保留

验收：编译零错误；三个 Config Inspector/窗口自测（增删改/右键复制粘贴/拖拽排序/技能选中与分栏拖拽/批量填充/保存到磁盘）。**重点确认 ListView 绑定在 Tuanjie 下 +/- 与拖拽确实写回资产；若无效，回退方案是在公共层补 onItemsAdded/onItemsRemoved/itemsReordered 手写回调 + ApplyAndRefresh 刷新**。

### P12 Tool 组件整理 + 菜单栏整理〔小-中〕✅ 2026-08-16 完成（编译零错误零警告，待用户自测）
1. ✅ LightPillarEffect(536 行) 拆分为 4 个 partial：主文件（全部序列化中文字段+生命周期+Play/Stop+PlaySequence 编排+FadeOutAndClear+CreateImage 辅助，Inspector 零变化）、Animations（AnimateBeam/AnimateGroundFlash）、Sparks（SpawnSparks/AnimateSpark）、Resources（共享纹理/Sprite 静态字段+CreateTextures/CreateMaterial+EaseOutCubic）
2. ✅ `WaitForSeconds` 缓存：新增 `Framework/Wait.cs`（`Wait.Seconds(float)`，毫秒精度 int key 字典，微小浮点差异不膨胀缓存），12 文件 20 处 `new WaitForSeconds(` 全部替换（WaitForSecondsRealtime 属 unscaled 动画不在范围，保留）
3. ✅ MainHallScreen(524 行) 拆为 3 partial：主文件（全部 Inspector 字段+生命周期+事件处理器+按钮回调+场景退出预加载）、Animation（入场/退出交错动画+按钮列表初始化+交互开关）、Background（UpdateBackground+地址字段）
4. ✅ TextCombiner 仅做子方法整理，不拆文件：提取 `RegisterEntryCallback(index)`（4 处注册重复收敛）/`DetachAllHandlers()`（3 处退订重复收敛）；SetSingleEntry(TextEntry) 改为 ClearAllEntries+AddEntry 复用（行为等价）；删除 Awake 中空 if 死代码与调试 Info 日志
5. ✅ **菜单栏整理**（用户追加需求，"有些放入对应 config 底下"）：
   - 全部 Tools 菜单项显式 priority，最终顺序：快速打开场景(-100..-92) → 编辑器启动(-90..-88) → ItemConfig(-80) → 图标Config(-79) → Localization(-78) → 存档(-77) → 测试(-76) → 导出 APK(-70..-68) → 批量替换 TMP 字体(-20) → 查找丢失的脚本引用(-19) → Setup URP(-18)（-68→-20 大间隔自然形成分隔线，杂项压底）
   - `Tools/图标/` → `Tools/图标Config/`（对应 ElementFactionIconConfig 资产，与 ItemConfig 命名风格一致）
   - EditorPlayPreferences 6 项开/关对收敛为 3 项勾选式切换（与存档/删档测试模式同模式）
   - WishProbabilityTest.cs 从 Framework/Test 移到 Editor/Tool（纯编辑器工具归位，git mv 保 GUID，namespace GIC.Framework→GIC.Editor）
   - Localization/存档/导出 APK 菜单路径未变（skill 与记忆引用不受影响）

验收：编译零错误零警告；祈愿光柱特效/主厅进出动画与背景切换/各 UI 动画自测通过；菜单栏确认新顺序与勾选式开关行为。

## 战斗系统批次（本计划之外，落地时另行设计）

- B1: ActionQueue（协程串行）+ DamagePipeline（同步+Phase hooks）架构落地
- B2: Buff 体系最小可用（生命周期/tick/堆叠；现 BaseBuff 为空壳）
- B3: Framework/Battle 四管理器与 Battle 目录的职责边界重定（Skill/Unit 实例的创建-持有-执行归属统一）
- B4: 技能执行结算打通后的示例技能与验证

## 执行约定

1. 每批次开始：重读本计划对应条目与相关项目记忆/skill；结束：在本文档勾选完成状态
2. 过程：读码 → 小步修改 → `start_compilation_pipeline` → `unity_console get` 清零
3. 收尾：用户自测涉及流程 → 确认后 git commit（沿用 `refactor:`/`fix:` 前缀中文描述风格）
4. 发现本计划未覆盖的新问题：小则顺手修并在 commit 说明，大则记入本文档「新增发现」节，不扩当前批次范围

## 进度总览

| 批次 | 状态 | 提交 hash | 日期 |
|---|---|---|---|
| P1 快速止血 | ✅ | d9a422d | 2026-08-14 |
| P2 日志统一 | ✅ | c6744b4 | 2026-08-14 |
| P3a Framework DI | ✅ | fdf0a42 | 2026-08-14 |
| P3b UI DI + 删兼容属性 | ✅ | aae0c2c | 2026-08-14 |
| P4 EventBus owner 登记 | ✅ | 0bd0ba4 | 2026-08-14 |
| P5 GameScene 收敛 | ✅ | 3c28912 | 2026-08-15 |
| P6 PlayerManager 拆分 | ✅ | c6b0980 | 2026-08-15 |
| P7 Screen 基类与公共服务 | ✅ | cf39227 | 2026-08-15 |
| P8 SettingsScreen 拆分 | ✅ | 44b4083 | 2026-08-15 |
| P9 CoopScreen 拆分 | ✅ | c5bf862 | 2026-08-15 |
| P10 Skill/Card 视图解耦 | ✅ | 324a834 | 2026-08-15 |
| P11 Editor UITK 重写 | ✅ | 5cbf73b | 2026-08-15 |
| P12 Tool 整理 + 菜单栏 | ✅ 待自测 | — | 2026-08-16 |

### 下一对话交接

**P12 已完成待用户自测**（编译零错误零警告）。自测通过后 git commit，P1-P12 全部收官；后续优化属战斗系统批次（B1-B4，见下节），落地时另行设计。

注意事项：
- 本次涉及用户可感知变化：Tools 菜单顺序/图标→图标Config 改名/编辑器启动 3 项勾选式；WishProbabilityTest 移动到 Editor/Tool（若用户有旧菜单习惯需提示）
- 工作区遗留用户自改未提交（勿混提交）：WishPool_Mondstadt.asset（star5Weight）、WishDrawController.Video.cs、mc_16x10.mp4、UnitConfig.asset、ItemConfig.asset

## 工作区遗留（非优化计划内，2026-08-15 用户自己改动，未提交）

- `Assets/Resources/Configs/WishPool_Mondstadt.asset`：star5Weight 1→100（用户调卡池权重）
- `Assets/_Scripts/UI/Screen/Wish/WishDrawController.Video.cs`：4 行改动（用户自查视频逻辑）
- `Assets/StreamingAssets/mc_16x10.mp4`(+meta)：测试视频，去留由用户定
- 以上不属 P1-P12 范围，P11 批次开始前不要动它们，也勿混入提交
