# 代码架构指南

> 2026-08-16 创建。一页式代码地图：新会话（人或 AI）先读本文建立全貌，再按需深入各专项文档/skill。
> **编码/UI/本地化/文档/术语等通用规范见 docs/20-项目规范.md（2026-08-29 起为规范权威文档）**。
> 玩法设计见 docs/00-11 与 docs/units/；祈愿/大地图实现见 docs/12、docs/13；输入系统详见 docs/15（键盘/锁）与 docs/24（手势层）；指令系统见 docs/25；GG 内置 IM 仅设计见 docs/26；技术陷阱详见 docs/14（含 Tuanjie UITK 陷阱）；优化史见 docs/archive/16（已归档）；AI派蒙桌宠见 docs/19；商业化/发布边界定盘见 docs/21（勿重新论证）；文档总导航见 docs/00-概述.md。

## 1. 项目概况

- **游戏**："协议核心"——同时回合制卡牌战术战棋（原神 IP，最多 6 人，LAN 联机 via Mirror）
- **引擎**：Tuanjie 1.9.3（Unity 2022.3 分支），URP
- **技术栈**：C# 单一程序集（**无 asmdef**，决策背景见 docs/20 §1.2；用命名空间隔离）、Mirror 联机、Unity Localization（6 语言）、Addressables 大资源、Unity UI（UGUI + TMP）
- **目标**：个人 Demo/作品集，非商业上线（scope 原则：2v1 优先 6 人、2-3 势力优先 7、垂直切片优先广度）

## 2. 目录结构（Assets/_Scripts）

**命名空间约定**：namespace 固定为模块根（GIC.Framework / GIC.Data / GIC.UI / GIC.Battle / GIC.Tool / GIC.Editor），目录仅做模块内物理分组，不映射到 namespace——新文件按此写，不要按目录逐级嵌套 namespace。

| 目录 | 命名空间 | 职责 |
|------|----------|------|
| Framework | GIC.Framework | 核心层：Wargame 组合根、GameScene、DI、EventBus、AssetCache、Audio、Save、Input(InputManager/InputLocks/KeyAction/KeyBindingEntry + Gestures 手势层)、Command（CommandSystem 统一指令系统+GameCommandsCore：help/give/card/time）、Network(Player/RoomManager)、Pool、Collection（卡组/收藏域：CardManager/CardDeck/DeckCodeCodec/UnitManager，2026-09-19 B3 归位自 Framework/Battle 迁入） |
| Data | GIC.Data | 纯数据：Config 类（Battle/ 下 Unit/Item/Position/Skill/Board（BattleMapConfig/TileType）/BattlePalette（战斗配色资产 Configs/BattlePalette.asset）/Buff（BuffType）、Card/、Wish/）、战斗协议（Battle/Protocol/：ActionData/BattleCommand/Segment/BattleSnapshot/BattleMapData/传输消息）、事件类（Event/）、玩家数据（Player/）、枚举 |
| UI | GIC.UI | 界面层：Screen/（各弹层场景脚本）、Popup/、Skill/、Common/ |
| Battle | GIC.Battle | 战棋域（B1 已落地）：Unit/、Card/、Skill/、Tile/、Buff/、Flow/（TurnResolver/执行器/DamagePipeline/ElementReactionResolver/BattleSimState/BattleSession/TurnFlowController/AIDebugBrain/BattleLaunchConfig/ProjectileResolver（B5 连续命中判定）/BattleMetrics（判定常量收口））、View/（BattlePlayer/UnitView/BattleBoard/BattleHud 四分件=主+TopBar+Build+Layout/BattleViewFactory/BattleViewTween/BattleDamageNumbers/BattleOverheadBars；操作规范=gic-battle-hud skill） |
| Tool | GIC.Tool | 通用组件与工具：Component/（TextCombiner/LightPillarEffect/ArcLayout…）、扩展方法 |
| Pet | GIC.Pet | 桌宠形态（同 exe `--pet-mode` 双形态）：PetMode 开关、PetHostBase 共用宿主基类（找骨/命中烘焙/拎起姿势/对话装配，2026-08-31 下沉）→ PetWindowController（桌面 Win32）/PetInGameHostController（游戏内 RT）；行为/视线/表情/物理层经 IPetHost 分发（docs/19）。**依赖方向：Pet→Framework/Data/UI 单向**；Framework 反向例外挂点=GameScene 装配 PetIntentIpcHost/PetInGameHost 启动分发（组合根职责，明文豁免） |
| Editor | GIC.Editor | 编辑器工具（全部 UITK，见 gic-editor-tool skill）；DevTest/ 开发用测试脚本（不入包） |

依赖方向（约定）：UI→Framework/Data，Battle→Framework/Data，Editor→全部；禁止反向。UI→Battle 仅限既定桥：开局参数 BattleLaunchConfig（consume-once）+ BattleMapConfig 数据 + BattleHud 复用 SkillDetailView/SkillIconView；战斗组合根现由 BattleScreen 兼任，B7 分端装配前域内化（docs/11「B7 联机准备」节）。

## 3. 核心系统速查

| 系统 | 入口/文件 | 关键约定 | 深入 |
|------|-----------|----------|------|
| DI 容器 | Wargame.Instance.Context | [Component]/[Autowired]/[PostConstruct]；Manager 直达禁止 | gic-di-injection skill |
| 场景/UI 导航 | **UIManager（统一栈）** + SceneType（地址/根加载器） | P1-P4 重构收官（2026-09-13，docs/23）：弹层=池化 prefab 面板（Settings/Backpack/Wish/Coop：UIRoot 四层级容器+预热泵+根转换自动入池）；Map=唯一场景制弹层；根场景 Splash/MainHall/Battle 不入栈。导航=UIManager.Open/GoBack/PopToPrevious；面板打开先等 MainHall 退场动画；SceneType.Load() 仅根场景合法，其余四条目为寻址 token | gic-new-screen skill + docs/23 |
| 事件 | EventBusHub.Instance | owner 登记订阅；请求类事件 Immediate=true；战斗逻辑不走 EventBus | gic-eventbus skill |
| 资源 | AssetCache（[Autowired]） | 大贴图 Addressables 走 AssetCache；Configs 小 prefab 走 Resources | gic-assetcache skill |
| 音频 | AudioManager.Instance | MusicType 优先级；Screen 用 ScreenBase Safe 系列 | gic-audio skill |
| 存档 | SaveManager.CurrentSave | 分区结构（progress/settings/pet，v11 重置式版本）；写盘=原子写+滚动 .backup，读档三级回退 主档→备份→新档；变更统一走 Modify/ModifyNow（变更+自动标脏一步）；初始存货=InitialSaveConfig（SO，含 devTestItems 开发分区：#if 开发态建档发放测试物资、正式版编译剔除）；读档后 EnsureValid 防御修复；写盘失败发 OnSaveFailedEvent→toast | gic-save-system skill |
| 输入 | InputLocks 静态门面 | (owner,reason) 精确配对锁 | docs/15 + gic-input-system skill |
| 手势层 | Framework/Input/Gestures/（**P1-P4 全量收官 2026-09-13**） | PointerInputPump 归一（触摸激活抑制鼠标流）→GestureHub（门1 输入锁/门2 UI 命中/门3 surface 过滤 + first-accept-wins 仲裁，两门均可按面豁免）→识别器（Tap 连击链/LongPress/Drag 三模式+ShortTap 与 OnTapCandidate/Pinch，纯 C# 可离线断言）；常量=GestureMetrics 双档全局统一；surface 能力位 BypassUIGate/IgnoresInputLocks（桌宠 overlay 专用）；桌宠三连击链保持按下驱动+捏合滚轮留本面轮询（docs/24 §7.11 勘定）；Win32 桌宠独立（桌面进程无 Wargame） | docs/24 |
| 指令系统 | CommandSystem（Framework/Command/，**2026-09-13 落地**） | 设置页指令行与 AI 派蒙 run_command 工具共用一套指令（作弊/时间/开界面/自动抽卡/退出游戏/快捷消息编辑）；注册=[GameCommand] 属性反射扫描（新指令即扫即用零接线）；解析=`/` 可选前缀+中文别名+大小写不敏感；IDE 式补全（Tab 补词/↑↓/点击，hint 单行预算约束）；help 目录注册表自动生成；存档变更一律 ModifyNow；桌宠本地指令（start_game/qm）由 PetLocalCommands 在桌宠进程内拦截（游戏没开也有效） | **docs/25** |
| 联机 | PlayerManager(数据)/RoomManager(流程) | 单向依赖 Room→Player；Handler 常驻订阅 | docs/20 §1.2 |
| 祈愿 | WishManager/FlowController/DrawController | PlanShot 预计算 → PlaySteps 纯动画；命运之缘为背包物品（相遇/纠缠之缘，射击自动消耗、纠缠优先；星辉累计 20/200 按跨越档位赠发，详见 docs/12 §4.3） | gic-wish skill + docs/12 |
| 本地化 | TextCombiner 组件 | 禁止直接 tmp.text 赋值 | gic-localization skill |
| 卡组管理 | CardManager + DeckRow.prefab + DeckSwitchPanel | 动态卡组 1~30（常量在 CardManager）；deckNames.Count=卡组数事实源、deckId 稠密恒等（删除时全量重映射 inDecks/order/currentDeck）；卡组密语=DeckCodeCodec "GIC1."+Base64URL（varint ≤8 张）；卡组名上限 10 字；切换条/行内文字不加粗、选中行=整行金色实底 (0.75,0.62,0.32)；本地化 UIText 11000 段；迁移工具=Tools/TG/卡组管理面板迁移（DeckBarMigrationTool，幂等） | docs/14 §27 |
| 背包页签图标 | TabGlyphs/glyph_N.png + disc.png（ItemCategoryView） | 原神式分层：每页签 1 张白 glyph + 公共圆盘；NormalIcon=glyph×灰蓝、SelectIcon=SelectDisc(米白)+SelectGlyph(深蓝灰) 两层运行时染色（PopLayers 同步缩放动画）；换图标=换 glyph PNG 或调三色 Image.color，勿再烘焙颜色进图；迁移工具=Tools/TG/背包页签图标重构迁移（TabGlyphMigrationTool，幂等） | docs/14 §32 |
| 日志 | GICLog 静态门面 | Info 编译期删除；Warn/Error 恒出 | — |

## 4. 场景清单（Assets/Scenes）与面板（Resources/Prefabs/UIPanels）

| 场景/面板 | 类型 | 说明 |
|------|------|------|
| Boot | 根 | 组合根：GameScene 挂载（UIManager 组件+UIRoot 层级容器随之常驻），Wargame 初始化 |
| SplashScreen | 根 | 启动动画 → MainHall |
| MainHall | 根 | 大厅（SpriteRenderer 背景 + UGUI 按钮）；按钮打开面板前先播按钮退场动画 |
| **MapScreen.unity** | 弹层·唯一场景制 | 继承 ScreenBase 入 UIManager 栈，GoBack 返回。3D 地图（MapCamera 正交相机 rotation identity 沿 -Z + XY 垂直画布 SpriteRenderer + 3D 锚点射线点击，交互见 docs/13 与 gic-map skill），UI 仅标题/区域按钮条/关闭按钮/FadeOverlay 遮罩 |
| **SettingsScreen / BackpackScreen / WishScreen / CoopScreen .prefab** | 弹层·池化面板（P3 完成） | 场景已删，四屏为 prefab 面板（预热实例化+打开复用）；生命周期=OnInit 一次性 wiring + OnShow 每开一次；入场/退场动画**全部由公共组件 GlassPanelAnimator 唯一承载**（UI/GlassPanelAnimator.cs，2026-09-13 提取并四屏全量换装——四份手写同配方漂移实证：变暗瞬时+模糊扫入扫出/纯滑动+淡入+§37/§39 纪律内建；挂载=面板根挂+显式元素条目(Settings/Backpack/Wish——Wish 模糊层空=纯滑动+整组淡入、退场系数 1 保 0.2s 等时长)/视图面板挂+自动收集(Coop，BackDim 落面板首子节点)；入场走 ScreenBase.PlayGlassEnter 包装=Entering 锁+isClosing 守卫，退场=CloseScreen(() => animator.ExitRoutine())）；**动画体裁判定（2026-09-13）**：MainHall 按钮交错飞入=stagger 编排体裁（逐按钮延迟+绝对屏外起点+逆序退场+开门编排联动，MainHallScreen.Animation.cs）、Map=相机缩放+UI 淡出+3D 遮罩淡入双路（MapScreen），与面板滑动配方不同族不并入（出现第二例再提取 Stagger/Fade 系组件）；Coop 另有列表↔房间切换过渡编排（CoopScreen.Animation.cs 仅存编排层）+建房等待态（2026-09-13 拍板：反馈上列表页中央文字「正在创建房间…」（CreatingRoom，UIText 9065，复用 EmptyRoomHint）+隐藏房间列表+建房中锁退出（OnLeaveRoomClick 按 _startingHost 拦截 ESC/返回全路径，SetCreatingUI 为标志唯一写点；超时弹 Coop_HostStartTimeout 复位））+开局校验轻提示（ValidateStartConditions：人数超地图出生区上限/出生点冲突（Random 不参与）/颜色冲突/有玩家未准备→PopupText Coop_PlayerLimitExceeded/Coop_SpawnConflict/Coop_ColorConflict/Coop_NotAllReady（Id 16-19，2026-09-14 已装配五语言+CSV 同步））+开局**原神式加载页转场**（SceneFadeOverlay 门面：Cover(BattleMapConfig map, 0.2s) 按所选地图势力换中央徽标+随机词条，**开局即 StopMusic(0.5f) 淡出当前音乐**（2026-09-14 拍板：加载页只留进度旋律；位置曲链随停，回大厅由 PositionManager MainHall 激活钩子复活），**Additive 根切换**（GameScene.SwitchRootScene：新根 Additive 加载+旧根 UnloadAsync 分帧卸载——激活帧卸载风暴根治，2026-09-13 拍板；**嵌套协程宿主必须 GameScene**——挂转场面板会被清栈 SetActive(false) 腰斩致 SceneTransition 锁泄漏；Splash→MainHall/退战回大厅暂保持 Single 无遮盖；BattleLaunchConfig.BuildSinglePlayer+Prepare 只写配置不触发加载），淡入淡出统一 0.2s，15s 安全兜底；Resources/Prefabs/LoadingOverlay.prefab + LoadingOverlayDriver：纯白底+中央势力徽标（SetFaction←地图 faction 字段驱动；**徽标分层动画**（2026-09-15）：配「徽标分层表」的势力=底图静止+动层旋转——蒙德=官方原图无损拆层 `Resources/UI/Other/Faction/mondstadt_frame.png`（外框/尖饰/毂环原像素，含叶尖×框线交叉插值重建）+`mondstadt_windmill.png`（官方单片翼叶绕毂心 (511.5,474.4) 精确 60° 均匀复制六叶——用户拍板弃原版镜像三对非均匀排布"转起来露馅、大致即可"；Emblem 下 Windmill 子节点轴心偏移 (-0.12,+8.81) 对准毂心，动层转速 -40°/s 可调；AI 逐帧生成两试未过像素验收弃用——扩散模型做不了外框像素静止+精确旋转+无缝循环）；未配分层表的势力=整标缓转兜底（ElementFactionConfig.GetFactionIcon））+灰蓝词条文案（**多条随机池**，LoadingTip_*/02~05 各 Title/Line1/Line2，UIText 9066~9080，点击页面任意处换条=Backdrop raycastTarget+IPointerClickHandler）+底部元素行**逐像素填充扫描**（Dim/Lit 双层+RectMask2D 裁剪矩形从左往右展宽=原神式进度点亮；帧时长钳制 0.05s 防单帧尖峰瞬移+Reveal 等三段填充走完才揭幕，保底≈旋律时长）+**进度旋律**（2026-09-13：填充每越一个触发点播音符；蒙德现配=**原曲直截段**——用户拍板"直接用原曲截取旋律并加工尾部"，官方音源取本地 GI 解包转换版 webrefs/genshin-unpack/gichess/audio/bgmwav（bgmogg 同名 ogg），截《蒙德城繁忙的午后》0→2.05s（0~0.33s 原曲自带轻引子+主体 riff，2026-09-13 用户拍板砍短"大概 2 秒左右"）尾 0.4s 淡出=Resources/Audios/Loading/mondstadt_city_riff.wav 单条目整段播放；音符采样重建方案（长笛+吉他 21 条目分层）被否决"许多地方不像原曲"——机制保留于驱动（多乐器分层通用载体），试验采样素材已清删（可由"字母谱→Philharmonia 采样"链路重制，见 gic-audio skill），后续势力亦可走该链路；填充**三段制**（拍板"丝滑优先"）：正常段 0→卡点 85% 承载旋律（`填充时长`=正常段时长≈音乐段时长，选段 ≤3s，现 1.55s 与 2.05s 截段同收）→加载未完**卡点停住**（进度条停 85% 明示加载中）→Reveal（BattleScreen 装配完）=真实加载完成放行→0.3s 扫尾至 100%→揭幕；未配旋律的势力=静默）+右下版本号，复刻用户提供的原神加载页截图）+**列表页背景视差**（BackgroundParallaxUI=大厅 BackgroundParallax3D 的 Canvas 版同义，挂大图 Image；外层 wrapper 已按名排除滑动组=背景板静态起落（2026-09-13 拍板，GlassPanelAnimator 自动收集排除 BackDim/Background 结构层），内层图归视差驱动）；可见期事件绑定（Coop：OnShow 绑/OnDisable 解）。新建界面走 gic-new-screen skill Checklist A |
| BattleScreen | 根·独立 | 战斗战场（独立根场景，SceneType Single 加载；UIManager 监听 Single 加载自动入池全部面板）：**透视 55° 斜俯视相机**（FOV45，depth 10 solid color）+ BattleRoot（Session 装配 + BattleBoard；**Start=AssembleRoutine 分帧装配**——建盘/立牌/绑定逐帧摊薄，2026-09-13 拍板"加载不阻塞"防加载页动画冻结；重活首次成本由建房期 SceneFadeOverlay.PreWarm 预热吃掉：加载页实例化+UnitFactory/SkillFactory 初始化+首枚单位实例化缓存）。唯一入口：联机界面创建房间→开始游戏（单人：真人 P1 + AI 补位 P2，地图下拉=Configs 清单）；**战斗独立时间系统**（2026-09-14 用户拍板：TurnFlowController 战斗时钟——初始 6:00 整、每回合结束 +20 分钟，时段边界沿用 TimeUtility 8:00-20:00，BattleTimePeriod 驱动音乐选池）；**战斗地图音乐**（2026-09-14：BattleMapConfig.position 绑定大地图位置（星落湖→StarfellLake），装配完毕揭幕处起**轮换链**——播完→间隔 10s→按战斗时段重选池→随机下一首（参考大厅位置曲模式），**昼夜跨界立即淡切**（2026-09-22：回合结束 +20 分钟跨界瞬间=进选择阶段，在播曲若属旧时段→AudioManager.SwitchMusicWithFade 淡出旧曲+淡入新池曲；间隔冷却中不插手、留给下次选曲），_battleMusicActive 防退出续播+回合阶段事件自愈重试，池空仅 Warn；退出 StopMusic 硬切+回大厅钩子复活位置曲，链路见 gic-audio skill「战斗地图音乐」节）；关闭不走 GoBack 直接 MainHall.Load()。技术设计见 docs/active/22 |
| PaimonPet | 桌宠专用 | 不走 SceneType 跳转：`--pet-mode` 启动时 GameScene 守卫直接 LoadScene 本场景；GI 官方模型+PetWindowController（窗口体制见 docs/19 §3.1）。PaimonPreview 为其调姿用母版（不打包）。**单场景方案（2026-08-24）**：动作测试面板 AnimUICanvas 由 PetSceneSyncTool 重建（挂 PetEditorOnly 门：编辑器 Play 可见/构建版自禁用），PaimonToon 材质不读灯 |

编辑器启动选项（Tools/编辑器启动/）：自动全屏 / 从 Boot 启动 / ParrelSync 同步 Clone Play。

## 5. 配置资产

- `Resources/Configs/`：UnitConfig / ItemConfig / PositionConfig / MapConfig / ElementFactionConfig / WishPool_*.asset / **BattleMapConfig**（战场地图：字符行布局，BattleMap_FirstMap 为第一张图；联机界面地图下拉=本目录全部 BattleMapConfig 实清单） —— 均有自定义 Inspector（列表型走 ConfigInspectorBase，批量工具在 Inspector 底部）
- `Resources/Configs/Skills/`：**技能配置资产（2026-09-23 独立化）——每技能一个 SkillConfig SO（data=SkillData：skillID/icon/skillType/customParams/timeline）**；UnitConfig.unitDataList[].skills=引用列表（顺序=skillIndex 语义勿重排）；通用技能（Common_Walk 等 8 角色）共享单资产；修改技能参数=改资产本体（UnitData 编辑窗选中技能即编辑资产）而非 UnitConfig；`Resources/Configs/SkillTimelines/`=时轮时间轴（技能表现/判定规格，SkillTimelineEditor 编辑）
- 角色数据源头：`docs/units/{势力}/{角色名}.md`（按势力分目录，AI 读取后同步 enum+asset+本地化，见 gic-new-unit skill）
- Addressables（Assets/Art/）：`PositionBack/{Region}/{pos}_{time}` 大厅背景、`WishArt/{unit}` 祈愿 4K 立绘
- Sprite Atlas ×6（Assets/SpriteAtlases/）：小 UI 图入 Atlas，大图不入

## 5b. 预加载地图（2026-09-21 全站点盘点；各预热/预载点：时机 × 内容 × 机制 × 入口）

> **半统一现状**：大资源（贴图/视频/瓦片）已统一收口 AssetCache（优先级队列+Persistent+帧预算+去重引用计数）；其余机制（GameObject 池化/工厂反射/场景预载）各自实现、时机各点自选，**无全局清单**——跨点协同（IO 带宽/内存峰值/时序重排连带）靠本文登记。**新增预热点规范**：Addressables 资源一律 AssetCache.Preload/LoadAsync（禁直调 Addressables，见 gic-assetcache skill），落点后同步入下表。

| 时机 | 站点（入口文件） | 内容 | 机制 |
|------|------|------|------|
| Boot 帧（容器构建） | Framework/ConfigManager.cs（[Bean] 懒加载，[PostConstruct] Init 直接触发全部） | 7 份配置资产同步 Resources.Load + BuildCache（Unit/Item/Position/StarVisual/ElementFaction/BattlePalette/InitialSave）；存档读档 | 同步 Load |
| Boot 帧 | Framework/PositionManager.cs（Boot 场景 [Component]，构造注入） | PositionConfig + **曲池 AudioClip 随资产引用隐式进内存**（无显式代码） | 隐式（资产依赖） |
| Splash.Start | UI/Screen/SplashScreen.cs | 大厅背景（Sprite/VideoClip 按位置+时段）+ WishArt/columbina 默认立绘 | AssetCache.Preload（Persistent+Low） |
| 进大厅帧 | UI/Screen/MainHallScreen.cs + MainHallScreen.Background.cs | 背景 LoadAsync(**High**) 上屏（与 Splash 的 Preload 同地址去重衔接）+ 大地图初始视野瓦片（UI/Screen/Map/MapTilePreloader.cs，Persistent+Low；传送换区域时补预载+卸旧） | LoadAsync / Preload |
| Splash 就绪+30 帧（2026-09-21 方案 B 重排） | UI/UIManager.cs PrewarmLoop | 4 面板（Settings→Backpack→Wish→Coop）Instantiate + alpha=0 两帧渲染态预热（窗内 blocksRaycasts=false 防隐形面板吃点击）+ 入池（每屏歇 30 帧摊成本；冷开 285.9ms→首开 74.4ms→池化重开 11.6ms，docs/14 §38）；**连带**：WishScreen.Awake → 全部角色 4K 立绘 AssetCache.Preload（随泵一并提前至 Splash）；泵未完遇根转场（SceneTransition 锁）挂起、转场毕 +60 帧再续；桌宠形态直载 PaimonPet 等不到 Splash=泵挂起 | GameObject 池化 |
| 建房等待期（点"创建房间"） | UI/Screen/Coop/CoopScreen.RoomFlow.cs → UI/SceneFadeOverlay.PreWarm | 加载页 prefab 实例化+TMP 首用烘焙两帧 + **战斗工厂**（UnitFactory.PreWarm 实例化一枚立牌销毁+SkillFactory.Initialize 反射注册）——唯一战斗入口（单/多人均经 Coop），全项目只跑一次 | GameObject+工厂预热 |
| 退场前（点地图按钮瞬间） | MainHallScreen.ExitWithPreloadCoroutine → GameScene.PreloadScene | Additive 场景先加载不激活、退场动画期间完成再 Activate（MapScreen 场景制弹层受益） | 场景级预载 |
| 动作前一刻（懒预热） | Wish 域：点祈愿按钮→WishDrawController.Video.cs PreloadStar5Video；切卡池淡出期→CharacterPanelController.PreloadSprite 并行预载新立绘；面板 OnEnable→WishAmbienceController 粒子 Simulate 预填充一个周期 | 按需 | Preload/LoadAsync/粒子预热 |

**机制盘点（6 种）**：① Resources.Load 同步（配置）；② AssetCache.Preload（Persistent+Low 队列）；③ AssetCache.LoadAsync（引用计数+High/Normal）；④ GameObject 池化预热（Instantiate+两帧渲染态——UIManager 面板泵/SceneFadeOverlay 加载页）；⑤ 工厂反射预热（UnitFactory/SkillFactory）；⑥ 场景级 Additive 预载（GameScene.PreloadScene→ActivatePreloadedScene）。

## 6. 常用工作流速查

| 任务 | 路径 |
|------|------|
| 新增/修改角色 | 编辑 docs/units/{势力}/{角色名}.md → gic-new-unit skill 同步 |
| 新增界面/场景 | gic-new-screen skill（SceneType 注册 + BuildSettings + ScreenBase 骨架） |
| 新增事件 | gic-eventbus skill checklist |
| 新增音效/BGM | AudioManager.PlaySFX/PlayMusic（界面配乐用 ScreenBase Safe；昼夜跨界淡切=SwitchMusicWithFade，时长=AudioManager Inspector「时段切换淡变」） |
| 新增大贴图 | gic-assetcache skill（Addressable 地址约定） |
| 批量本地化 | Tools/Localization/CSV 导出导入（gic-localization skill） |
| 导出测试 APK | Tools/导出 APK/快速导出（自动切 Android 平台） |
| 联机自测 | ParrelSync Clone 双开（Tools/编辑器启动/同步 Clone Play） |
| 桌宠构建/运行 | Tools/桌宠/构建 Windows 桌宠测试包 → `Builds\PetSpike\gic.exe --pet-mode`（构建判据/竞态守卫见 gic-pet skill；窗口体制/渲染现状见 docs/19 §3） |
| GI动画→MMD重定向 | Tools/桌宠/方案B: GI动画重定向到MMD骨架——GI .anim 转 MMD 骨骼 clip（`Animations/MMD/`，legacy，97 条全量）；**配置在 RetargetConfig.asset**；现为 MMD 兜底路线（现行=GI 官方模型直读 199 条，重导出后三步修复见 gic-pet skill）；动画异常排查先跑腿部/全身/手指审计，详见 gic-paimon-retarget skill |
| 派蒙渲染调整 | `Shaders/PaimonToon.shader`（自包含卡渲：ramp/SDF/描边，不读场景灯）+ GI 材质 `Art/PaimonPet/GI/Materials/`；**现行 v8=纯官方贴图×基色暗化描边**（光照全关，Inspector 可调回）；材质接线=Tools/桌宠/GI官方模型接线菜单（幂等），详见 docs/19 §3.2 |
| 提交 | git-commit-helper skill（PowerShell -m 破坏中文，用 -F 文件） |

**导出 APK 要点**：Tools/导出 APK/快速导出（QuickAPKBuilder，Mono+ARMv7，约 3 分钟）自动切 Android 平台——**必须切**，否则 Addressables 按 Windows 打包（DXT/Windows 路径）→ 真机黑屏无声音+体积大 200MB；构建后**不切回**原平台。也可直编 ProjectSettings.asset 三字段：scriptingBackend.Android(0=Mono,1=IL2CPP)、AndroidTargetArchitectures(1=ARMv7,2=ARM64)、managedStrippingLevel.Android(3=Low)。APK ~270MB vs Windows ~2GB 正常；编辑器崩溃日志=%USERPROFILE%\AppData\Local\Tuanjie\Editor\Editor.log。

## 7. 战斗系统现状与规划（速查至 2026-09-26；批次规划=docs/18 决策五/六，逐批拍板与返修记录=docs/18，实现要点=docs/active/22/28/29，陷阱=docs/14，开口项登记=docs/11）

> 本节只记「现在是什么」。各批次的落地过程、拍板原话、返修史不再在此展开——docs/18 按批次收录拍板记录，施工蓝图（docs/active/22/28/29）含实现要点，docs/14 收陷阱，git 历史留全过程。

**已落地系统速查**：

| 域 | 现状要点 | 深入 |
|----|----------|------|
| 单位与卡牌 | Unit 组件容器（Stats/Identity/GridPosition/Element/Inventory/Moveable/Status/UI）+ UnitFactory/SkillFactory（[SkillAttribute] 反射注册；三层分流：注册类→ConfiguredSkill（effects 非空=数据驱动）→UnimplementedSkill 占位保 skillIndex 对齐）；Unit.prefab=Resources/Prefabs/Units/；Card.prefab 全项目唯一卡牌形态（两策略 InitCardDisplay 必须全量声明 overlay 态；新立绘放方角原图勿再裁圆角；未拥有角色卡=半透明遮罩仍可点详情）；星级血量回落=1★100/2★150/3★200/4★300/5★600（GetHPByStarLevel，baseHP 均 Unspecified）；技能参数=value+baseType 二元组（描述渲染=SkillDescriptionBuilder，基准语义纪律=docs/20 §5.1） | gic-new-unit skill |
| 协议与执行层 | Data/Battle/Protocol/：BattleCell/ActionData（ActionType 12 种）/BattleCommand（union+visibilityScope 迷雾预留）/Segment/BattleSnapshot（UnitState+PlayerResourceState 含 handCards）/BattleMapData（平铺地形 int+ForceType）/8 消息类；JsonUtility 平铺（无 Dictionary）+IBattleSerializer 隔离，LocalBattleTransport 同进程也走 JSON wire（B7 换 Mirror 上层零改动）。执行层 Battle/Flow/：TurnResolver（攻速分桶→片前快照→瞬发独立结算→移动同步逐步展开→效应统一应用→死亡判定→Segment 推流→ack 门控 15s 超时快进）、MovementResolver（体积绝对层→阻挡规则层→地形层；同片按轮推进、对穿/同格共存/被挡停格前）、DamagePipeline（乘区公式 docs/05 §5.6，无 hooks）、TurnFlowController（选择/执行两阶段循环+选择倒计时+OnSelectTimerExpired）、BattleSimState/BattleSession/BattleLaunchConfig（consume-once，唯一入口=联机建房→开始游戏） | docs/active/22 |
| 判定体系（B5） | 受击体=立牌真实大小圆柱（直径 0.42=BattleMetrics.UnitCylinderDiameter，底座圆盘可视化），判定与格脱钩；移动中单位连续插值位置可被中途命中（所见即所得）；投射物=ProjectileResolver 时间轴分段求交（24 格上限/虚空消散产 Effect 命令；Damage 命令带命中点千分定点 hitX/hitY）；移动 0.18s/格、投射物 8 格/秒（Host 与播放同源，常量收口 BattleMetrics）；纯瞬发仍按片初快照 | docs/18 决策四、active/22 §11 |
| 时轮系统（B-S1/S1b/S2） | SkillTimelineAsset 七轨 clip（挂 SkillData.timeline，null=兜底旧行为）；分工铁律=时间与规格归时轮、数值归 SkillParamKey；移动技能化（移动行动同产 SkillCast；步数=SkillData.ResolveMoveDistance 单出口，基准换算 10%×移速；瞄准=十字四向）；命令 launchMs 双语义（投射物发射延迟/命中类效果应用时刻，0=立即）；SkillCast 命令=过门槛施放即产（B-S3 表现轨起点）；编辑器=Tools/TG/时轮编辑器 | docs/18 决策八、active/28 |
| 效果原子库（B-1/B-2/B-2c） | SkillEffectConfig union 原子（触发 OnCast/OnHit/OnVanish × kind 七种：Damage/Heal/ApplyBuff/AttachElement/EnergyGain/TriggerSkill/MoraPlunder + 势力筛选器 AllAllies/CasterRadiusAllies 等）挂 SkillData.effects；EffectCompiler 编译期展开；**加技能=配 effects+时轮零代码**（新判定形制才扩编译分支）；预判 WouldHitEnemyInDirection 与结算同形（判定 kind 工厂强制；特殊形态走技能类覆写同语义纪律）；治疗换算单出口=ResolveHealAmount（BasedOnMaxHealth=被治疗者各自/BasedOnAttack=施法者）；伤害按 baseType 分流（BasedOnMaxHealth=施法者上限→FlatDamage 加法区）；编辑器=UnitConfigEditor 效果原子编辑区 | docs/18 决策九、active/29 |
| Buff | BaseBuff 回合制计时（回合结束减一含施加当回合，docs/04 §4.4）+同类合并（时长累加/级别取大）+全局注册表；StatBuff 基类收口数值型（AttackUp=3/MoveSpeedUp=4，移速含 Buff 直连移动距离换算链）；BurnBuff（每回合末 10 火伤×级别 3 回合）；ApplyBuff/RemoveBuff 命令+头顶徽章行；TriggerSkill 防环（MaxTriggerChainDepth=3） | docs/06、docs/18 |
| 元素与反应 | 登场无附着（UnitElement 初始 Physical）；附着覆盖=消耗被反应附着；融化/蒸发=+50%×级别并入增伤乘区（易伤/增伤分区）、冻结=FreezeBuff 硬控；ReactionEffect=反应事实载体（SkillHitResolver.Hit 唯一产出点，覆盖瞬发/箭雨/投射物三链）；客户端附着小图标（血条左侧）+冻结冰色即时同步；反应名前缀仅增伤反应（「蒸发 N」「融化 N」，UIText 12028/12029） | docs/06、docs/18 |
| 元能/体力/摩拉（B6a/B6d） | 元能：移动 +10（被挡也算）/战技首次命中 +10（同片按目标去重）/爆发延奏命中不获能/爆发消耗=EnergyCost（SkillParamKey 9，门槛=消耗值非上限）；体力：开局 60/回合 +5/移动·战技·爆发各 10（StaminaGate：星级≥3 才扣、1~2 星豁免、延奏/契约 0、不足落空同元能）；摩拉：开局 200/回合 +5/部署扣费 TrySpendMora；常量收口 BattleMetrics；StatChange 命令按 metadata 分流（Mora/Stamina 的 targetUnitId=玩家 ID，先于 _views 单位查表——docs/14 §78）；HUD 左上角=myinfo 槽 MoraChip/StaminaChip（ItemCounterChip 公用组件） | docs/18 决策七/十 |
| 部署+手牌（B6c） | 手牌=玩家存档当前卡组完整投影（角色卡+物品卡；HandCard 条目 count 一等属性；货币牌数量=资源池镜像：>0 必有卡/空堆移除/发放复活；GainCard/LoseCard 公用对偶）；物品卡点击=占位提示不进部署链；部署=核心半径 2+碰撞判定链（体积绝对层/互不阻挡/地形，与移动同构——DeployUnitExecutor.IsDeployCellValid）；空卡组回退丘丘人×2；卡不消耗可重复出战 | docs/18、gic-battle-hud skill |
| 战斗 HUD | 结构真源=BattleHud.prefab（Slots/{key} 槽契约、槽内首子级=控件；四分件=主/TopBar/Build/Layout）+布局编辑模式（14 件可拖可缩、三方案入存档 settings、编辑期冻结倒计时）；表驱动四键（移动=特殊技能 skills[Move]，图标随常态移动类型）；点击式瞄准=金色待定+提交制（完成选择按钮=确认/Pass 唯一入口、确认即定死、超时=OnSelectTimerExpired 无条件复用按钮链路、KeyAction.Confirm 空格/回车快捷键）；拖动式瞄准=轮盘（大圆盘=方向距离转盘、小圆盘贴身不出盘、判定基准=盘心、键心死区=小盘半径、金格出屏=SetDragFollowTarget 相机跟随）；瞄准高亮=推荐分色（可选推荐青蓝 #4CC2F1/不推荐红，BattlePalette.瞄准已选色）；手牌下沉热区（指针接近上移）；倒计时=61.2 号+SDF 黑描边 0.08+<5s 红色脉动；顶部节奏=回合(0.99)→confirm(0.95 屏中)→时钟(0.91)→倒计时(0.5,0.85)→queue(0.81,0.85 右上) | gic-battle-hud skill、docs/18 决策六 |
| 表现层 | 头顶条+伤害数字=屏幕空间层（独立 Overlay 画布 sortingOrder 38/39：血条=队伍色圆角黑边+量子刻度每 50 血一段上限 10 段、元能条=白色 10 段、附着图标随血条左缘；伤害数字=对数尺寸/停留时长映射+屏幕边缘夹取+SDF 描边 0.08，治疗「+N」/伤害「N」/反应「蒸发 N」）；立牌=两态模型（选择阶段散开/执行阶段收拢重叠格心）+55° 后仰+全身版 2.5×；被挡移动=撞墙弹回；世界 quad/Unlit/世界 TMP 唯一出口=BattleViewFactory（材质持有方释放）；配色单源=BattlePalette.asset；补间=BattleViewTween（末帧保证 t=1）；全灭软停=BattleOver=9+胜负提示（12031/12032） | docs/18 决策六、gic-battle-hud skill |
| 地形与水面 | 三地块 mesh 扩满 1×1 消缝；水面=BattleBoard.BuildWaterSurface 整片焊接单 Mesh（世界坐标 UV+「水面细分」顶点加密波浪，材质由水块材质槽 1 携带）；WaterTile 只剩 0.30 湖床（四壁/顶面 submesh 退役为空）；视觉表面高度单出口=GetVisualSurfaceHeight（水格含波峰带；CellToWorld/GetDecalHeight 全走它），GetSurfaceHeight=拾取/Host 名义面（勿掺波峰带）；BattleWaterFlow 队列=Transparent-1（2999）保一切透明件恒画于水面之上；板面点击拾取=TryPickBoardCell 两遍收敛（视觉面高度，docs/14 §86）；shader=BattleGrassSway/BattleWaterFlow（Assets/Shaders/）；纹理=Assets/Art/Battle/Textures/ | docs/14 §89 |
| AI 双脑 | AI 玩家脑=AIDebugBrain v2 全候选评分制（只操控 3~5 星；攻击=CanCast/元能/体力三门槛+十字逐向预判+伤害/斩杀/获能评分；延奏=蒙德或自身+治疗缺口/增益估值；移动=十字逼近+射击线加分；Pass 兜底；常量收口类头部）；低级单位脑=LowUnitBrain v3/v4（1~2 星自主行动不占玩家配额；移动=BFS 最短路首步绕行+打不了换目标巡逻；Host 在 BeginSelectPhase 生成先于玩家选择）；共享原语=BattleHeuristics（CrossDirections 唯一方向域、PreviewLineTargets 镜像编译器、IsMondstadtUnitName 单出口）；测试军=BattleScreen 双方各 1 安柏/凯亚/芭芭拉/丘丘人（对称镜像） | docs/18 决策十一 |
| 交互与防线 | 任意单位可选中查看（含敌人/低级——技能盘/详情/瞄准全开放，目标域按选中单位队伍；查看态技能键不置灰=IsSelectedControllable 单源）；提交防线=SubmitAim 拦截（toast Battle_NotYourUnit/Battle_MinorUnit，拦截后保持瞄准态）+Host 星级/归属双保险；敌我判定=TeamType 口径、操控权/资源归属=playerId（docs/14 §81）；死亡不清任何东西（BurnBuff 烧尸体飘数字=预期）；退出须经 BattleExitConfirmDialog 确认（ESC/右键/结束按钮，IClosable 栈顶） | docs/18 决策十二/C1 |

**仍空缺（要点，完整清单与登记=docs/11）**：元素层数系统、燃烧正式来源（火+草）、箭矢正式素材/箭雨天降视觉/震屏闪白、战斗 UI SFX、B-S3 客户端表现轨消费+素材路线、B-S2 编辑器增强（灰盒预览 scrub）、回合四阶段完整流程、召唤系（凛冽轮舞）/复苏（闪耀奇迹）/OnVanish/条件计数器/形状库/护盾类换算、资源点系统、B7 LAN（双端同构债/组合根域内化/布局编辑联机处置/Host 方向校验）、B8（建筑卡/物品卡使用/迷雾/协议核心 Unit 化/命座/敌方资源显示）、AI 玩家脑 EvaluateMove BFS 直线段、固定测试军退役时机。

**架构参考**：MC mod gichess 旧 Java 实现（~20+ 角色，源码 D:\Game\mod\wg-template-1.21.4\src\main\java\com\wg\gichess\，仅架构参考——旧 mod 资产一律不再用）；时轮/效果原子库学习源=EGamePlay（m969/EGamePlay，MIT；本地快照=.codely-cli/webrefs/skill-timeline-system/EGamePlay_src/——取概念模型勿抄运行时，对照表=docs/active/29 §2）。

**铁律**：战斗逻辑不走 EventBus（回合制严格串行）；命令粒度=片级原子事件；Host 永不跑在客户端前面（ack 门控）；片内瞬发效应快照结算、移动同步逐步结算（docs/active/22 §1-2）；效应→命令对账=BattleEffectCommandAudit（每种已结算效应必产对应命令，新 BattleEffect 子类不登记映射默认报警）。

## 8. 开发环境备忘

- 编辑器：D:\Tool\2022.3.62t11\Editor\Tuanjie.exe；ADB 在其 AndroidPlayer\SDK 下
- Tuanjie 与标准 Unity 的 API 差异（UITK/EditorPrefs/SpriteAtlas 等）：docs/14 + gic-editor-tool skill 陷阱表
- 构建配置：测试 Mono+ARMv7（~3min）/ 发布 IL2CPP+ARM64；直接编辑 ProjectSettings.asset
- Android 包名 com.HGAME.gic；TextMesh Pro/ 目录 git 忽略（材质改动不入库）
- 删档测试：Tools/存档/删档测试模式（编辑器专用，打包恒关）
